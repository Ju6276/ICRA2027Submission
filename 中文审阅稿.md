# FoRe-VLA 中文审阅稿（对应远程最新版本）

> 对应英文提交：`1c48d70`。本文档用于审阅论文的研究主线、贡献边界、问题定义、方法和实验设计，不是逐句直译。

## 标题

英文标题：

**FoRe-VLA: Only the Residual Needs to Be Fast — Distilling Force-Attributable Corrections for Sensor-Rate Contact-Rich Manipulation**

建议中文理解：

**FoRe-VLA：只有残差需要快速更新——面向传感器频率接触操作的力条件修正蒸馏**

这里的 *force-attributable* 应谨慎理解为“教师在真实力条件和空力条件下产生的输出差异”，而不是严格的因果力贡献。

## 相比上一版发生的核心变化

1. 论文的核心贡献不再是“null-force token”本身，因为学习式缺失模态 token 已经有相关工作。
2. 核心贡献也不再是一般性的慢—快架构，因为 FAVLA、LIFT 等工作已经采用了类似结构。
3. 新版的核心论点是：现有快速分支仍然在重新生成完整动作，而 FoRe-VLA 只让快速分支学习由力条件引起的动作偏差，因此可以使用显著更小、更快的网络。
4. 不再蒸馏一个独立的慢速 nominal student。冻结教师的 null mode 直接作为部署阶段的慢速参考策略。
5. 两次教师采样不仅共享视觉—语言 prefix，还共享完全相同的 flow-matching 初始噪声，从而避免采样随机性污染残差标签。
6. 快速学生只输出 9 维位姿残差，不修正夹爪通道；夹爪动作由慢速语义分支决定。
7. 蒸馏损失从原来的四项缩减为 residual loss 和 composition loss 两项，不再对学生加入专家行为克隆损失。
8. 新增随机慢速周期、推理延迟和 jitter 的训练机制，并通过 context-age time token 让同一个学生适应不同部署频率。
9. 新版明确提出反证标准：如果 100 Hz 相比 30 Hz 没有改善接触结果，或者学生不使用力也能预测相同残差，那么论文的核心论点不成立。

## 中文摘要

接触丰富的机器人操作要求策略在一个控制周期内完成修正，但当前慢—快式力感知视觉—语言—动作模型的快速分支仍然需要重新生成完整的动作序列。即使视觉—语言 prefix 已经完全缓存，每次快速更新仍需要执行完整的 flow-matching 去噪过程。我们在 RTX 4090 上测得该过程需要约 76 ms，即仅能达到 13 Hz，比 100 Hz 力传感器的采样频率低一个数量级。

我们的核心观点是，高频分支并不需要重新生成完整动作，只需要预测动作中由力条件引起的修正部分。该修正函数比完整动作生成更简单，因此可以由显著更小的网络实现。然而，机器人示范只提供最终动作标签，并不提供其中“力条件修正量”的独立监督。

为构造这一标签，我们对冻结的时序力感知教师模型进行成对查询。在相同观测、相同视觉—语言 prefix 和相同 flow-matching 噪声下，一次输入真实测量的力历史，另一次输入学习得到的空力条件。两个输出之间的偏差被用作快速学生的监督目标。教师在空力条件下生成的动作序列则直接作为慢速参考，因此无需额外蒸馏独立的标称策略。

最终的快速学生由因果力编码器和单个注意力模块构成，包含 44.7 M 参数，只需一次确定性前向推理。其推理延迟为 2.4 ms，对应 417 Hz；与十步 flow-matching 动作专家相比，高频路径的计算时间缩短了 31.8 倍。生成训练目标时，我们随机化慢速更新时间、推理延迟和时间抖动，并将当前缓存上下文的实际年龄输入学生，从而使同一个学生能够适应训练中未固定出现的慢速分支频率。当前论文报告延迟分析和离线策略分解诊断；完整的机器人任务性能仍需通过后续硬件实验验证。

## 中文引言

视觉—语言—动作模型在通用机器人操作中展现出较强的语义推理和任务泛化能力，但接触丰富的操作仍然困难。视觉无法直接揭示接触力、初始滑移、冲击以及接触稳定性，因此 ForceVLA、ForceVLA2 和 TaF-VLA 等工作开始将力/力矩或触觉信息引入 VLA。真实物理反馈在这类任务中不仅是辅助信息，也是精确、安全交互的重要依据。

但是，向 VLA 提供力输入并不意味着系统能够及时响应力变化。视觉—语言推理的计算开销较大，而力可能在一个控制周期内迅速变化。当前较常见的解决方案是按照执行频率拆分策略：慢速视觉—语言主干低频更新可复用的任务上下文，快速分支则结合缓存上下文和最新力测量生成动作。FoRe-VLA 接受并沿用这一慢—快部署结构。

现有方案的瓶颈在于，快速分支仍然是一个完整的动作生成器。例如，FAVLA 在缓存的 key/value 上下文上重复运行 $\pi_0$ 动作专家，LIFT 也会在缓存的视觉—语言 prefix 上刷新反应式动作专家。缓存虽然省去了视觉—语言前向过程，却没有消除动作生成成本：每次高频更新仍需执行完整的 flow-matching 去噪。

我们的实测结果说明，这不是一个可以忽略的工程细节。在 RTX 4090 上，prefix 已缓存时，动作专家的一次前向需要约 7.6 ms；标准十步 $\pi_0$ 采样需要约 76 ms，只能达到 13 Hz。即使只执行一步，也已消耗 100 Hz 控制周期中约 76% 的时间预算。因此，完整动作专家与 100–1000 Hz 力传感器之间的频率差距具有结构性。

FoRe-VLA 的出发点是：高频分支不需要重复生成完整动作。若控制命令写成参考动作与修正量之和，那么参考动作主要随语义任务阶段缓慢变化，可以从最近一次慢速更新的动作序列中插值得到；只有修正量需要随最新力测量重新计算。相比完整动作生成，力条件修正是一个更小、更简单的预测问题。

但这种分解面临监督困难。示范数据只包含总动作以及同步的力测量，没有标注动作中哪一部分应归属于力反馈。如果两个分支都用总动作训练，慢速分支可能吸收接触相关行为，快速分支也可能重复生成标称运动，导致分解欠约束。

我们利用冻结教师构造缺失的监督标签。首先训练一个接收视觉、语言、机器人状态和原生频率力历史的时序力感知教师。随后在相同观测下执行两次教师查询：一次输入真实力历史，一次输入学习式空力条件。两次查询共享同一个视觉—语言 prefix 和同一个 flow-matching 噪声，使力条件成为两个样本之间唯一发生变化的因素。真实力输出减去空力输出，得到教师对力条件变化产生的动作偏差。

学习式空力条件也用于避免 zero padding 的分布外问题。若直接输入零向量，教师可能因为收到训练分布之外的无效输入而性能崩溃，此时完整输出与零力输出之间的差异会混合“力信息贡献”和“模型失效”。新版方法通过在冻结教师上适配 null token 和低秩 null-mode adapter，使空力分支保持为一个有能力的无当前力测量模式。

教师的空力动作序列直接作为慢速参考；快速学生只学习完整力输出与空力输出之间的位姿偏差。该学生仅包含因果力编码器、一个注意力模块和线性残差头，使用一次确定性前向即可达到 417 Hz。训练目标生成阶段进一步随机化慢速分支周期、推理延迟和时间抖动，并将缓存上下文的实际年龄显式提供给学生，使一个模型能够跨不同部署频率运行。

本文的贡献可以概括为：

1. 提出按照“输出归因”而不是仅按照网络架构对力感知策略进行时间尺度分解。通过只让高频分支学习教师的力条件偏差，高频路径从 76 ms 降低到 2.4 ms，从 13 Hz 提升到可覆盖力传感器频率的 417 Hz。
2. 提出受控的成对条件差分：在冻结教师上适配学习式空力条件，并令完整力查询和空力查询共享视觉—语言 prefix 与 flow-matching 噪声，减少零填充造成的分布偏移和独立采样造成的随机误差。
3. 提出与具体频率无关的异步训练方式：在目标生成中随机化慢速更新周期、推理延迟和 jitter，同时让学生接收上下文年龄，从而适应不同的部署频率与延迟。

需要强调：成对差分、null token 和慢—快架构单独看都不是本文的新发明。论文真正要主张的新意是：**把这种条件差分用作跨时间尺度策略分解的监督信号，并利用更简单的残差任务显著缩小高频路径。**

## 中文相关工作

### 1. 力感知视觉—语言—动作模型

ForceVLA 将六轴力/力矩作为 VLA 的独立模态，并通过力感知混合专家结构与视觉—语言特征融合。ForceVLA2 引入阶段级力提示、原始力直连路径以及混合力—位置控制。TaF-VLA 研究触觉与力表示对齐，Tactile-VLA 和 VLA-Touch 将物理反馈与高层推理及控制结合。FD-VLA 则采取相反方向，从视觉和机器人状态中蒸馏力表示，以便部署时不使用真实力传感器。

这些方法主要研究力或触觉应该如何表示、对齐和融合。FoRe-VLA 假设训练和部署阶段都存在真实力测量，并将已有力感知 VLA 作为待分解的教师模型。

### 2. 反应式和异步物理反馈

ManipForce 通过频率感知表示保留异步 RGB 和力/力矩流；Reactive Diffusion Policy 将慢速视觉规划器与快速触觉控制器结合。FAVLA 使用慢速视觉—语言主干生成 key/value 上下文，并由快速动作专家结合最新力序列生成动作。LIFT 将反应式动作专家接入预训练策略，通过零初始化 cross-attention 注入力记忆，并缓存视觉—语言 prefix。Fast-in-Slow、DAM-VLA、AT-VLA、T-Rex 和 PhaForce 分别采用共享参数、原生频率模态更新、可变频率专家或阶段调度式快速修正。Compliant Residual DAgger 则使用在线执行和人工修正，为慢速基础策略学习快速残差。

FoRe-VLA 沿用慢—快部署结构，但有两个主要区别：

1. 现有方法的快速分支仍然生成完整动作，即使 prefix 已缓存，每次更新仍需运行完整动作专家；FoRe-VLA 则只让快速分支预测力条件位姿修正。
2. 现有方法通常在训练时固定频率比例或延迟偏移；FoRe-VLA 随机化这些时间参数，并将真实 context age 输入学生。

FoRe-VLA 与 Compliant Residual DAgger 的监督来源也不同：后者通过在线交互和人工修正获得残差，本文则完全通过冻结教师的离线条件差分生成标签。因此，本文不需要额外机器人交互，但也继承了离线数据无法覆盖策略执行分布偏移的局限。

### 3. 空条件查询与反事实差分

已有缺失模态工作使用学习式 token 替代缺失输入，避免将“模态缺失”与“有效的数值零”混淆；Masked Modality Projection 则利用剩余模态预测缺失模态表示。FoRe-VLA 使用学习式 token 的目的不是实现缺失传感器鲁棒性，而是构造一个不包含当前样本力测量、同时又尽量保持教师处于有效分布内的查询条件。

对生成模型进行条件与无条件差分也不是新概念。Classifier-free guidance 在推理时使用条件输出与无条件输出的差异增强条件控制，guidance distillation 则将组合后的 guided prediction 蒸馏为更便宜的学生。它们主要压缩同一频率、同一规模的生成过程，而不涉及将不同模态职责分配到不同时间尺度。

在具身策略中，Counterfactual Modality Attribution 和 Embodied Interpretability 使用反事实输入分析各模态对输出的影响；CofactVLA 用语言遮蔽分支减少视觉捷径；IDR 对冻结 VLA 进行 zero-padding 模态干预，测量输出变化并在推理时以 gated residual 方式融合。

FoRe-VLA 与这些工作的主要区别不是“首次使用差分”，而是差分的用途：已有方法用差分解释、去偏或调整一个单频率模型；FoRe-VLA 将差分用作独立快速模块的训练目标，以实现跨时间尺度的策略分解。与 IDR 相比，本文还使用适配后的学习式空力条件，而不是直接 zero padding，以降低无效输入造成的分布外退化。

## 问题定义

机器人接收视觉观测 $V$、语言指令 $L$、机器人状态 $S$ 和带时间戳的原生频率力/力矩流 $F$。这些信息天然异步：图像以相机频率到达，视觉—语言推理完成得更慢，而力信号通常快一到两个数量级。

动作在末端执行器坐标系中表示为：

$$
A=[p\in\mathbb{R}^3;\;r\in\mathbb{R}^6;\;g\in\mathbb{R}]\in\mathbb{R}^{10},
$$

其中 $p$ 是位置，$r$ 是旋转矩阵前两列构成的连续 6D 旋转表示，$g$ 是夹爪动作。使用 6D 旋转表示而不是欧拉角，是因为本文需要对动作做差；欧拉角在 $\pm\pi$ 处的分支跳变会产生虚假的 $2\pi$ 残差。

在当前控制时刻 $t$，最终命令分解为：

$$
A^{\mathrm{cmd}}(t)=A^{\mathrm{ref}}(t)+\Delta A(t),
$$

其中 $A^{\mathrm{ref}}(t)$ 从最近一次慢速更新产生的动作 chunk 中插值得到，$\Delta A(t)\in\mathbb{R}^9$ 只修正位姿。夹爪通道由慢速分支负责，因为抓取开合更接近语义决策，而不是高频反应。

这样分解的计算优势是：参考动作在语义时间尺度上变化，可以低成本插值；每当新力数据到达时，只需重新计算位姿修正。但监督困难在于，示范只给出总动作，没有给出 $\Delta A$ 的标签。本文的目标就是从冻结力感知教师中构造这一标签。

## 中文方法

### 1. 时序力感知教师

教师建立在 ForceVLA/$\pi_0$ 风格 flow-matching 策略上，并将瞬时力表示替换为因果时序编码器。对于教师推理时刻 $t_k$，从带时间戳的原生力流中读取固定长度窗口：

$$
\mathcal{F}_k=\{F(\tau)\mid t_k-w\leq\tau\leq t_k\}.
$$

窗口不先降采样到视觉频率，而是保留原始高频采样。有效性 mask 用于区分“窗口尚未填满”和“真实零力”。因果 TCN 将窗口编码为：

$$
z_k^F=E_T^F(\mathcal{F}_k).
$$

教师输出完整动作 chunk：

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F)\in\mathbb{R}^{H\times10}.
$$

时序力编码使教师能够观察接触起始、冲击瞬态、力振荡以及恢复过程，而不是只看到视觉帧时刻的一次力采样。

### 2. 学习式空力模式

为了在不提供当前力测量的情况下查询同一个教师，引入共享的学习式空力 token：

$$
z_{\varnothing F}\in\mathbb{R}^{D_F}.
$$

它代表“没有当前实例的力测量”，而不是物理零力。仅使用一个 token 可能使空力分支退化，因此在冻结全部教师权重后，同时训练该 token 和一个仅在 null mode 生效的 rank-$\rho$ adapter：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\,\sigma(W_{\mathrm{in}}h).
$$

adapter 只修正 flow velocity 的 9 个位姿通道，并利用教师原本的动作回归损失进行训练：

$$
\mathcal{L}_{\varnothing}
=d\big(T_\theta(V_k,L,S_k,z_{\varnothing F}),A_k^{\mathrm{expert}}\big).
$$

其目的不是另外训练一个无力 VLA，而是为同一个冻结教师建立一个能力正常的 null mode，避免差分主要反映 zero padding 导致的模型崩溃。

### 3. 成对反事实差分

冻结教师后，在同一个观测上执行两次查询：

$$
A_{T,k}^{\mathrm{full}}
=T_\theta(V_k,L,S_k,z_k^F;\epsilon),
$$

$$
A_{T,k}^{\mathrm{null}}
=T_\theta(V_k,L,S_k,z_{\varnothing F};\epsilon),
\qquad \epsilon\sim\mathcal{N}(0,I).
$$

两次查询共享：

- 完全相同的视觉—语言 prefix；
- 完全相同的 key/value cache；
- 完全相同的 flow-matching 初始噪声 $\epsilon$。

共享噪声是构造有效标签的必要条件。如果两次采样使用独立噪声，输出差异可能主要来自生成随机性，而不是力条件变化。

空力输出直接作为慢速参考：

$$
A_{T,k}^{\mathrm{nom}}=A_{T,k}^{\mathrm{null}}.
$$

力条件残差定义为两个动作 chunk 的位姿部分之差：

$$
\Delta A_{T,k}^{F}
=\left(A_{T,k}^{\mathrm{full}}\right)^p
-\left(A_{T,k}^{\mathrm{null}}\right)^p
\in\mathbb{R}^{H\times9}.
$$

这一差值不是对比学习，因为没有正负样本和对比损失；也不是严格的因果力归因，因为视觉和状态仍然可能包含接触线索。准确的表述应是：它表示教师在真实力条件替换空力条件后发生的策略输出变化。

### 4. 慢速参考与缓存语义意图

慢速分支就是冻结教师的 null mode，不再蒸馏额外的 nominal student。每次慢速更新产生：

- 空力条件动作 chunk $A_{T,k}^{\mathrm{null}}$，作为参考轨迹；
- 视觉—语言 prefix $H_k$，作为快速学生的语义上下文来源。

由于原始 prefix 包含数百个 token，方法通过固定的分段均值池化压缩成 $M$ 个 intent bins：

$$
Z_k^{\mathrm{intent}}=\mathrm{Pool}_M(H_k,m_k)\in\mathbb{R}^{M\times d}.
$$

快速分支不接收新图像，只通过缓存 intent 获得视觉和语言信息。因此，缓存的新旧程度必须明确提供给快速学生。

### 5. 快速力条件残差学生

快速学生读取最新力窗口，并通过因果 TCN 得到多 token 力表示：

$$
Z_t^F=E_S^F(F_{t-w_S:t}).
$$

学生的输入包括：

- 缓存的语义 intent bins；
- 最新高频力历史；
- 当前机器人状态；
- 当前插值得到的参考动作；
- 描述上下文年龄和动作插值位置的 time token；
- 一个学习式 residual query。

这些条件被视为无序集合，分别加入类型 embedding，但不使用位置编码。单个 pre-norm attention block 对条件进行融合，随后线性头从 residual query 的状态一次输出 $K$ 步位姿修正：

$$
\Delta\hat A_{t,1:K}=W_{\mathrm{res}}H_t[q]\in\mathbb{R}^{K\times9}.
$$

残差头采用零初始化，因此训练开始时控制器等价于恒等修正：

$$
A^{\mathrm{cmd}}=A^{\mathrm{ref}}.
$$

学生输出短 chunk 而不是单步修正，是为了应对学生自身推理延迟或执行器频率高于学生更新频率的情况。当新推理尚未完成时，执行器仍可使用上一个 chunk 的后续步骤。

### 6. 蒸馏损失

新版只训练快速学生，并只保留两个损失。

残差损失要求学生拟合教师的力条件差分：

$$
\mathcal{L}_{\mathrm{res}}
=\sum_{j=1}^{K}w_j\,
d(\Delta\hat A_{t,j},\Delta A^F_{T,j}).
$$

组合损失要求参考动作加学生残差后恢复完整力教师的位姿输出：

$$
\mathcal{L}_{\mathrm{comp}}
=\sum_{j=1}^{K}w_j\,
d\big((A_j^{\mathrm{ref}})^p+\Delta\hat A_{t,j},(A_{T,j}^{\mathrm{full}})^p\big).
$$

总损失为：

$$
\mathcal{L}
=\lambda_{\mathrm{res}}\mathcal{L}_{\mathrm{res}}
+\lambda_{\mathrm{comp}}\mathcal{L}_{\mathrm{comp}}.
$$

权重 $w_j\propto\beta^{j-1}$ 随未来步数衰减，因为第一步最常执行，后续步骤主要用于处理推理停顿。

新版不包含：

- nominal imitation loss，因为慢速参考直接来自冻结教师；
- 对专家动作的 student behavior-cloning loss，因为这会迫使快速学生吸收教师自身的模仿误差，而这些误差并不属于力条件修正。

### 7. 与频率无关的目标生成

如果学生只在固定慢速频率和固定延迟下训练，它会过拟合这一时间配置。因此，目标提取后，每个训练 epoch 重新随机生成异步调度：

- 慢速更新周期从指定范围随机采样；
- 推理延迟从指定范围随机采样；
- 每次更新加入随机时间 jitter；
- 尚未完成推理的 packet 不可被使用；
- 如果一个 packet 尚在传输中就被更新 packet 超过，则将其丢弃。

快速学生接收时间特征：

$$
\gamma_t=
\left[
\frac{t-t_k}{c},\;\alpha_t
\right],
\qquad
\alpha_t=\frac{t-t_j}{\tau_A}.
$$

其中 $(t-t_k)/c$ 表示缓存语义上下文的年龄，$\alpha_t$ 表示当前位于相邻动作 waypoint 之间的插值位置。这两个量分别描述“语义信息有多旧”和“动作 chunk 执行到哪里”。

### 8. 异步部署

部署时包含三个并行环节：

1. **慢速线程**：教师以 null mode 运行，发布参考动作 chunk、intent bins、观测时间戳和 packet 就绪时间。
2. **快速线程**：读取最新 packet、最新高频力窗口和机器人状态，生成带有效时间范围的残差动作 chunk。
3. **执行器线程**：在当前控制时刻采样最近的有效 chunk，并计算参考动作加残差；执行器不等待神经网络推理。

如果快速推理延迟，执行器继续使用上一残差 chunk 的后续步骤；如果没有任何未过期 chunk，则保持当前状态并报告错误，而不是盲目外推。

## 实验设计的中文说明

新版实验围绕四个问题组织：

1. 只学习力条件残差是否真的提高了高频路径的可执行频率？
2. 教师差分中是否存在足够、可学习的力相关信号，学生是否真的使用了力输入？
3. 以传感器频率执行是否比 13–33 Hz 的缓存 prefix 动作专家带来更好的接触结果？
4. 随机时间训练是否让一个学生迁移到不同的慢速频率和推理延迟？

当前已经有延迟结果：

| 高频路径 | 参数量 | 延迟 | 频率 |
|---|---:|---:|---:|
| 动作专家，1 步 flow | 334 M | 7.6 ms | 132 Hz |
| 动作专家，4 步 flow | 334 M | 30.4 ms | 33 Hz |
| 动作专家，10 步 flow | 334 M | 76.1 ms | 13 Hz |
| FoRe-VLA 残差学生 | 44.7 M | 2.4 ms | 417 Hz |

计划比较的 baseline 包括视觉—语言策略、瞬时力教师、时序力教师、缓存 prefix 的完整动作专家、直接慢—快行为克隆以及完整 FoRe-VLA。

主任务指标应包括：

- 每个任务和阶段的成功率；
- 峰值力与力冲量；
- 超力限制次数；
- 扰动恢复率和恢复时间；
- 残差误差与组合动作误差；
- 慢速/快速频率、端到端延迟和参数量。

决定性实验是固定同一个学生模型，只限制其执行频率为 13、30、50 和 100 Hz，比较接触峰值力、冲量、恢复时间和成功率。如果 100 Hz 相比 30 Hz 没有改善接触结果，那么更快的 417 Hz 网络并不能支持论文的机器人性能主张。

## 新版明确提出的反证标准

### 1. 残差信号幅值

令教师力条件残差能量为：

$$
\sigma_F^2=\mathbb{E}\lVert\Delta A_T^F\rVert^2,
$$

慢速参考陈旧造成的误差为：

$$
\sigma_R^2=\mathbb{E}\lVert A^{\mathrm{ref}}-A_T^{\mathrm{null}}\rVert^2.
$$

若 $\sigma_F^2/\sigma_R^2\geq1$，说明力条件修正至少与上下文陈旧误差相当，值得设置独立快速分支。若该比例低于 0.25，则组合误差主要来自慢速参考过旧，提高慢速分支频率可能比训练残差学生更有效。

### 2. 对力输入的真实依赖

相较 zero-residual baseline，完整学生带来的残差误差下降必须比 force-ablated student 至少高 10 个百分点。如果移除力输入后性能几乎不变，说明学生主要从 intent、状态和参考动作推断修正，论文“力条件快速分支”的前提不成立。

### 3. 接触局部性

完整学生相对 force-ablated student 的优势应当在接触阶段大于自由空间阶段。否则，学生学习到的可能不是接触驱动修正。

## 结论与局限

FoRe-VLA 的核心不是提出另一个慢—快架构，而是通过教师条件差分，将高频分支的任务从“完整动作生成”缩减为“力条件位姿修正”。这一缩减使高频路径从 334 M 参数、十步 flow 去噪变为 44.7 M 参数、一次确定性前向，并从 13 Hz 提高到 417 Hz。

但当前稿件仍有以下明确局限：

1. 完整力与空力输出的差异是条件差分，不是严格因果归因。
2. 学生性能受教师上限约束，教师的模仿错误不会由学生自动修复。
3. 残差监督完全来自离线示范，无法覆盖由学生自身执行引起的策略分布偏移；后续可能仍需少量在线 residual fine-tuning。
4. 加性位姿修正不一定能处理需要离散策略切换的接触事件。
5. 异步组合需要明确处理延迟、稳定性和安全限制。
6. 当前已经完成的是离线延迟与分解分析；机器人成功率以及 100 Hz 是否优于 30 Hz 仍待硬件实验验证。

## 审阅时最值得关注的三个问题

1. **“force-attributable”是否说得太强。** 正文已经反复限定为 conditional difference，但标题仍使用 attributable。需要确保审稿人不会把它理解为未经证明的因果归因。
2. **null mode 是否会让残差过小。** null token 和 adapter 都用专家动作训练，可能使 null 输出逼近 full 输出。必须报告接触/非接触阶段的残差能量及其相对 reference-staleness error 的比例。
3. **速度是否转化为任务收益。** 2.4 ms 和 417 Hz 只能证明计算效率。论文最终是否成立，主要取决于固定模型的 13/30/50/100 Hz 执行频率实验能否显示更低峰值力、更快恢复或更高任务成功率。
