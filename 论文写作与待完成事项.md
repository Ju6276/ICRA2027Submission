# ForceDelta-VLA：论文写作规范与待完成事项

本文档汇总当前论文修改过程中形成的写作规范，并记录 ForceDelta-VLA 在成为完整投稿论文之前仍需完成的工作。

## 一、论文写作规范

### 1. 标题

- 标题应准确表达核心技术贡献，避免只描述宽泛应用。
- 保留与研究对象直接相关的关键词，例如 VLA 与 contact-rich manipulation。
- 不将尚未得到实验验证的 high-frequency、real-time 等强结论写入标题。
- 方法名称应与核心机制对应，并在全文中保持稳定。

### 2. 摘要

摘要应依次包含：

1. 任务及核心困难；
2. 现有方法的结构性局限；
3. 本文方法；
4. 关键技术设计；
5. 主要实验结果。

写作时应注意：

- 不列出完整实验指标清单；
- 不写网络层数、训练参数等实现细节；
- 不罗列相关工作；
- 未完成的结果必须明确标为占位值，不能写成既成事实；
- 传感器采样频率不等于策略推理或执行频率，不能用 100 Hz 数据流暗示策略必然以 100 Hz 运行。

### 3. Introduction

推荐叙述顺序为：

> 应用问题 → 已有方法 → 结构性缺口 → 研究问题 → 方法概述 → 贡献

具体要求：

- 不连续列举大量论文名称；
- 不详细介绍任务、实验流程和完整指标；
- 不写“我们尝试了什么、后来如何修改”等研究或调试过程；
- 实验项目和评价指标不能作为方法贡献；
- 贡献应对应新的问题形式、学习目标、模型组件或执行机制；
- 不使用缺少实验支持的绝对判断；
- 不将运行时间降低自动解释为接触性能提升；
- 实验结果可以简要概括，但不能取代方法贡献。

### 4. Related Work

- 按技术路线组织，而不是按论文名单组织；
- 同类方法应合并描述，在句末集中引用；
- 只有代表独立技术路线的工作才需要在正文中点名；
- 每个小节应形成“技术路线—代表机制—共同能力—未解决问题”的递进；
- 避免写成“A 做 X、B 做 Y、C 做 Z”的逐篇摘要；
- 避免使用 `None of these works` 等容易被反例推翻的绝对表述；
- gap 应解释现有设计为何仍留下问题，而不是只强调前人没有采用本文方法；
- 不为增加引用数量而引入与本文 force/torque 设定无关的 tactile 工作；
- 明确区分 force-representation distillation 与本文的 residual distillation；
- Related Work 可以导向问题，但不应在每个小节重复介绍 ForceDelta-VLA。

当前三个小节的技术分工应保持为：

1. **Force-Aware Vision-Language-Action Policies：**直接力条件、力表征学习和力感知优化；
2. **Fast--Slow and Asynchronous Force Control：**视觉运动层和 VLA 层的多频率或异步处理；
3. **Residual Learning and Conditional Distillation：**残差监督来源及通过条件预测构造蒸馏目标。

### 5. Problem Formulation

Problem Formulation 只定义理解研究问题所必需的内容：

- 数据中包含哪些观测和监督；
- teacher 预测什么；
- reference 与 residual 如何组成部署动作；
- 示范中缺少什么监督；
- 本文需要解决的核心学习问题。

以下内容不应放在该小节：

- 单臂与双臂动作的完整维度展开；
- 6D rotation 的实现说明；
- 通道归一化统计量；
- 网络层数和训练超参数；
- 类似代码接口的张量记账。

这些细节应放在 Implementation Details 或补充材料中。

### 6. Method

- 方法行文顺序必须与真实训练流程和 Fig. 2 一致；
- 三个训练阶段应保持清晰：
  1. temporal force-conditioned teacher；
  2. learned null-force mode；
  3. residual target construction and distillation；
- learned null token 与 null-specific adapter 是第二阶段的两个组成部分，不是两个互不相关的阶段；
- 统一使用 `full-force mode`、`null-force mode` 和 `null-mode reference`；
- 避免混用 factual、counterfactual、semantic reference 等容易引起因果解释的术语；
- full/null prediction difference 是 model-defined conditional difference，不是力的唯一因果贡献，也不是普通对比学习目标；
- 介绍本文组件时，不应使用其他论文的设计代替本方法论证；
- 不记录 branch cut、orthogonalization 等调试过程；
- 伪代码只有在图无法准确表达执行顺序时才有必要保留，否则会与架构图或异步部署图重复；
- 所有公式、符号和图示必须使用同一套动作分解与阶段定义。

### 7. Figures

- Fig. 1 用于表达问题、核心思想和相对已有方法的差异，不应复刻完整网络结构；
- Fig. 2 必须与三阶段训练严格对应；
- Fig. 3 用于解释异步部署中的查询、完成、发布、缓存、时间对齐和 residual 更新；
- 图中不能保留已经从正文删除的模块、分支或损失；
- caption 应说明图所表达的机制或结论，而不只是复述标签；
- 主图可以占据第一页较大空间，但不能破坏标题、摘要和模板布局；
- 正文、图、caption 和伪代码之间不能出现架构不一致。

### 8. Experiments

推荐章节顺序为：

1. Hardware Platforms；
2. Tasks and Data Collection；
3. Baselines and Metrics；
4. Implementation Details；
5. Quantitative Results；
6. Qualitative Analysis；
7. Failure Analysis；
8. Ablation Studies。

指标与协议应满足：

- 主指标为每项任务成功率；
- 分别报告单臂和双臂任务的平均成功率；
- 每个方法、每项任务使用两个独立训练 seed，每个 seed 测试十次；
- peak force 在全部试验上统计，避免幸存者偏差；
- completion time 只在成功试验上统计；
- 明确定义任务计时起止点、力的计算方法和双臂统计方式；
- 定性分析必须由真实执行帧、力曲线、reference 和 residual 支撑；
- failure analysis 应使用真实失败轨迹及明确分类。

实验写作中应避免：

- 在 Tasks and Data Collection 中重复计算总轨迹数；
- 将测试随机化、成功判定等评价协议混入任务描述；
- 因为某个 baseline 开源而在正文中特别强调 `open-source`；
- 在效率不是核心主张时加入独立的大规模 computational-efficiency 比较；
- 用 runtime reduction 代替真实机器人性能证据；
- 用 provisional training、will be reported 等未完成语气写正式投稿正文。

### 9. Results、Limitations 与 Conclusion

- Quantitative Results 报告数值和统计结论；
- Qualitative Analysis 解释典型接触过程中的行为变化；
- Failure Analysis 说明方法在何种条件下失效；
- 结果分析不能只重复表格中的最高值；
- limitation 应对应真实方法边界，而不是泛泛而谈；
- future work 应从当前限制自然导出，并保持精简；
- Conclusion 应总结问题、方法和实验结论，不重新展开 Method 或 Related Work。

## 二、当前论文待完成事项

### A. 必须完成：真实实验与数据替换

当前稿件中的红色内容仍是假设值，需要全部替换为实验记录，包括：

- 最终硬件型号与传感器配置；
- 每项任务的成功判定标准；
- 最终训练超参数；
- 八项任务的成功率；
- 峰值接触力；
- 成功试验完成时间；
- 定性行为观察；
- 失败类型及出现次数；
- 全部消融实验结果。

在这些内容完成前，论文具备完整结构，但实验结论仍不能作为正式投稿结果。

### B. 验证 staleness target 的数学正确性

当前陈旧性目标为：

$$
\Delta A_{T,t}^{\mathrm{stale}}
=\mathcal P\!\left(A_{T,t}^{\mathrm{null}}\right)
-\mathcal P\!\left(A^{\mathrm{ref}}(t)\right)
+\Gamma(S_t,S_k).
$$

需要根据真实动作定义验证：

- $\Gamma$ 的方向和符号是否正确；
- reference 与 current prediction 是否位于相同坐标系；
- 相对位姿动作如何从 $S_k$ 转换到 $S_t$；
- rotation displacement 如何计算；
- 双臂状态变化是否逐臂独立转换；
- force target 与 staleness target 相加后，是否确实对应当前 full-force teacher prediction。

应在实现和数学推导一致后再保留“二者之和匹配当前 full-force teacher”这一结论。

### C. 验证 rotation residual 的几何定义

当前实现使用连续 6D rotation representation，需要明确：

- residual 是否直接在 6D representation 中相加；
- 或者先将 residual 转换为旋转，再在 $SO(3)$ 上组合；
- composed rotation 如何恢复为合法旋转矩阵；
- rotation loss 使用何种距离；
- reference、teacher target 与 student output 是否使用完全相同的旋转约定。

如果真实控制使用轴角、四元数、旋转矩阵或 Lie algebra，应按照实际实现重新定义相关公式，而不能仅在 Implementation Details 中笼统说明。

### D. 补充最接近的异步残差相关工作

需要核查并讨论与本文结构最接近的研究方向：

- asynchronous action-chunk generation and execution；
- time-aware residual correction，例如 A2C2；
- fast--slow VLA action pathways；
- inference/execution temporal alignment。

重点不是增加论文数量，而是回答：

- 现有方法如何处理推理延迟；
- residual correction 的监督来自哪里；
- 是否使用力/力矩输入；
- 是否从预训练力感知 teacher 离线构造 correction target。

不能将“异步 residual pathway”本身作为完全独立的新颖点；本文的主要区别应集中在 residual supervision 的构造方式及其与 force-aware teacher 的关系。

### E. 加强 baseline

除当前 $\pi_{0.5}$、ForceVLA、TA-VLA 和 ImplicitRDP 外，应考虑加入或实现以下对照：

- 完整 temporal-force teacher；
- null-mode reference only；
- teacher 按系统实际可达到的频率运行；
- 最接近的 asynchronous/time-aware residual policy；
- 可复现的 fast--slow force-aware VLA。

若外部方法没有论文、代码或无法公平复现，应明确原因，并使用等价的内部受控对照，而不是给出无法验证的复现结果。

### F. 报告 teacher、null mode 与 composed policy 的质量

至少需要分析：

- full-force teacher 的任务成功率；
- null-mode reference 的任务成功率；
- 加入 residual 后的 composed-policy 成功率；
- null-mode prediction 是否保持合理动作分布；
- full/null difference 是否主要出现在接触阶段；
- residual student 能否逼近 paired teacher target。

这些结果用于区分：

- 性能提升是否主要来自更强的 temporal-force teacher；
- null mode 是否提供了有效 reference；
- residual distillation 是否恢复了有意义的 force-conditioned behavior。

### G. 验证 shared flow noise 的必要性

当前 full-force 与 null-force teacher query 共享初始 flow noise。若这一设计用于减少生成随机性并稳定 paired target，需要通过以下证据验证：

- shared noise 与 independent noise 下的 target variance；
- 两种设置下的 residual fitting error；
- 两种设置下的最终任务性能。

若实验显示共享噪声影响很小，则不应将其作为核心技术设计强调。

### H. 补充异步部署的真实系统数据

为了证明 asynchronous execution 确实成立，至少需要报告：

- reference generator 的实际更新频率；
- residual policy 的实际更新频率；
- reference inference latency；
- residual forward latency；
- 部署时的缓存年龄或延迟分布；
- 新 reference 尚未完成时，系统如何保持连续控制；
- correction bound 的设置和触发方式。

这些数据用于验证部署机制，而不是将论文转化为单纯的效率比较。

### I. 完善实验协议

需要明确：

- 两个 seed 分别代表什么；
- 模型是按任务单独训练还是多任务联合训练；
- 测试初始状态、物体位置和接触条件如何采样；
- success 由人工判定还是自动判定；
- F/T 数据如何做 bias removal、重力补偿和滤波；
- 双臂 peak force 如何聚合；
- completion time 的起点和终点；
- 是否报告置信区间或统计显著性。

### J. 补充正式定性结果图

正文仍需要一张真实实验结果图，至少包括：

- 代表性执行帧；
- 同步 force/torque trace；
- null-mode reference；
- force-conditioned residual；
- composed command；
- contact onset、correction 和 recovery 的关键时间点。

该图应直接说明 residual 在何时介入、修正了什么，以及自由空间阶段是否保持接近零。

### K. 控制泛化主张

当前实验主要验证固定八项接触任务。如果没有额外泛化实验，不应暗示广泛的 VLA generalization。

若要强化 VLA 价值，可以增加至少一种受控泛化设置：

- 未见物体；
- 未见初始位姿；
- 未见摩擦或刚度；
- 未见语言表达；
- 未见接触扰动。

如果不增加泛化实验，论文应将结论限定为 contact-rich control improvement，而不是开放世界语义泛化。

## 三、建议完成顺序

1. 验证 staleness target 与 rotation composition；
2. 确定最接近的 asynchronous/residual baselines；
3. 固化硬件、训练和评价协议；
4. 完成主实验并替换全部红色内容；
5. 完成 teacher/null/residual 分解实验；
6. 完成 shared-noise 与其他核心消融；
7. 补充异步部署数据、定性图和失败分析；
8. 根据真实结果重写 Abstract、Introduction、Results 和 Conclusion 中的数值与结论。
