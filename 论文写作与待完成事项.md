# ForceDelta-VLA：论文写作规范与待完成事项

本文档汇总当前论文修改过程中形成的写作规范，并记录 ForceDelta-VLA 在成为完整投稿论文之前仍需完成的工作。

## 一、论文写作规范

### 1. 标题

- 标题应准确表达核心技术贡献，避免只描述宽泛应用。
- 保留与研究对象直接相关的关键词，例如 VLA 与 contact-rich manipulation。
- 不将尚未得到实验验证的 high-frequency、real-time 等强结论写入标题。
- 方法名称应与核心机制对应，并在全文中保持稳定。

### 2. 摘要

摘要应依次包含：

1. 任务及核心困难；
2. 现有方法的结构性局限；
3. 本文方法；
4. 关键技术设计；
5. 主要实验结果。

写作时应注意：

- 不列出完整实验指标清单；
- 不写网络层数、训练参数等实现细节；
- 不罗列相关工作；
- 未完成的结果必须明确标为占位值，不能写成既成事实；
- 传感器采样频率不等于策略推理或执行频率，不能用 100 Hz 数据流暗示策略必然以 100 Hz 运行。

### 3. Introduction

推荐叙述顺序为：

> 应用问题 → 已有方法 → 结构性缺口 → 研究问题 → 方法概述 → 贡献

具体要求：

- 不连续列举大量论文名称；
- 不详细介绍任务、实验流程和完整指标；
- 不写“我们尝试了什么、后来如何修改”等研究或调试过程；
- 实验项目和评价指标不能冒充方法创新；若单列为 empirical contribution，应强调验证范围与受控比较；
- 贡献应对应新的问题形式、学习目标、模型组件或执行机制；
- 不使用缺少实验支持的绝对判断；
- 不将运行时间降低自动解释为接触性能提升；
- 实验结果可以简要概括，但不能取代方法贡献。

### 4. Related Work

- 按技术路线组织，而不是按论文名单组织；
- 同类方法应合并描述，在句末集中引用；
- 只有代表独立技术路线的工作才需要在正文中点名；
- 每个小节应形成“技术路线—代表机制—共同能力—未解决问题”的递进；
- 避免写成“A 做 X、B 做 Y、C 做 Z”的逐篇摘要；
- 避免使用 `None of these works` 等容易被反例推翻的绝对表述；
- gap 应解释现有设计为何仍留下问题，而不是只强调前人没有采用本文方法；
- 不为增加引用数量而引入与本文 force/torque 设定无关的 tactile 工作；
- 明确区分 force-representation distillation 与本文的 residual distillation；
- Related Work 可以导向问题，但不应在每个小节重复介绍 ForceDelta-VLA。

当前三个小节围绕论文的技术争论组织：

1. **Force-Aware and Multi-Rate Manipulation Policies：**力感知完整动作生成与多频率更新解决物理信息如何进入控制，但不直接提供独立加性修正的离线监督；
2. **Residual Policy Learning：**已有残差方法如何通过奖励或额外在线纠正使修正可辨识；
3. **Policy Distillation：**缺失模态表示、完整策略蒸馏和 guidance distillation 分别支撑 null mode、紧凑 student 与成对预测差分。

### 5. Problem Formulation

Problem Formulation 只定义理解研究问题所必需的内容：

- 数据中包含哪些观测和监督；
- teacher 预测什么；
- reference 与 residual 如何组成部署动作；
- 示范中缺少什么监督；
- 本文需要解决的核心学习问题。

以下内容不应放在该小节：

- 单臂与双臂动作的完整维度展开；
- 6D rotation 的实现说明；
- 通道归一化统计量；
- 网络层数和训练超参数；
- 类似代码接口的张量记账。

这些细节应放在 Implementation Details 或补充材料中。

### 6. Method

- 方法行文顺序必须与真实训练流程和 Fig. 2 一致；
- 三个训练阶段应保持清晰：
  1. temporal force-conditioned teacher；
  2. learned null-force mode；
  3. residual target construction and distillation；
- learned null token 与 null-specific adapter 是第二阶段的两个组成部分，不是两个互不相关的阶段；
- 统一使用 `full-force mode`、`null-force mode` 和 `null-mode reference`；
- 避免混用 factual、counterfactual、semantic reference 等容易引起因果解释的术语；
- full/null prediction difference 应直接定义为 teacher-induced residual target；除非论文提出因果结论，否则不主动引入 causal attribution 等额外解释框架；
- 介绍本文组件时，不应使用其他论文的设计代替本方法论证；
- 不记录 branch cut、orthogonalization 等调试过程；
- 伪代码只有在图无法准确表达执行顺序时才有必要保留，否则会与架构图或异步部署图重复；
- 所有公式、符号和图示必须使用同一套动作分解与阶段定义。

### 7. Figures

- Fig. 1 回答为什么需要显式 correction supervision：完整动作预测中的功能纠缠与监督缺口；
- Fig. 2 回答 correction target 如何通过三阶段训练构造并蒸馏；
- Fig. 3 回答 residual 如何利用 cached reference/context 与 current force/state，在 reference query pending 时独立执行；
- 图中不能保留已经从正文删除的模块、分支或损失；
- caption 应说明图所表达的机制或结论，而不只是复述标签；
- 主图可以占据第一页较大空间，但不能破坏标题、摘要和模板布局；
- 正文、图、caption 和伪代码之间不能出现架构不一致。

### 8. Experiments

推荐章节顺序为：

1. Experimental Setup：Hardware, Tasks, and Data Collection；Baselines and Evaluation Protocol；Implementation Details；
2. Main Results：Task Performance、Contact Quality，以及定性与失败分析；
3. Independent Residual Execution：runtime 与 reference-delay robustness；
4. Ablation Studies。

指标与协议应满足：

- 主指标为每项任务成功率；
- 分别报告单臂和双臂任务的平均成功率；
- 每个方法、每项任务使用两个独立训练 seed，每个 seed 测试十次；
- peak force 在全部试验上统计，避免幸存者偏差；
- completion time 只在成功试验上统计；
- 明确定义任务计时起止点、力的计算方法和双臂统计方式；
- 定性分析必须由真实执行帧、力曲线、reference 和 residual 支撑；
- failure analysis 应使用真实失败轨迹及明确分类。

实验写作中应避免：

- 在 Tasks and Data Collection 中重复计算总轨迹数；
- 将测试随机化、成功判定等评价协议混入任务描述；
- 开源不能被写成方法优势；但可以在说明 baseline 可公平复现时简洁标注 publicly reproducible；
- 在效率不是核心主张时加入独立的大规模 computational-efficiency 比较；
- 用 runtime reduction 代替真实机器人性能证据；
- 用 provisional training、will be reported 等未完成语气写正式投稿正文。

### 9. Results、Limitations 与 Conclusion

- Quantitative Results 报告数值和统计结论；
- Qualitative Analysis 解释典型接触过程中的行为变化；
- Failure Analysis 说明方法在何种条件下失效；
- 结果分析不能只重复表格中的最高值；
- limitation 应对应真实方法边界，而不是泛泛而谈；
- future work 应从当前限制自然导出，并保持精简；
- Conclusion 应总结问题、方法和实验结论，不重新展开 Method 或 Related Work。

## 二、当前稿件已落实的修改

- 新颖性已从泛化的 fast--slow 或 asynchronous control 转为 **residual supervision**：从冻结 force-aware teacher 的 full-force/null-force 配对预测中离线构造可独立执行的 correction target，不需要 reward、simulation 或额外 corrective demonstrations。
- 三条技术贡献统一为：null-mode residual distillation、staleness-aware residual distillation，以及 independently executable residual pathway；实验用于分别验证三者，不再单独作为贡献条目。
- Related Work 已从 full action/residual、额外数据或在线交互、是否依赖 generative action inference 三个轴区分 FAVLA、LIFT 和 DAM-VLA，并删除“现有方法均重新生成完整动作”这类绝对表述。
- 主对照已增加 **Temporal Teacher**：直接 rollout Stage 1 teacher，不训练 null mode，不进行 residual distillation。
- ImplicitRDP 已明确定位为 publicly reproducible fast--slow visual--force baseline，而非 asynchronous VLA baseline。
- Method 已明确 Eq. (9) 中 reference、current null prediction 与 $\Gamma$ 的状态基准和坐标关系。
- Method 已将已知的 $\Gamma(S_t,S_k)$ 作为 student 条件输入，避免要求 staleness head 从缺少 $S_k$ 的上下文中推断坐标重基准项。
- 已按训练与部署代码确认：student 只读取其 query 时刻对齐的当前 reference sample；其余 $K-1$ 个 reference samples 仅用于逐步构造 target、command composition 和 reconstruction analysis，不作为 student 输入。
- 已按训练代码确认 $\Gamma(S_t,S_k)$：它由归一化 state 的前九个位姿坐标之差，经训练集 state/action 标准差比例缩放后得到；rotation block 是连续 6D 坐标的分量差，而非 $SO(3)$ 上的精确相对旋转。
- Method 已明确 rotation residual 的实际实现：在 6D action representation 中相加，再通过 Gram--Schmidt 投影为合法旋转；这不是 $SO(3)$ 上的旋转合成。
- Method 使用独立的 Method Overview 小节概括三阶段训练流程，使章节层级与 Fig. 2 及后续三个方法模块一致。
- 三阶段仅在 Method Overview 和 Fig. 2 中表示训练顺序；正文按方法功能组织为 teacher/null reference、residual distillation 和 independent execution，避免把 Stage 1--3 混入不同标题层级。
- Method 已区分 reference query $k$、residual packet $q$、learned residual query $q_{\mathrm{res}}$、torque $\boldsymbol\tau$ 与 flow time $\eta$，避免一符多义。
- Independent Residual Execution 已按真实实现写明：每次 Fast inference 发布带时间戳的 $K$ 步 correction chunk；过期前缀被跳过，剩余步骤按区间顺序执行且不插值，整包过期后拒绝并进入安全策略。
- 主文已明确每个 residual packet 与其查询时读取的 reference packet 绑定；新 reference 仅供随后发起的 residual query 使用，不能与旧 correction chunk 直接混合。
- Problem Formulation 仅给出 reference、force correction 与 staleness correction 的抽象组合；$K$ 步 target 索引和异步 packet 选择分别在后续 Method 与 supplementary 中定义，避免在问题定义中提前混入部署细节。
- 主文 Method 已压缩为问题定义、监督目标、残差策略与部署机制；空力模式完整优化式、时序权重归一化、reference/residual 数据包时间戳、有效步骤选择及失效回退规则已移至 supplementary，并同步中文翻译。
- 正文必须独立给出使方法成立的全部关键定义；supplementary 仅承载更细的优化超参数、完整 packet 伪代码、额外统计和敏感性结果，不能用于补足正文中的核心方法逻辑。
- Experiments 已将 runtime/update-rate 与 reference-latency robustness 合并为 Independent Residual Execution；contact-localized correction 通过 Main Results 中的代表性 rollout 可视化分析，residual-bound sensitivity 移至补充材料。
- 成功率协议已固定为每任务两个 seed、每 seed 10 次；补充材料已为 seed-wise counts 预留报告位置。
- contact quality 使用全部试验的 peak force；completion time 仅在成功试验上统计。
- 通过浮动边界控制保证实验图表位于 Conclusion 与 References 之前。

## 三、正式投稿前必须完成的实验与记录

### A. 固化设备、训练和评价协议

- 确认 X Square Robot 双臂平台的具体产品型号、腕部/外部相机型号及每臂 wrench 接口；两个平台均不得写成额外安装末端 F/T 传感器；
- 确认九项任务的可重复 success criterion；
- 用最终训练日志确认 optimizer、steps、batch size、learning rate、flow steps 和模型结构参数；
- 明确两个 seed 的含义、按任务训练还是联合训练、测试初始状态采样及 success 判定方式；
- 明确 F/T bias removal、重力补偿、滤波、双臂力指标聚合，以及 completion time 的起止时刻。

### B. 完成主结果与精确 teacher 对照

对 $\pi_{0.5}$、ForceVLA、TA-VLA、ImplicitRDP、Temporal Teacher 和 ForceDelta-VLA 报告：

- 九项任务的 success rate 及单臂/双臂平均值；
- 每个 seed 的独立成功次数；
- 全部试验的 peak force；
- 成功试验的 completion time。

Temporal Teacher 必须直接使用 Stage 1 的 temporal force-conditioned teacher rollout，以隔离 temporal force encoder 自身收益与 residual decomposition 的收益。

### C. 补齐 runtime 与 responsiveness 实测

在同一部署设备、batch size 1 和真实闭环中报告：

- Temporal Teacher 的完整推理延迟与实际更新率；
- ForceDelta reference worker 的推理延迟与实际发布率；
- residual policy 的前向延迟与实际更新率。

不能用前向时间的倒数代替真实 policy-packet publication rate；chunk 内 command rate $f_A$ 应单独报告，不能与新观测驱动的策略更新率混为一谈。

### D. 完成延迟与 residual bound 敏感性

- 在 USB insertion 和 plug insertion 上，在推理完成与 packet publication 之间加入 0/100/200 ms 延迟；
- 仅比较架构匹配的 Temporal Teacher 与 ForceDelta-VLA，报告 success rate 和 peak force；不为没有同等 reference/residual 分解的 ImplicitRDP 人为定义 reference latency；
- 在 USB insertion 和 drawer opening 上，将最终确定的 $(b_F,b_S)$ 联合缩放为 $0.5\times/1\times/2\times$，结果放入 supplementary；
- 记录真机实际使用的 $b_F$ 与 $b_S$：明确其为九维 bound 还是广播标量，并说明在归一化坐标中、组合前、逐坐标限幅，双臂时对每只手臂独立应用。README 中的 0.02/0.10 仅为示例，不能作为论文配置。

### E. 完成消融、定性图和失败分析

- 固定同一个完整模型，比较四种执行组合：reference only、without force residual、without staleness residual 和 full composition；同时报告成功率与 peak force；
- paired teacher target vs. demonstration-derived target；
- native-rate causal force history vs. instantaneous force；
- learned null mode vs. zero force vs. null token without adapter；
- with/without schedule replay；
- 用真实执行帧、force trace、null-mode reference、residual 和 composed command 构成定性图；
- 使用真实失败轨迹区分 teacher reference error、超出 residual bound 及需要离散策略切换的失败。

### G. 替换所有红色占位内容

当前红色 `X` 和红色文本是明确的待填项，不是可投稿结果。完成上述实验后，必须：

1. 用原始记录替换全部占位值；
2. 根据实测结果重写 Abstract、Introduction、Results 和 Conclusion 的结论句；
3. 删除 `\assumed{}` 宏和 provisional/final-text 提示；
4. 确保表格、图、正文和中文翻译使用同一组最终数据。

### H. 固化仍会影响公式的执行协议

以下内容必须根据真实代码确认后写入 Method 或 supplementary，不能凭文字假设：

- teacher/null-reference chunk 的采样频率与 Fast correction chunk 的采样频率是否相同；若不同，应分别使用 $f_{\mathrm{ref}}$ 和 $f_F$，并说明 full/null teacher targets 如何重采样到 Fast 时间网格；
- 离线数据中的 state/action 为 30 Hz 时，Fast student 在更密集时间戳上的 $S_t$、reference 和 supervision 如何获得，例如采用时间插值还是仅在 30 Hz 数据点训练；
- residual inference packet 的实际发起频率、完成/发布频率和 chunk 内 command rate；三者不得用同一个“update rate”代替；
- reference chunk 到达最后一个时间戳后、且新 reference 尚未完成时的明确行为，包括保持末项、拒绝数据包或进入安全策略；
- gripper command 的时间对齐规则，例如 nearest sample 或 zero-order hold；不能将离散 gripper command 与 pose 一起连续插值；
- $\operatorname{Pool}_M$ 的具体 pooling 操作和 $\operatorname{Mask}_F$ 的具体屏蔽方式；
- force/staleness correction bound 的最终数值必须写入主文 Implementation Details；更完整的单位、逐通道限幅位置、bound sensitivity 和 safe hold/abort 触发逻辑可在 supplementary 展开。

## 四、建议完成顺序

1. 固化硬件、训练、任务判定和评价协议；
2. 测量 batch-one inference latency 和实际 update rate；
3. 完成主实验，尤其是 Temporal Teacher 的精确 teacher 对照；
4. 完成 reference-delay 和 residual-bound sensitivity；
5. 完成消融、定性图和失败分析；
6. 替换全部红色占位，并依真实结果重写结果性文本。
