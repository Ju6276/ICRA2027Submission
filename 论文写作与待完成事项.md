# ForceDelta-VLA：投稿前待完成事项

本文档区分已经固定的决定、尚未完成的工作和暂不实施的增强项。

## 一、已经完成或固定

- 评估协议固定为每种方法、每项任务 20 次真实机器人评估，不声称存在独立训练 seed；不额外要求置信区间。
- reference-delay 范围固定为 0/100/200 ms，不再扩展更长延迟。
- 接触质量主指标保持 peak force；completion time 只在成功 trial 上统计。
- 术语已统一：正常查询为 `force-conditioned`；学习式缺失力输入模式、预测和 reference 均为 `missing-force`；监督构造中使用全零 wrench 的对照为 `zero-wrench teacher query`；部署时将 residual 输入替换为全零张量的诊断为 `zeroed residual input`。$\pi_{0.5}$ 单独表示完全没有力输入通路的独立 VLA baseline。
- Cached-context target 已按真实实现重写：reference、force-conditioned 与 missing-force teacher 查询共享缓存视觉—语言前缀 $E_k$ 和 flow noise $\epsilon_k$，并在时刻 $t$ 更新状态与力历史；目标不再依赖 student 不可见的当前图像 $V_t$。
- Related Work 已说明 FAVLA 与本文在 full-action generation 和 explicit additive correction supervision 上的区别。
- FAVLA 在评估时没有官方实现，因此不列入定量 baseline；Baseline 段落已解释每个入选方法的作用和 FAVLA 未复现的原因。
- 当前 fast branch 继续使用 cached intent token，并假设任务层视觉上下文在一个 reference interval 内局部有效。
- 当前旋转实现保持 bounded 6D additive residual 与 Gram--Schmidt projection。
- 代表性定性图中的 10 mm 上限描述的是 translational residual magnitude，不能换算成旋转幅度；当前不新增旋转实验，也不把旋转近似作为审稿缺陷。
- 暂不增加语言、未见任务或新物体泛化实验。
- 不增加 residual 离线 target-error 分析，优先使用现有真机实验验证。
- 内部 supplementary 记录不作为论文审稿问题来源；其中的临时符号、占位值和未完成配置不进入对正文的批判性判断。
- Force-dependence diagnostic 已完成：Shuffled force history 保留边缘分布但破坏观测—力—动作对应关系；Zeroed residual input 将实测力历史替换为全零张量，但保留力编码器和 residual branch，且可能产生分布外输入。两项结果已加入 execution/component ablations。
- 核心归因消融已完成：Single combined residual head 与 Fast full-action distillation 的成功率和峰值力均已写入 supervision/decomposition ablations。
- 消融定义已在表前按两组逐项说明，并在正文中加粗每个 variant 名称；表格本身仅加粗完整方法与最佳数值。
- 已完成全文一致性与防御性表述审查：删除重复的 20-trial 辩解、success-conditioned metric 重复限定、Related Work 中重复的数据优势声明和 Conclusion 中重复的失败计数；消融结论统一使用与证据强度匹配的 `shows/supports`。
- Single-arm task rollouts 附录不随投稿正文提交；源码保存在 `appendix_single_arm_rollouts.tex`，且不由 `main.tex` 引入。

## 二、必须确认的实验与实现协议

### 1. 硬件、训练与评价

- 确认 X Square Robot 的具体型号、相机型号和每臂 wrench 接口。
- 明确每项任务是独立训练还是联合训练，以及测试使用的 checkpoint。
- 明确测试初始状态、目标位姿、物体位置和其他随机化范围。
- 固化九项任务的 success criterion，以及完成时间的起止时刻。
- 从最终日志确认 optimizer、batch size、training steps、learning rate、flow steps、$H$、$K$、$T_F$、$\beta$ 和 $\lambda_{\mathrm{stale}}$。
- 明确 wrench bias removal、重力补偿、滤波，以及双臂 peak-force 聚合方式。
- 确认所有 baseline 使用相同示范数据、视觉输入、控制器增益、安全限幅和可比的部署资源；无法统一的部分在正文说明。
- 每种方法、每项任务进行 20 次真实机器人评估；不声称存在独立训练 seed。

### 2. 异步执行协议

- 明确 reference query rate、reference packet publication rate、residual query rate、residual packet publication rate、packet 内 action-sample rate $f_A$ 和 command application rate。
- 确认 teacher/missing-force-reference chunk 与 residual chunk 是否使用相同 action-sample rate；若不同，分别使用 $f_{\mathrm{ref}}$ 和 $f_F$，并说明 target 重采样方式。
- 说明 30 Hz state/action 数据如何监督更高查询频率的 residual：只在 30 Hz timestamp 训练，还是进行因果插值。
- 明确 reference chunk 到达末尾而新 reference 尚未完成时的行为。
- 明确离散 gripper command 的时间对齐方式，不能与 pose 一起连续插值。
- 明确 residual packet 过期、无有效 packet 和 worker 超时时的 hold/abort 行为。

### 3. 模型实现

- 写明 $\operatorname{Pool}_M$ 的具体 pooling 操作和 $\operatorname{Mask}_F$ 的具体实现。
- 保持当前作用范围：缓存任务层视觉上下文在一个 reference interval 内局部有效；staleness residual 只补偿 robot-state/reference-time mismatch，不负责任意场景变化。
- 核对实现细节与已写公式完全一致：reference、force-conditioned 和 missing-force 三次查询必须复用同一缓存前缀 $E_k$ 与同一 flow noise $\epsilon_k$；若代码并非如此，应以真实实现为准同步修改公式。

## 三、仍需完成或决定的实验

### 1. 主结果与运行时

- 用真实记录核对九项任务的 20-trial success counts、全部 trial 的 peak force 和成功 trial 的 completion time。
- 确保 Temporal Teacher 是 Stage-1 teacher 的直接 rollout，用于隔离 temporal force encoder 与 residual execution 的收益。
- 在相同部署硬件和 batch size 1 下核对 teacher、missing-force reference、residual 和 ImplicitRDP 的 forward latency 与真实 packet publication rate。

### 2. 物理泛化（高优先级）

- **Physical generalization：**选择 USB insertion 与 wiping 等代表性任务，测试训练范围外的插入偏角、表面摩擦或接触阻力。
- 至少报告 success rate 和 peak force；不将其表述为语言或任务语义泛化。

### 3. 敏感性与定性分析

- 完成并核对既定的 0/100/200 ms reference-delay 数据，不扩展更长延迟。
- 定性图使用真实执行帧、force trace、missing-force reference、force/staleness residual 和 composed command。
- 使用真实失败轨迹核对 reference failure、excessive staleness 和 nonlocal recovery 三类统计。

## 四、目前未实施的可选增强

以下是审稿增强项，不是当前方法成立的必要条件：

- **30 Hz 当前视觉进入 fast branch：**当前架构仍只使用 cached intent token。若加入，应采用异步视觉缓存，不让 100 Hz residual worker 等待图像编码，并重新训练 schedule replay。
- **Force-history representation：**若实验预算允许，比较 native-rate temporal force history 与 instantaneous force input。
- **Missing-force implementation：**若需要进一步隔离 missing-force token 与 mode-specific adapter 的作用，增加 missing-force token without adapter 对照。
- **严格 $SO(3)$ rotation composition：**当前保留 bounded 6D additive residual 与 Gram--Schmidt projection；只有 residual rotation 较大或出现姿态误差时再切换到 Lie algebra/quaternion composition。
- **更多接触指标：**当前决定保持 peak force；只有审稿反馈明确要求时，再考虑 contact-phase RMS、95th percentile、force impulse 或安全阈值超限时间。

## 五、最终写作与同步

- 所有 residual target 统一称为 `teacher-defined force-conditioned deviation target` 或 `teacher-defined force-responsive correction target`；student 输出称为 `force-responsive residual/correction`。
- 统一使用 `force-conditioned prediction/query` 与 `missing-force mode/prediction/reference`；`missing-force` 明确定义为 learned missing-force-input mode，并与 `zero-wrench teacher query`、`zeroed residual input`、`w/o force residual` 和 $\pi_{0.5}$ 区分。
- 不使用 ground-truth、causal、counterfactual 或 force-marginalized 等未由方法定义支持的术语。
- 用最终实验记录替换内部文件中的全部 `\assumed{}`、红色文字和 `X`。
- 根据最终数据同步更新 Abstract、Introduction、Results、Conclusion、三张方法图和中文翻译稿。
- 最终重新编译并检查页数、表格宽度、交叉引用、参考文献和匿名信息。

## 六、建议完成顺序

1. 固化真实实现、硬件和评价协议。
2. 核对主结果、runtime 和 Temporal Teacher。
3. 完成 physical-generalization 实验。
4. 完成既定 delay sensitivity、定性图和失败统计。
5. 用最终记录统一全文并进行投稿前检查。
