# ForceDelta-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力感知视觉—语言—动作（VLA）策略改善了接触密集型操作，但通常仍在同一个生成式动作预测中耦合标称任务运动与接触相关调整。离线示范没有提供将该预测分解为可复用 reference 和显式 correction 的标签。我们提出 ForceDelta-VLA，一种从成对完整力条件与学习式空力预测中构造 teacher-defined correction target 的残差蒸馏框架，无需任务奖励，也无需额外纠正或在线示范。互补目标补偿延迟推理中逐渐陈旧的 reference。所得可独立执行残差通路复用缓存任务上下文和带时间戳的空力 reference，同时利用近期力历史更新位姿修正，无需重新生成完整动作。在八项接触密集型单臂和双臂任务上，相较于最强基线，ForceDelta-VLA 将平均成功率提高 <span style="color:red">X 个百分点</span>，将峰值接触力降低 <span style="color:red">X%</span>，并将完成时间缩短 <span style="color:red">X%</span>。


## 1. 引言

力感知视觉—语言—动作（VLA）策略通过以交互反馈为条件生成动作，提高了接触密集型操作性能。然而，这类策略通常在同一个生成式动作预测中同时表示标称任务运动与接触相关调整。部署时，两者具有不同语义：相邻控制周期可以复用标称运动，而物理修正应随新观测到的接触状态及时变化。因此，问题不仅在于力信号比语义上下文变化更快，也在于这类策略通常没有将修正显式表示为可单独监督和执行的对象。

一种自然分解是将任务层参考与力响应加性残差组合。该形式在保留 teacher 标称任务运动的同时，将修正显式表示为可直接执行的位姿空间更新。然而，从离线示范中学习该分解仍然是欠定的：示范只包含执行动作，并没有标注哪些行为属于参考或残差。直接使用动作监督允许参考保留接触相关行为，或让残差重复标称运动。

两个直观替代方案也不能消除这一歧义：从示范动作中减去缓存参考会混合物理修正与参考陈旧性；零力查询则表示一种有效物理状态，并不等价于缺失力条件。因此，核心问题是如何利用预训练力感知策略及其已有离线数据构造显式修正目标。

现有快—慢与异步系统解决物理信息何时更新的问题，但本身并不能确定一个可独立执行的修正分支应当预测什么。残差强化学习和纠正式模仿学习通过任务奖励或额外在线纠正获得该目标；本文则不依赖任务奖励、额外纠正示范或在线示范。即使获得了这一目标，部署仍需在复用语义上下文和处理陈旧 reference 的同时，使修正独立于生成式 reference policy 更新。

我们提出**空力模式残差蒸馏**。首先，学习式空力运行点与匹配噪声的完整力条件/空力 teacher 查询确定由 teacher 定义的力条件修正目标。其次，互补的陈旧性目标补偿 reference 与当前预测之间由 cache age 引起的偏差。最后，可独立执行残差通路复用缓存任务上下文和带时间戳的空力 reference，同时根据近期力历史更新位姿修正，与 teacher 的生成式动作推理解耦。

我们在四项单臂和四项双臂真实机器人任务上评估 ForceDelta-VLA，任务涵盖接触建立、精密插入、摩擦主导运动和受约束操作。相较于最强基线，本方法将平均成功率提高 <span style="color:red">X 个百分点</span>，同时将峰值接触力降低 <span style="color:red">X%</span>，将完成时间缩短 <span style="color:red">X%</span>。<span style="color:red">所有红色占位符均须由最终实验测量替换。</span>

本文贡献如下：

1. 将力响应策略分解表述为教师蒸馏问题，并提出**空力模式残差蒸馏**。学习式空力运行模式与匹配噪声的教师查询提供显式修正目标，无需任务奖励，也无需额外的纠正或在线示范；
2. 面向异步参考执行，提出**陈旧性感知残差蒸馏**，通过重放部署时序学习陈旧参考补偿，使缓存参考逐渐过时时，蒸馏得到的修正仍然有效；
3. 构建**可独立执行残差通路**，复用缓存任务上下文和带时间戳的空力 reference，并与生成式 VLA 异步地更新力响应位姿修正。


## 2. 相关工作

### 2.1 力感知操作策略

力感知 VLA 通过多模态融合、提示、时序记忆或力感知优化整合力/力矩信息；力中心模仿学习则从示范中联合建模运动与交互力。这些方法改善了接触敏感预测，但通常仍在统一策略输出中共同表示任务运动和物理适应。

视觉运动方法学习异步 RGB 与力/力矩流，按交互阶段调整扩散推理频率，或引入快速修正与混合力—位置通路。VLA 方法也会复用缓存语义特征、通过纠正 rollout 训练反应式专家，或按传感器频率刷新模态专用潜在状态。这些设计改变物理反馈影响控制的调度方式或架构通路。ForceDelta-VLA 则从离线力感知 teacher 的匹配完整力条件与空力预测中导出显式加性修正监督，并使蒸馏后的修正独立于完整动作生成执行。

### 2.2 残差策略学习

残差策略学习用学习得到的修正增强标称控制器，并已用于接触密集型操作。现有方法通过强化学习或在线人工纠正获得残差监督，其中也包括针对冻结视觉策略的触觉条件残差适配。其他模仿学习方法则直接规定加性修正结构。在这些路线中，残差由奖励、纠正式交互或预设分解定义。ForceDelta-VLA 则从预训练力感知 teacher 的匹配完整力条件与学习式空力预测中离线构造显式残差目标。

### 2.3 策略蒸馏

学习式缺失模态表征能够区分不可用条件与具有物理意义的零值，从而支持使用独立的空力运行点，而不是简单地将力输入置零。近期机器人策略蒸馏方法将迭代扩散或流策略压缩为单步动作生成器，以实现更快的闭环控制；guidance distillation 则利用成对预测，将依赖模型的条件更新迁移到单个预测器。不同于完整策略蒸馏，本文保留生成式 VLA 作为参考，仅蒸馏其在动作空间中的修正偏差。

## 3. 方法

### 3.1 问题定义

我们考虑由遥操作轨迹组成的离线数据集 $\mathcal D$。在时刻 $t$，每条轨迹提供视觉观测 $V_t$、语言指令 $L$、机器人状态 $S_t$、示范动作 $A_t^*$，以及因果 wrench 历史

$$
\mathcal F_t=\{(s,\mathbf w(s))\mid t-T_F\leq s\leq t\},
$$

其中，$T_F$ 是历史窗口长度，$\mathbf w(s)=[\mathbf f(s);\boldsymbol\tau(s)]\in\mathbb R^6$ 表示作用于末端的外部 wrench。位姿动作和 wrench 测量均表示在相应的机器人基坐标系中；双臂样本将两臂对应的量进行拼接。力条件 VLA 教师 $T_\theta$ 将 $(V_t,L,S_t,\mathcal F_t)$ 映射为长度为 $H$ 的动作块，其中每一步动作均相对于发起该查询时的状态 $S_t$ 表示。

本文寻求一个由 teacher 定义的显式力响应修正目标，以及能够使该修正独立于 reference generator 更新的部署分解。该分解包括缓存 reference 和根据近期力反馈更新位姿修正的残差策略。令 $\mathcal P(\cdot)$ 提取动作中的位姿分量。对于从状态 $S_k$ 在 $t_k$ 发起的当前有效 reference 查询 $k$，令 $A_k^{\mathrm{ref}}(t)$ 表示其空力动作块在时刻 $t$ 对齐后的样本。控制时刻 $t>t_k$ 的归一化 delta-action 指令仍以 $S_k$ 为基准，并写为

$$
\mathcal P(A^{\mathrm{cmd}}(t))=\mathcal P(A_k^{\mathrm{ref}}(t))
+\Delta\hat A^{\mathrm{force}}(t)
+\Delta\hat A^{\mathrm{stale}}(t),
$$

残差仅修改位姿；夹爪指令沿用 reference，即 $g^{\mathrm{cmd}}(t)=g_k^{\mathrm{ref}}(t)$。力条件项建模 teacher 的力条件偏差，陈旧性项补偿缓存 reference 的不匹配。由于示范没有标注这两个分量，二者都需要构造监督。

上述动作组合在 teacher 的表示空间中进行。位姿分量 $\mathcal P(A)=[p;r]$ 包含三维平移 $p$ 和连续六维旋转表示 $r$。平移直接相加；旋转修正在 teacher 的六维动作坐标中相加，再通过 Gram--Schmidt 正交化映射为合法旋转矩阵。

训练分为图 2 所示的三个阶段：（1）训练时序力条件 VLA teacher；（2）冻结 teacher 并学习提供参考动作的空力运行模式；（3）冻结两种运行模式，构造力条件与陈旧性目标，并将二者蒸馏到残差策略中。

### 3.2 力条件教师与空力参考

#### 时序力条件教师

教师基于 ForceVLA，将其瞬时力嵌入替换为因果时序编码：

$$
z_t^F=E_T^F(\mathcal F_t).
$$

其余动作生成结构保持不变。教师在 $\mathcal D$ 上训练并预测

$$
A_{T,t,1:H}^{\mathrm{full}}=T_\theta(V_t,L,S_t,z_t^F;\epsilon).
$$

其中 $\epsilon$ 为初始流噪声。训练完成后冻结 $\theta$。

#### 空力参考模式

零力是有效物理测量，不能表示力历史缺失。我们引入学习式空力 token $z_{\varnothing F}$ 和 rank-$\rho$ 适配器 $\phi=\{z_{\varnothing F},W_{\mathrm{in}},W_{\mathrm{out}}\}$。适配器仅修改动作专家的位姿通道：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h),
$$

其中，$h$ 是 action expert 的隐藏状态，$v^p$ 是其位姿通道的 flow 输出，$\sigma$ 是逐点非线性函数。该 adapter 只在空力模式下启用。保持 $\theta$ 固定，使用原始流匹配目标优化 $\phi$：

$$
\mathcal L_{\mathrm{null}}
=\mathbb E_{\mathcal D,\eta,\epsilon}
\left[\ell_{\mathrm{FM}}\!\left(
T_{\theta,\phi}^{\mathrm{null}},A_{t:t+H-1}^{*};\eta,\epsilon\right)\right].
$$

其中，$\eta$ 表示 flow time。

由于仅在原始模仿学习目标下优化空力模式参数 $\phi$，而力条件教师始终保持冻结，该模式定义的是教师的无显式力输入运行点，而不是另一个独立优化的任务策略。

所得参考预测为

$$
A_{T,t,1:H}^{\mathrm{null}}
=T_{\theta,\phi}^{\mathrm{null}}(V_t,L,S_t,z_{\varnothing F};\epsilon).
$$

### 3.3 空力模式残差蒸馏

#### 成对修正目标

对于相同的 $(V_t,L,S_t)$，完整力条件查询与空力查询复用视觉—语言前缀和初始噪声 $\epsilon$。令 $t_j=t+(j-1)/f_A$ 表示第 $j$ 个动作步的预定执行时间，其中 $f_A$ 是动作频率，$j=1,\ldots,K\leq H$。由 teacher 定义的力目标为

$$
\Delta A_{T,t,j}^{\mathrm{force}}
=\mathcal P\!\left(A_{T,t,j}^{\mathrm{full}}\right)
-\mathcal P\!\left(A_{T,t,j}^{\mathrm{null}}\right).
$$

该目标用于监督力残差 head。本文仅将这一由模型定义的条件差分用作蒸馏目标，而不将其解释为力的因果归因。

部署时，当前 reference 由查询 $k$ 在状态 $S_k$ 下生成，而当前空力预测相对于 $S_t$。坐标重基准项 $\Gamma(S_t,S_k)$ 根据两个状态中每条机械臂的末端位姿计算，并表示在与 $\mathcal P(A)$ 相同的机器人基坐标系、6D 位姿表示和归一化动作坐标中。从 $S_k$ 到 $S_t$ 重基准缓存 reference 时需要减去 $\Gamma(S_t,S_k)$，因此陈旧性目标为

$$
\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}
=\mathcal P\!\left(A_{T,t,j}^{\mathrm{null}}\right)
-\mathcal P\!\left(A_k^{\mathrm{ref}}(t_j)\right)
+\Gamma(S_t,S_k).
$$

令 $\mathcal R(S,a)$ 表示将以状态 $S$ 为基准的相对动作 $a$ 转换为绝对机器人命令。两项目标在每个预测步上对齐两个坐标基准：

$$
\begin{aligned}
&\mathcal R\!\left(S_k,\mathcal P(A_k^{\mathrm{ref}}(t_j))
+\Delta A_{T,t,j}^{\mathrm{force}}
+\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}\right)\\
&\qquad\approx\mathcal R\!\left(S_t,
\mathcal P(A_{T,t,j}^{\mathrm{full}})\right).
\end{aligned}
$$

该关系对平移分量严格成立；旋转对齐则在将加性修正后的 6D 表示投影到 $SO(3)$ 后进行评估。

#### 快速残差策略

每次完成 reference 查询时，系统同时发布由缓存视觉—语言前缀 $E_k$ 及其 mask $m_k$ 池化得到的 $M$ 个 intent token，即 $Z_k^{\mathrm{intent}}=\operatorname{Pool}_M(E_k,m_k)$。学生专用的因果编码器将 $\mathcal F_t$ 映射为 $u_t^F$。缓存年龄和 reference 插值位置表示为

$$
\xi_{t,k}=\left[\min\!\left(\frac{t-t_k}{c},1\right);\alpha_{t,k}\right].
$$

残差策略采用有序条件序列

$$
\mathcal C_t=[P_Z(Z_k^{\mathrm{intent}});u_t^F;P_S(S_t);
P_A(A_k^{\mathrm{ref}}(t));P_\xi(\xi_{t,k})].
$$

其中，$P_Z,P_S,P_A$ 和 $P_\xi$ 是映射到共享 token 维度的学习式投影。令 $\mathcal C_t^{\mathrm{maskF}}=\operatorname{Mask}_F(\mathcal C_t)$ 表示保持序列结构不变、但屏蔽力 token 后的条件序列。

共享注意力模块 $\Phi$ 和学习式查询 $q_{\mathrm{res}}$ 预测两个长度为 $K$ 的修正块：

$$
\Delta\hat A^{\mathrm{force}}_{t,1:K}
=W_{\mathrm{force}}\Phi([\mathcal C_t;q_{\mathrm{res}}])[q_{\mathrm{res}}],
$$

$$
\Delta\hat A^{\mathrm{stale}}_{t,1:K}
=W_{\mathrm{stale}}
\Phi([\mathcal C_t^{\mathrm{maskF}};q_{\mathrm{res}}])[q_{\mathrm{res}}].
$$

该 mask 避免陈旧性输出直接依赖实测力。两次计算共享 $\Phi$，并使用独立的零初始化输出投影。

#### 陈旧性感知时序重放

在离线残差策略训练中，我们通过采样 query 间隔和 query-completion latency 重放异步执行。令 $t_i$ 和 $\bar t_i$ 分别表示查询 $i$ 的发起和完成时间，并定义 $k\equiv k(t)=\arg\max_{i:\bar t_i\leq t}t_i$。所选查询决定陈旧性目标使用的缓存 intent、reference、时序特征和 $\Gamma(S_t,S_k)$。重新采样调度使策略覆盖不同的 reference age、插值相位和 completion latency。

采用归一化时序权重

$$
w_j=\frac{\beta^{j-1}}{\sum_{m=1}^{K}\beta^{m-1}},
\qquad 0<\beta\leq1,
$$

其中，$\beta<1$ 更强调近期修正，$\beta=1$ 则对所有预测步赋予相同权重。采用归一化坐标均方损失 $\ell_{\mathrm{pose}}$，蒸馏目标为

$$
\mathcal L_{\mathrm{distill}}=\sum_{j=1}^{K}w_j\left[
\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{force}}_{t,j},\Delta A^{\mathrm{force}}_{T,t,j})
+\lambda_{\mathrm{stale}}\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{stale}}_{t,j},\Delta A^{\mathrm{stale}}_{T,k\rightarrow t,j})
\right].
$$

$\lambda_{\mathrm{stale}}$ 用于平衡力条件监督与陈旧性监督。

### 3.4 独立残差执行

Reference worker 和 residual worker 相互独立运行。查询在 $t_k$ 发起，并在 $\bar t_k$ 完成时发布不可变数据包 $(A_{T,k,1:H}^{\mathrm{null}},Z_k^{\mathrm{intent}},S_k,t_k,\bar t_k)$。Reference 动作样本携带时间戳：

$$
t_{k,n}^{\mathrm{ref}}=t_k+(n-1)/f_A,
\qquad n=1,\ldots,H.
$$

满足 $t_{k,n}^{\mathrm{ref}}<\bar t_k$ 的 reference 样本会被跳过，不再直接执行。对于 $t\geq\bar t_k$，$A_k^{\mathrm{ref}}(t)$ 通过原始动作块上的时间戳插值得到。

第 $q$ 个 residual 推理数据包在时刻 $t_q^F$ 发起，读取最近发布的 reference 数据包 $k_q$ 以及当前 $(\mathcal F_{t_q^F},S_{t_q^F})$，并在 $\bar t_q^F$ 完成时输出两个 $K$ 步修正块。其样本时间戳为

$$
t^F_{q,j}=t_q^F+(j-1)/f_A,\qquad j=1,\ldots,K.
$$

第 $j$ 步在区间 $[t^F_{q,j},t^F_{q,j}+1/f_A)$ 内保持不变；residual 样本不进行插值。在控制时刻 $t$，执行端选择发起时间最新、已经完成且尚未到期的数据包及其当前有效步骤：

$$
\begin{aligned}
q^*(t)&=\arg\max_{q:\,\bar t_q^F\leq t<t_q^F+K/f_A}t_q^F,\\
j(t;q^*)&=1+\left\lfloor(t-t_{q^*}^F)f_A\right\rfloor.
\end{aligned}
$$

因此，推理过程中已经过期的前缀会被跳过，其余步骤依次执行，直至更新的 residual 数据包完成。在当前所选步骤的有效区间内，式 (1) 中的各项定义为

$$
\begin{aligned}
k&=k_{q^*},\\
A_k^{\mathrm{ref}}(t)
&\equiv A_{k_{q^*}}^{\mathrm{ref}}(t^F_{q^*,j(t;q^*)}),\\
\Delta\hat A^{(\cdot)}(t)
&=\Delta\hat A^{(\cdot)}_{q^*,j(t;q^*)}.
\end{aligned}
$$

两项修正分别限幅，并在归一化 delta-action 坐标中完成组合。所得指令仍以 $S_{k_{q^*}}$ 为基准，并通过 $\mathcal R$ 转换为绝对机器人命令。如果 residual 数据包在 $t_q^F+K/f_A$ 之后才完成，则拒绝该数据包；若当前不再存在有效数据包，系统进入平台的安全保持或中止策略。

## 4. 实验

我们在单臂和双臂接触密集型操作上评估 ForceDelta-VLA。实验考察三个方面：（1）相对于外部基线和架构匹配基线的任务性能与接触质量；（2）推理效率及其对延迟 reference 更新的鲁棒性；（3）通过诊断和消融分析所学习的残差分解。

### 4.1 实验设置

#### 硬件、任务与数据采集

我们在两个机器人平台上评估八项真实机器人任务，包括四项单臂任务和四项双臂任务（见任务表）。单臂平台采用一台七自由度 Franka Emika Panda，配备一个腕部 Intel RealSense D405 相机和一个外部视角 D405 相机，示范通过 3Dconnexion SpaceMouse 遥操作采集。双臂实验采用 X Square Robot 平台，每条机械臂配备一个腕部相机，并设置一个共享外部相机，示范通过 Leader--Follower 遥操作采集。与 ForceVLA [1] 一致，我们使用机器人自身提供的 wrench 估计，无需额外安装腕部力/力矩传感器。

每项任务均采集 200 条示范。多视角图像、机器人状态和命令动作以 30 Hz 记录，带时间戳的 wrench 估计以 100 Hz 记录。

#### 基线与评估协议

本文比较四个外部基线：$\pi_{0.5}$、ForceVLA、TA-VLA 和 ImplicitRDP。此外，我们将第一阶段 teacher 直接 rollout 为 Temporal ForceVLA，作为隔离残差蒸馏贡献的架构匹配对照。每种方法在每项任务上评估两个独立训练 seed，每个 seed 测试 10 次，并报告成功率、峰值接触力和完成时间。

#### 实现细节

时序力条件 teacher 在八张 NVIDIA A100 GPU 上进行全参数微调。所有真实机器人推理和 runtime 测量均使用一张 48 GB NVIDIA RTX 4090D GPU。

### 4.2 主要结果

#### 任务性能

主表同时回答外部比较和内部受控比较：ForceDelta-VLA 与最强可复现快—慢视觉—力 baseline 比较，并与其蒸馏来源 Temporal ForceVLA 比较。

| 方法 | 按按钮 | 翻物体 | 插 USB | 开柜门 | 插插头 | 拔插头 | 擦白板 | 开抽屉 | 单臂均值 | 双臂均值 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| $\pi_{0.5}$ | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| ForceVLA | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| TA-VLA | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| ImplicitRDP | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| Temporal ForceVLA | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| **ForceDelta-VLA** | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> |

#### 接触质量

峰值力定义为全部试验上的 $\max_t\|\mathbf f(t)\|_2$，完成时间仅统计成功试验；两项指标均为越低越好。

| 方法 | 单臂峰值力 ↓ | 双臂峰值力 ↓ | 单臂完成时间 ↓ | 双臂完成时间 ↓ |
|---|---:|---:|---:|---:|
| $\pi_{0.5}$ | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| ForceVLA | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| TA-VLA | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| ImplicitRDP | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| Temporal ForceVLA | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| **ForceDelta-VLA** | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> | <span style="color:red">**X**</span> |

#### 定性与失败分析

定性分析使用同步执行帧、力曲线、reference 和 residual 解释插入、擦拭及受约束开门等具体案例。失败统计分为错误 reference、超出 residual bound 和需要离散恢复策略三类；第一类继承自 teacher，后两类体现加性连续修正的表示边界。“自由空间残差接近零”等跨任务结论仅在后续 residual diagnostics 中使用定量数据报告。

### 4.3 独立残差执行

#### 推理延迟与更新率

本文在部署硬件上测量 batch size 1 的推理延迟和实际 policy packet 发布频率。后者不包含由控制接口固定的 chunk 内指令频率 $f_A$。

| 方法/路径 | 推理延迟 (ms) ↓ | Packet 更新率 (Hz) ↑ |
|---|---:|---:|
| ImplicitRDP reactive path | <span style="color:red">X</span> | <span style="color:red">X</span> |
| Temporal ForceVLA | <span style="color:red">X</span> | <span style="color:red">X</span> |
| ForceDelta reference | <span style="color:red">X</span> | <span style="color:red">X</span> |
| ForceDelta residual | <span style="color:red">X</span> | <span style="color:red">X</span> |

#### Reference latency 鲁棒性

在 USB 插入、插头插入和打开抽屉上注入 0--300 ms query-completion delay，以评估对 reference delay 的敏感性。新查询等待期间，两种方法均继续执行最近完成的动作块。Temporal ForceVLA 保留上一完整动作块；ForceDelta-VLA 则保留空力 reference，并继续根据当前力/力矩反馈更新 residual。

| 方法 / Delay (ms) | Success 0 | 50 | 100 | 200 | 300 | Peak 0 | 50 | 100 | 200 | 300 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Temporal ForceVLA | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| **ForceDelta-VLA** | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |

### 4.4 残差分解分析

#### 接触局部性
为刻画力条件修正，根据同步力阈值和任务阶段标签将轨迹划分为 free space、contact onset 和 sustained contact。对于 teacher target $\Delta A_T^{\mathrm{force}}$ 和 student output $\Delta\hat A^{\mathrm{force}}$，分别报告平移范数，以及相对于对应空力 reference 所引起的旋转变化。旋转变化通过将 reference 与力修正后的 6D 表示投影为合法旋转后计算 $SO(3)$ 测地角得到。

#### Reference 陈旧条件下的重建
我们评估随着缓存 reference 逐渐陈旧，reference 与两个 student output 的组合结果和当前完整力条件 teacher 的一致程度。平移误差使用物理单位，旋转误差在投影后使用 $SO(3)$ 测地角；两者均按 cache age 分桶，并与移除 staleness target 的学生比较。最终诊断图包含：（a）三个接触阶段的 residual magnitude；（b）有无 staleness target 时 reconstruction error 随 cache age 的变化。

### 4.5 消融实验

消融包括：示范导出的 residual target、仅瞬时力、零力条件、不带 adapter 的 null token、移除 staleness target，以及移除 schedule replay。所有结果使用红色 X 占位，等待最终实验替换。

## 5. 结论与未来工作

本文提出 ForceDelta-VLA，一种识别 teacher-defined 力响应修正、补偿陈旧 reference 并使该修正独立于完整动作生成执行的策略分解框架。匹配的完整力条件预测与学习式空力预测定义修正目标，无需任务奖励，也无需额外纠正或在线示范；陈旧性感知蒸馏补偿延迟 reference；可独立执行残差通路则利用近期力历史更新位姿修正，无需重新生成完整动作。<span style="color:red">最终稿将总结真实机器人实验如何通过任务性能、接触质量、残差诊断与 reference latency 鲁棒性验证该分解。</span>

该方法受离线 teacher 的准确性与数据覆盖范围限制；有界加性残差无法表达离散策略切换，异步组合在变化延迟下也尚无形式化安全保证。未来工作将引入少量在线纠正、离散恢复决策和延迟感知安全约束。
