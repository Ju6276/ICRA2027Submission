# FoRe-VLA 中文翻译稿

## 标题

**FoRe-VLA：只有残差需要快速更新——面向传感器频率接触丰富操作的力条件可归因修正蒸馏**

## 摘要

接触丰富的操作要求策略在一个控制周期内完成修正，但当前慢—快式力感知视觉—语言—动作（Vision-Language-Action，VLA）模型的快速分支仍然需要重新生成完整的动作块。即使视觉—语言前缀已经完全缓存，该分支每次更新仍需执行完整的流匹配去噪过程：我们在 RTX 4090 上测得其耗时为 76 ms，即 13 Hz，比 100 Hz 力传感器低一个数量级。

我们认为，高频分支根本不需要重新生成完整动作，只需要生成动作中可归因于力的部分，而这是一个简单得多的函数。然而，示范数据只标注总动作，从不单独标注这一分量。我们通过在同一观测上对冻结的时序力感知教师进行两次查询来获得该标签：一次输入实测力历史，另一次输入学习得到的空力条件。两次查询共享视觉—语言前缀和流匹配噪声，因此两个样本之间唯一的差异是力条件。

由此得到的偏差用于监督一个轻量级残差学生，该学生以传感器频率接收实时力输入；教师在空力条件下生成的动作块直接作为慢速参考，因此无需蒸馏额外的标称策略。该学生包含 44.7 M 参数，仅需一次确定性前向传播，实测耗时 2.4 ms（417 Hz），使高频路径的计算时间缩短 31.8 倍。

在生成教师目标时，我们随机化教师的更新周期、推理延迟和抖动，并让学生以缓存上下文的实测年龄作为条件，因此一个训练完成的学生可以部署在训练中未见过的慢速分支频率下。本文报告延迟分析和离线分解诊断；任务层面的结论仍需等待实验章节所述的机器人评估。

## 1. 引言

视觉—语言—动作模型在通用机器人操作方面展现出强大潜力，但接触丰富的任务仍然困难，因为视觉无法直接揭示交互力、初始滑移、冲击或接触稳定性。力感知 VLA 通过将力/力矩或触觉观测引入动作生成来解决这一限制。在这些系统中，真实物理反馈可供策略使用，并且对于精确、安全的交互至关重要。

将力作为输入本身并不能保证及时响应。视觉—语言推理的计算开销相对较大，而力可能在一个控制周期内发生变化。当前一种标准做法是按照频率拆分策略：慢速视觉—语言主干以低频更新可复用的上下文，快速分支则结合缓存上下文和最新力测量进行处理。我们采用这种部署结构。

然而，其快速分支仍然是一个完整的动作生成器。FAVLA 在缓存的 key/value 上下文上重新运行 $\pi_0$ 动作专家，LIFT 则在缓存的视觉—语言前缀上刷新动作块内的反应式动作专家。缓存消除了视觉—语言前向传播，却没有消除动作生成成本：每次高频更新仍然需要完整的流匹配去噪过程。

我们对此进行了直接测量。在视觉—语言前缀已经缓存的情况下，RTX 4090 上一次动作专家前向传播需要 7.6 ms，因此标准 $\pi_0$ 采样器的十个去噪步骤需要 76 ms，即 13 Hz。这与 ForceVLA2 在同一 GPU 等级上报告的 15 Hz 一致。仅一次前向传播就已经占用 100 Hz 时间预算的 76%。因此，动作生成速度与 100–1000 Hz 力传感器之间的差距不是实现细节，而是结构性问题。

我们的出发点是，高频分支从来不需要重新生成完整动作。如果控制命令被写成参考动作与修正量之和，那么只有修正量需要在新力测量到达时重新计算；参考动作在语义时间尺度上变化，可以从最近一次慢速更新中插值得到。修正量比完整动作块简单得多，因此一个更小的网络应该足以预测它——前提是能够为其提供监督。

然而，示范数据只包含总动作和同步力测量，从不包含动作中可归因于力的部分的标签。如果两个分支都使用相同的总动作训练，这种拆分将是欠约束的：参考分支可能吸收接触相关行为，而快速分支可能重复生成标称运动。

我们使用冻结教师构造缺失的标签。首先使用同步的视觉、语言、状态以及原生频率力历史训练时序力感知教师。随后，在同一观测上对其进行两次查询：一次输入实测力历史，另一次输入学习得到的空力条件；两个输出之间的差异即为可归因于力条件的偏差。

为了使这一差异具有意义而非偶然产生，我们采用了两个设计。第一，两次查询共享视觉—语言前缀和流匹配噪声，因此两个样本之间唯一改变的是力条件；若使用独立噪声，差异将主要由采样方差主导。第二，空力条件是在冻结教师上适配的学习式 token，而不是零填充输入。因此，空力分支仍然是一个有效的无力测量策略模式，而不会发生分布外崩溃；否则，得到的偏差将衡量模型失效，而不是力条件的作用。教师在空力条件下生成的动作块同时作为慢速参考，因此不需要蒸馏独立的标称策略。

最终的学生由一个因果力编码器和单个注意力模块组成，并输出一个较短的位姿修正块。该学生包含 44.7 M 参数，只需一次确定性前向传播，实测耗时 2.4 ms（417 Hz），使高频路径的计算时间缩短 31.8 倍。

在生成目标时，我们随机化教师更新周期、推理延迟和抖动，并向学生提供缓存上下文的实测年龄，因此一个训练好的学生可以部署在训练中未见过的慢速分支频率和延迟下。这与固定频率比例或将所有模态降采样到统一训练频率的方法不同。

在移除某个条件的情况下对模型输出求差本身并不是新方法。Classifier-free guidance 在推理时构造相同形式的差异，guidance distillation 则将引导后的组合结果压缩到计算成本更低的学生中。在具身系统中，反事实模态干预已被用于解释驱动预测的模态、消除视觉捷径，以及在测试时重新加权动作。这些方法都使用差异来解释、去偏或调整一个以单一频率运行的模型。

我们的主张是对这种差异的一种不同用途：将其作为跨时间尺度分解策略的监督目标，而这种监督使高频路径能够缩小一个数量级。

本文的贡献如下：

1. 我们提出并实测验证：力感知策略可以按照输出归因而非仅按照网络架构进行分解。使用教师中可归因于力条件的偏差而不是完整动作监督高频分支，可将该分支的单次更新时间从 76 ms 降低至 2.4 ms，使反应式控制从 13 Hz 提升到力传感器频率。
2. 我们为该目标提出一个定义明确的反事实构造方法：在冻结教师上适配学习式空力条件，并在共享前缀和共享流匹配噪声下进行查询，从而将力条件与采样噪声以及零填充干预造成的分布偏移区分开来。
3. 我们提出与具体频率无关的异步训练方案。在生成目标时随机化慢速更新周期、推理延迟和抖动，并让学生以实测上下文年龄作为条件，使同一个学生能够迁移到不同部署频率。

本文在实验部分报告延迟分析和离线分解诊断。任务层面的结论，特别是以 100 Hz 而非 30 Hz 执行是否能够改善接触结果，将由机器人实验验证；我们同时给出能够否定该分解方法的判据。

## 2. 相关工作

### 2.1 力感知视觉—语言—动作模型

力和触觉反馈能够揭示图像中存在歧义或不可见的物理交互。ForceVLA 将六轴力/力矩感知作为 VLA 的一等模态，并通过力感知混合专家结构将其与视觉—语言特征融合。ForceVLA2 增加了阶段级力提示、直接原始力路径以及混合力—位置控制。TaF-VLA 对齐触觉与力表示，Tactile-VLA 和 VLA-Touch 则将物理反馈连接到高层推理和下游控制。FD-VLA 采取相反方向，从视觉和状态中蒸馏力表示，使推理阶段不再需要物理传感器。

这些工作主要研究如何表示、对齐和融合力或触觉。我们假设训练和部署期间力传感器始终可用，并将此类模型作为需要被分解的教师。

### 2.2 反应式与异步物理反馈

当所有模态都被同步到视觉或 VLA 推理频率时，高频物理感知很容易丢失。ManipForce 通过频率感知表示保留异步 RGB 和力/力矩流，Reactive Diffusion Policy 则将慢速视觉规划器与快速触觉控制器配对。

FAVLA 使用慢速视觉—语言主干生成 key/value 上下文，再由快速动作专家结合最新力序列进行处理，并根据预测的力方差信号调度动作专家的执行次数。LIFT 在预训练策略上接入反应式动作专家，通过零初始化交叉注意力注入因果力记忆，缓存视觉—语言前缀，并将力窗口对齐到固定的推理延迟偏移。Fast-in-Slow 通过参数共享将高频系统嵌入慢速推理主干。DAM-VLA、AT-VLA、T-Rex 和 PhaForce 分别引入原生频率模态更新、可变频率专家或按阶段调度的高频修正。Compliant Residual DAgger 则通过在线执行和人工修正，在慢速基础策略之上学习快速残差。

我们采用这种部署结构，但在两个方面与其不同。

第一，上述方法中的高频分支仍然是动作生成器。缓存消除了视觉—语言前向传播，但每次更新仍然需要完整的动作专家去噪过程，因此这些系统的执行频率通常被限制在约 13–33 Hz。我们为高频分支分配一个严格更小的任务。

第二，这些方法的时间配置通常在训练期间固定，例如固定频率比例、固定延迟偏移，或将所有模态统一重采样到一个频率。我们则随机化时间配置，并将实测上下文年龄提供给学生。

我们与 Compliant Residual DAgger 的监督来源也不同。其残差目标来自在线交互和人工修正，而我们的目标来自对冻结教师的离线反事实查询。该工作表明在线修正对于接触丰富的残差学习十分重要，这也限制了本文纯离线构造所能达到的效果。

### 2.3 空条件查询与反事实差分

学习式缺失模态 token 使用共享可训练嵌入替代缺失输入，避免将不可用测量与物理上有效的零值混淆。Masked Modality Projection 则利用仍然可用的模态预测缺失模态 token。

我们利用学习式 token 的第二项性质，而不是第一项性质：本文不以缺失传感器鲁棒性为目标，而是需要一种能够隐去当前测量、同时使教师保持在有效分布内的查询方式。

在移除某个条件的情况下对生成模型输出求差本身已有研究。Classifier-free guidance 在推理时使用条件输出减去无条件输出的差异来增强条件控制；guidance distillation 则训练学生通过一次推理复现引导后的组合结果，从而减少每个采样步骤的计算成本。与本文的区别在于差异的用途：guidance distillation 在相同频率下压缩同等规模的模型，并不涉及模态；我们则将差异作为一个独立、更小并以不同频率运行的模块的学习目标。

反事实模态干预也已被用于具身策略。Counterfactual Modality Attribution 通过耦合反事实上的 Shapley 值量化多模态大语言模型中的模态贡献；Embodied Interpretability 通过完整信息策略与反事实策略之间的散度评估 VLA 输入的因果影响；CofactVLA 使用语言遮蔽反事实分支消除视觉捷径。与本文机制最接近的 IDR 在冻结 VLA 上执行零填充模态干预，测量由此产生的输出变化范数，并在测试时将其作为门控残差重新融合。

我们的构造在两个方面不同。上述方法使用反事实差异来解释、去偏或调整一个以单一频率运行的模型，没有训练独立模块；我们使用它跨时间尺度分解策略，而这正是高频路径能够产生数量级变化的原因。

此外，IDR 使用零填充进行干预，而我们在冻结教师上适配学习式空力条件。零填充的力输入处于教师训练分布之外，因此诱导出的输出变化会同时包含力信息的作用和教师面对无效输入时的性能退化。我们在实验中直接比较这两种方式。

与已有工作的谨慎表述一致，我们不声称该偏差是力的因果效应。视觉和状态仍然是两个分支的条件，因此该偏差仅表示教师在完整力条件和空力条件下的输出变化，不作更强解释。

## 3. 问题定义与方法

### 3.1 问题定义

我们考虑接触丰富的机器人操作。系统接收视觉观测 $V$、语言指令 $L$、机器人状态 $S$，以及从带时间戳的原生频率数据流到达的力/力矩测量 $F$。这些数据流是异步的：图像以相机频率到达，视觉—语言前向传播以更低频率完成，而力数据的到达频率比前两者高一到两个数量级。

动作在末端执行器坐标系中表示为

$$
A=
[\,p\in\mathbb{R}^{3};\;
r\in\mathbb{R}^{6};\;
g\in\mathbb{R}\,]
\in\mathbb{R}^{10},
$$

其中，$r$ 是旋转矩阵的前两列。我们使用这种表示而不是欧拉角，因为后续目标是动作差值，而欧拉角在 $\pm\pi$ 处的分支切换会向待学习的量中引入虚假的 $2\pi$ 跳变。记 $A^p\in\mathbb{R}^9$ 为动作的位姿部分。

令 $t_k$ 为最近一次慢速更新的时间戳，$t$ 为当前控制时刻。控制命令由参考动作与修正量构成：

$$
A^{\mathrm{cmd}}(t)
=
A^{\mathrm{ref}}(t)
+
\Delta A(t),
\qquad
\Delta A(t)\in\mathbb{R}^{9}.
$$

其中，$A^{\mathrm{ref}}(t)$ 从 $t_k$ 时刻生成的动作块中插值得到，修正量仅作用于位姿；夹爪通道由慢速分支负责，因为抓取决策属于语义决策而非快速反应。

这种分解使高频控制在计算上变得可行：$A^{\mathrm{ref}}$ 在语义时间尺度上变化，可以低成本插值，因此每当新力测量到达时，只需重新计算 $\Delta A$。

但这也产生了监督问题。示范数据提供总动作和同步力，却没有 $\Delta A$ 的标签。如果使用相同总动作监督两个分量，分解将是欠约束的：参考动作可能吸收接触相关行为，而修正分支可能重复生成标称运动。我们从冻结的力感知教师中获得 $\Delta A$ 的显式目标。

### 3.2 时序力感知教师

教师以 ForceVLA 为基础，而 ForceVLA 本身是一个 $\pi_0$ 风格的流匹配策略。我们将其瞬时力表示替换为因果时序编码器。

对于每个教师推理时间戳 $t_k$，从带时间戳的原生频率数据流中读取固定长度窗口：

$$
\mathcal{F}_k
=
\left\{
F(\tau)
\mid
t_k-w\leq\tau\leq t_k
\right\}.
$$

该窗口直接从原生频率数据流中选择，不先降采样到视觉观测频率。系统使用有效性 mask，使只填充了一部分的窗口能够与真实报告零力的窗口区分开。

因果时序卷积网络对该窗口进行编码：

$$
z_k^F=E_T^F(\mathcal{F}_k).
$$

编码结果被投影到原始力融合维度。教师仍然使用单个力 token，因此后续多模态融合和动作生成路径保持不变。其预测为

$$
A_{T,k}^{\mathrm{full}}
=
T_\theta(V_k,L,S_k,z_k^F)
\in\mathbb{R}^{H\times10}.
$$

时序编码器使教师能够观察短时交互动态，包括接触开始、冲击瞬态、振荡和恢复，而这些现象无法由视觉频率下的单个力样本表达。

### 3.3 学习式空力条件

为了在不提供当前力测量的情况下查询同一个教师，我们引入学习式 token：

$$
z_{\varnothing F}\in\mathbb{R}^{D_F}.
$$

该 token 被插入力表示 $z_k^F$ 进入融合模块的位置。它表示缺少当前样本的特定力观测，而不是物理零力。由于所有样本共享同一个 token，它可以编码数据集层面的力先验，但不能携带当前力历史。

隐去力输入会使教师性能下降，而退化的分支会使后续差值衡量模型退化而非力条件。为使空力分支恢复能力，我们冻结全部教师权重，只优化 $z_{\varnothing F}$ 和一个 rank-$\rho$ adapter。该 adapter 仅在 null mode 下修正流速度的位姿通道：

$$
v^p
\leftarrow
v^p+
W_{\mathrm{out}}\,
\sigma(W_{\mathrm{in}}h),
$$

其中

$$
W_{\mathrm{in}}\in\mathbb{R}^{\rho\times d},
\qquad
W_{\mathrm{out}}\in\mathbb{R}^{9\times\rho}.
$$

$h$ 是动作专家的隐藏状态，$\sigma$ 为 SiLU 非线性函数。训练目标是教师原本针对示范动作使用的动作回归损失：

$$
\mathcal{L}_{\varnothing}
=
d\!\left(
T_\theta(V_k,L,S_k,z_{\varnothing F}),
A_k^{\mathrm{expert}}
\right).
$$

这一步使本文区别于零填充干预：零向量处于教师训练分布之外，而上述训练产生同一网络中有效的无当前力测量模式。该 adapter 是教师的一种运行模式，而不是一个独立策略；本文不训练或部署独立的无力 VLA。

### 3.4 成对反事实差分

教师随后被冻结并接受两次查询。由于教师是流匹配采样器，直接进行两次独立查询会使结果同时受到力条件和采样噪声影响。因此，除了力条件之外，我们共享所有其他变量。

视觉—语言前缀及其 key/value cache 只计算一次，并使用同一个噪声样本 $\epsilon$ 初始化两次积分：

$$
A_{T,k}^{\mathrm{full}}
=
T_\theta(V_k,L,S_k,z_k^F;\epsilon),
$$

$$
A_{T,k}^{\mathrm{null}}
=
T_\theta(V_k,L,S_k,z_{\varnothing F};\epsilon),
\qquad
\epsilon\sim\mathcal{N}(0,I).
$$

共享 $\epsilon$ 不是一种计算优化。如果使用独立噪声，下面的差值会主要由采样方差而不是力条件主导，目标也会变成噪声。共享前缀还会使目标提取成本减半，因为视觉—语言前向传播是最昂贵的部分。

空力条件动作块作为参考：

$$
A_{T,k}^{\mathrm{nom}}
=
A_{T,k}^{\mathrm{null}}.
$$

可归因于力条件的偏差定义为

$$
\Delta A_{T,k}^{F}
=
\left(A_{T,k}^{\mathrm{full}}\right)^p
-
\left(A_{T,k}^{\mathrm{null}}\right)^p
\in\mathbb{R}^{H\times9}.
$$

我们将 $\Delta A_{T,k}^{F}$ 解释为：将学习式空力条件替换为实测力历史时，教师输出所发生的变化。这是输出空间差分，不包含正负样本对，也不使用对比损失。

它也不是力的因果归因。视觉和状态仍然是两个分支的条件，并且其本身可能携带接触线索。因此，该偏差只是条件差异，不作更强的解释。

### 3.5 慢速参考与缓存意图

慢速分支是以 null mode 运行的冻结教师，不蒸馏独立标称策略。每次慢速更新 $k$ 产生参考动作块 $A_{T,k}^{\mathrm{null}}$，并在同一次前向传播中产生提供给快速分支的紧凑上下文。

视觉—语言前缀 $H_k\in\mathbb{R}^{S\times d}$ 很长，其中 $S$ 可达数百。我们在前缀 mask 下，将其确定性地按连续区间进行均值池化，压缩成 $M$ 个区间：

$$
Z_k^{\mathrm{intent}}
=
\mathrm{Pool}_M(H_k,m_k)
\in\mathbb{R}^{M\times d}.
$$

系统同时保存有效性 mask，使完全由 padding 组成的区间被定义为零，而不是产生除零错误。

池化过程固定且不可学习，从而保证部署时可以从实时前缀中复现相同上下文，而无需增加额外训练参数。学生内部的学习式注意力投影器再将这些区间映射到自身宽度。意图 token 不需要复现教师的特定隐藏状态，其作用仅是向修正分支提供任务上下文。

在两次慢速更新之间，参考动作按照教师动作周期 $\tau_A$ 从缓存动作块中插值得到，并向学生提供该动作块的陈旧程度。

快速分支不接收新图像。视觉信息只通过 $Z_k^{\mathrm{intent}}$ 进入快速分支，因此其年龄需要显式给出。这与 FAVLA 的快速专家设计一致，也与教师差分的定义一致：在教师差分中，视觉—语言前缀在两个分支间共享，因此不是目标变化的来源。

### 3.6 快速力条件残差学生

学生接收实时力并输出位姿修正。一个与教师同类、但使用更短窗口并以传感器频率运行的因果 TCN 对最新力进行编码：

$$
Z_t^F
=
E_S^F(F_{t-w_S:t}).
$$

有效性 mask 在网络 stem 处应用，因此无效位置无论如何归一化都严格贡献零。

其他输入构成一个无序条件集合，包括缓存意图区间、最新机器人状态、当前参考动作以及时间 token。每种输入被投影到统一宽度，并加入学习式类型嵌入：

$$
X_t
=
\left[
\widetilde Z_k^{\mathrm{intent}};
Z_t^F;
P_S(S_t);
P_A(A^{\mathrm{ref}}(t));
P_\Gamma(\gamma_t);
q
\right],
$$

其中，$\gamma_t$ 是时间特征，$q$ 是学习式残差查询 token。

我们有意不使用位置编码。基于索引的旋转位置编码会人为赋予这些条件一个顺序，并且当意图区间数量 $M$ 改变时改变所有编码。

一个带 grouped-query attention 的单层 pre-norm 注意力模块处理该集合。所有条件彼此之间进行双向注意，而条件 token 看不到查询 token：

$$
H_t=\mathrm{Block}(X_t).
$$

线性头根据查询 token 的隐藏状态一次性输出整个修正动作块：

$$
\Delta\hat A_{t,1:K}
=
W_{\mathrm{res}}H_t[q]
\in\mathbb{R}^{K\times9}.
$$

$W_{\mathrm{res}}$ 使用零初始化，因此训练从恒等控制器开始：

$$
A^{\mathrm{cmd}}=A^{\mathrm{ref}}.
$$

输出以 $\tau_A$ 为间隔的 $K$ 个步骤，而不是单个步骤，使控制循环能够容忍自身延迟以及未知部署频率。如果某次推理延迟，或者执行器的运行速度快于学生，上一个动作块的后续步骤仍然是有效命令。

尽管动作块延伸到未来，这种设计仍然是因果的，因为教师仅根据 $t_k$ 时刻的单次观测生成整个动作块。第 $j$ 步表示教师根据当前可见力信息，对未来第 $j$ 个动作周期的预测；标签中没有使用未来力或未来状态。

### 3.7 蒸馏目标

只有快速学生需要训练。令 $\Delta A^F_{T,1:K}$ 和 $A^{\mathrm{full}}_{T,1:K}$ 为教师目标的前 $K$ 步，令归一化步骤权重满足 $w_j\propto\beta^{j-1}$。

第一项损失保持残差专门化：

$$
\mathcal{L}_{\mathrm{res}}
=
\sum_{j=1}^{K}
w_j
d\!\left(
\Delta\hat A_{t,j},
\Delta A^F_{T,j}
\right).
$$

第二项损失要求组合后的控制命令复现完整力教师：

$$
\mathcal{L}_{\mathrm{comp}}
=
\sum_{j=1}^{K}
w_j
d\!\left(
(A_j^{\mathrm{ref}})^p+
\Delta\hat A_{t,j},
(A_{T,j}^{\mathrm{full}})^p
\right).
$$

总目标为

$$
\mathcal{L}
=
\lambda_{\mathrm{res}}\mathcal{L}_{\mathrm{res}}
+
\lambda_{\mathrm{comp}}\mathcal{L}_{\mathrm{comp}}.
$$

衰减系数 $\beta<1$ 反映了第一步会在通常情况下执行，而后续步骤只会在学生暂停或执行器运行更快时执行。

本文不使用标称模仿损失，因为慢速参考来自冻结教师，而不是训练得到的策略。本文也不对示范动作加入行为克隆损失，因为该损失会要求学生吸收教师自身的模仿误差，而这些误差不能归因于力，并会污染本文所依赖的目标。教师与示范之间的误差在实验中作为独立诊断量报告。

### 3.8 与频率无关的目标生成

在单一慢速频率和单一延迟下训练的学生会学习该特定运行点。我们将时间配置视为一个分布。

目标首先以完整频率提取，每个训练 epoch 再重新生成时间调度：从 $[1/f_{\max},1/f_{\min}]$ 采样慢速更新周期，从 $[\ell_{\min},\ell_{\max}]$ 采样推理延迟 $\ell$，并从 $[-\eta,\eta]$ 采样每次更新的抖动。

重新采样始终基于未加入抖动的原始时间网格，而不是上一次已经加入抖动的时间戳，因此抖动只被应用一次，不会在多次重采样中累积。

在 $t_k$ 发布的数据包只有到 $t_k+\ell$ 后才能使用；在此之前的训练行被排除。如果一个数据包在传输中被更新的数据包超过，则丢弃旧数据包。这避免了简单提取方式可能产生、但物理上不可能存在的零年龄上下文。

学生随后被告知当前上下文的有效程度。令 $t_k$ 为正在使用的数据包对应的观测时间：

$$
\gamma_t
=
\left[
\frac{t-t_k}{c},
\alpha_t
\right],
\qquad
\alpha_t
=
\frac{t-t_j}{\tau_A}
\in[0,1).
$$

其中，$c$ 是固定的年龄缩放系数，$\alpha_t$ 是 $t$ 位于相邻两个动作块 waypoint 之间的插值比例。

这两个量不是共线的：上下文年龄会在一个慢速周期内持续增长，而 $\alpha_t$ 会在每个动作间隔内周期性变化。因此，两者共同表示语义上下文有多陈旧，以及控制命令位于一个动作区间的什么位置。

离线和在线阶段必须使用相同的 $c$，并且 $c$ 必须覆盖整个随机时间范围而不发生截断，因为截断后的年龄会隐藏学生原本需要补偿的上下文陈旧程度。

### 3.9 复杂度与可达到频率

教师差分的目的在于缩小高频路径。现有慢—快力策略虽然缓存视觉—语言前缀，却仍然在控制循环中保留动作专家，因此每次更新需要在动作专家上执行 $N_{\mathrm{flow}}$ 次去噪传播。

本文方法只需在小型网络上执行一次确定性前向传播。令 $d_{\mathrm{AE}}$ 和 $d_S$ 分别为动作专家和学生的宽度，则两者单次更新的复杂度分别为

$$
\mathcal{O}
\!\left(
N_{\mathrm{flow}}
L_{\mathrm{AE}}
d_{\mathrm{AE}}^2
\right)
$$

和

$$
\mathcal{O}
\!\left(
L_Sd_S^2
\right).
$$

去噪步数、网络深度和网络宽度三方面都使本文方法具有更低计算成本。

### 3.10 异步执行

部署阶段，教师以 null mode 作为慢速分支运行在独立线程中，并发布包含参考动作块、池化意图区间、观测时间戳以及数据包就绪时间的数据包。力数据以传感器频率持续写入环形缓冲区，机器人状态则独立更新。

学生运行在第二个线程中。每个周期读取最新数据包、最新力窗口和当前状态，并发布带有自身有效时间范围的命令动作块。

第三个循环为执行器。它只在当前控制时刻采样动作块并应用参考动作与修正量的组合。将发布过程与消费过程分离，使执行器永远不会因等待推理而阻塞。

如果学生更新延迟，系统会退化为执行上一个动作块的后续步骤，而训练过程已经覆盖了这一情况。如果不存在任何尚未过期的动作块，执行器将保持并报告错误，而不是进行外推。

系统还强制执行两个容易被静默违反的一致性要求。第一，学生接收的状态和力窗口必须使用与训练时相同的归一化统计量。第二，参考动作必须重新基于生成数据包时所观测的状态，而不是当前原始状态。

部署路径读取目标提取阶段记录的接口约定，包括池化区间数、动作宽度、动作块长度、动作周期、年龄缩放系数以及训练使用的时间范围；若部署配置与该约定不一致，系统将拒绝运行。

## 4. 实验

### 4.1 实验目标

实验依次回答四个问题：

1. 使用可归因于力条件的偏差而不是完整动作监督高频分支，是否确实改变了可达到的控制频率？
2. 该偏差中是否包含足够信号供小型学生学习，学生是否真正使用力，而不是从上下文中推断修正？
3. 以传感器频率执行是否能够改善接触结果，优于缓存前缀动作专家可达到的 13–33 Hz？
4. 随机时间训练是否使同一个学生能够迁移到不同部署频率？

第一个问题由直接测量回答。第三个问题决定本文贡献是否成立：如果以 100 Hz 运行并不能改善接触行为，那么有利的延迟结果本身没有意义。

### 4.2 高频路径可达到的频率

我们在同一设备 RTX 4090 上，以 batch size 1 测量两种设计的单次更新成本。

对于缓存前缀 baseline，我们直接提供视觉—语言 key/value cache，并测量动作专家在 816 个前缀 token 和 51 个后缀 token 上的一次前向传播，再乘以采样器的去噪步数。视觉—语言专家被替换为一个不会执行的窄占位模块，因此该测量只包含缓存后仍然存在的增量成本。

对于本文学生，我们测量包含力编码器和意图投影器在内的一次完整前向传播。

| 高频路径 | 参数量 | 延迟 | 频率 |
|---|---:|---:|---:|
| 动作专家，$N_{\mathrm{flow}}=1$ | 334 M | 7.6 ms | 132 Hz |
| 动作专家，$N_{\mathrm{flow}}=4$ | 334 M | 30.4 ms | 33 Hz |
| 动作专家，$N_{\mathrm{flow}}=10$ | 334 M | 76.1 ms | 13 Hz |
| **残差学生（本文）** | **44.7 M** | **2.4 ms** | **417 Hz** |

在标准 $N_{\mathrm{flow}}=10$ 设置下，本文方法使高频路径的计算时间缩短 31.8 倍。

13 Hz 与 ForceVLA2 在同一 GPU 等级上报告的 15 Hz 一致，支持该测量能够代表真实动作专家成本，而不是测试程序造成的偶然结果。

此外，即使只有一次动作专家前向传播，也已经占用 10 ms 时间预算的 76%。因此，该限制不能仅通过减少采样步数消除；若要使用该路径达到 100 Hz，就需要一步流匹配，而这对于该策略类型不是可用的采样器。本文学生则在相同预算下仍保留约四倍余量。

### 4.3 任务与评估协议

实验覆盖四类接触丰富的操作任务：建立接触、沿表面或机构进行约束运动、精密插入，以及从外部扰动中恢复。

至少一个任务必须要求力闭合，即纯视觉策略会完全失败，因为只有在这种任务中，可归因于力条件的修正才可能具有决定性作用。

每项任务的示范数据都包含同步的多视角图像、语言指令、机器人状态、控制动作以及以原生频率保留的带时间戳六轴力/力矩测量。

每项任务将报告 **[N]** 次试验和 **[K]** 个随机种子，并随机化物体配置、初始姿态和接触条件。只有在完成目标且未违反预设力限制时，试验才计为成功。所有方法共享相同的示范划分、控制接口和硬件。

### 4.4 Baseline 与指标

比较方法包括：

1. **视觉—语言策略：** 不使用力输入，也没有高频分支。
2. **瞬时力教师：** 只在视觉帧时刻采样力，用于分离原生频率力历史的作用。
3. **时序力感知教师：** 本文教师，以其可达到的频率作为单体策略执行。它既是学生蒸馏的性能上限，也是学生必须超过的频率下限。
4. **缓存前缀快速分支：** 以 FAVLA 和 LIFT 的方式，在缓存前缀上重新运行教师动作专家，并按照延迟表允许的频率执行。这是本文主要比较对象。
5. **直接慢—快行为克隆：** 使用本文学生架构，但只从示范动作训练，不使用成对教师目标，用于分离监督方式和网络架构的影响。
6. **FoRe-VLA：** 完整方法。

任务指标包括成功率和分阶段完成率。交互质量指标包括峰值力、力冲量、力限制违反次数以及扰动后的恢复率。

分解质量通过残差误差

$$
d(\Delta\hat A,\Delta A_T^F)
$$

和组合误差

$$
d(A^{\mathrm{ref}}+\Delta\hat A,A_T^{\mathrm{full}})
$$

衡量。两者都与零残差 baseline 比较，以表明模型实际捕获了多少偏差。

误差在重建后的绝对位姿上以物理单位报告，而不是在归一化增量上报告，并分别给出平移和旋转误差。运行时指标包括慢速频率、快速频率、端到端延迟和参数量。

### 4.5 实现细节

教师是一个 $\pi_0$ 风格 ForceVLA，在视觉—语言主干和动作专家上均使用 LoRA adapter。动作预测长度为 $H=50$，动作频率为 30 Hz。

其因果 TCN 力编码器从 100 Hz 带时间戳数据流中读取 100 ms 窗口，并拒绝年龄超过 12 ms 的样本。Null adaptation 在 9 个位姿通道上使用 rank $\rho=32$。教师目标使用 $N_{\mathrm{flow}}=10$ 提取，并将前缀池化为 $M=16$ 个宽度为 2048 的区间。

学生使用一个宽度为 1024 的注意力模块，MLP 维度为 4096，包含 8 个 query head、1 个 key/value head，head dimension 为 256，意图宽度为 1024，并输出 $K=5$ 个步骤，步骤衰减系数 $\beta=0.5$。其力编码器同样读取 100 Hz 数据流中的 100 ms 窗口。

随机时间配置从 5–15 Hz 采样慢速频率，从 50–300 ms 采样推理延迟，每次更新加入 $\pm10$ ms 抖动。年龄缩放系数设为 $c=0.6$ s，以覆盖整个时间范围而不截断。示范数据只使用力数据流能够持续达到至少 100 Hz 的轨迹。

### 4.6 主结果比较

主比较报告接触丰富操作中的任务成功率、恢复率、峰值力、力限制违反次数、延迟以及快速分支频率。机器人实验数值尚待填入。

结果将分别分析自由空间运动、接触开始、持续接触和扰动恢复阶段。这种分阶段分析十分重要，因为修正量预计应集中在交互附近。在自由空间中，标称策略和组合策略表现相近是分支专门化的证据，而不是负面结果。

### 4.7 执行频率消融

这是决定性实验。

实验保持训练好的学生不变，只将其执行频率限制为 13、30、50 和 100 Hz。前三个频率对应缓存前缀动作专家在不同去噪步数下可达到的频率。实验测量峰值接触力、力冲量和任务成功率。

此外，对匹配的接触事件使用三种设置进行回放：

1. 仅使用慢速分支；
2. 慢速和快速分支以慢速频率同步执行；
3. 完整异步执行。

实验报告从检测到力变化到第一个修正命令之间的延迟、恢复前最大力以及返回标称轨迹所需的时间。

如果 100 Hz 下的接触结果没有优于 30 Hz，那么延迟表中的优势没有实际作用，频率分解论点也将失败，无论残差拟合得多好。

### 4.8 反事实分解分析

我们检查用于生成目标的成对教师预测。对于每条轨迹，将 $\lVert\Delta A_T^F\rVert$ 与实测力和任务阶段共同绘制。有效的分解应在无约束运动中产生较小偏差，并在接触开始、瞬态和恢复附近产生更大且结构化的偏差。

我们将学习式空力条件与两种已有替代方案比较：与零填充干预一致的零力向量，以及随机力 token masking。

该实验检验学习式 null mode 能否使教师保持在有效分布内，以及零填充输入是否会将力贡献与教师面对无效输入时的性能退化混合。

我们还分别报告教师与示范之间在平移、旋转和夹爪通道上的误差：

$$
d(A_T^{\mathrm{full}},A^{\mathrm{expert}}).
$$

这一误差决定了任何从该教师蒸馏出的学生所能达到的上限。

### 4.9 否定判据

我们在报告结果前固定以下阈值，从而将较弱的分解识别出来，而不是在结果产生后对其进行解释。

令可归因于力条件的偏差能量为

$$
\sigma_F^2
=
\mathbb{E}
\lVert\Delta A_T^F\rVert^2,
$$

令慢速分支仅由参考陈旧产生的误差为

$$
\sigma_R^2
=
\mathbb{E}
\lVert
A^{\mathrm{ref}}
-
A_T^{\mathrm{null}}
\rVert^2.
$$

1. **信号幅值：** $\sigma_F^2/\sigma_R^2\geq1$ 表明可归因于力条件的分量至少与陈旧误差相当，值得设置专用分支。若该比例低于 0.25，则组合误差主要由参考陈旧主导，提高慢速频率将是更合理的干预。
2. **力依赖性：** 相对于零残差 baseline，完整学生带来的残差误差下降必须比移除力输入的学生至少高 10 个百分点。如果将力输入置零几乎不影响学生，则学生是在从意图、状态和参考中推断修正，本文方法的前提不成立。
3. **接触局部性：** 第二项判据中的差距必须在接触阶段大于自由空间阶段。否则，学生学习到的行为不是由接触驱动的。

### 4.10 消融实验

实验包括以下消融：

1. **力表示：** 瞬时力、降采样到视觉频率的力历史，以及原生频率因果力历史。
2. **反事实构造：** 学习式空力条件、零力填充、不使用 rank-$\rho$ adapter 的学习式 token，以及独立而非共享的流噪声。最后一项用于检验噪声耦合是否是构造有效目标的必要条件。
3. **学生上下文：** 分别移除缓存意图、机器人状态、参考动作 token、时间 token 或多 token 力序列。
4. **时间方案：** 比较固定慢速频率和延迟与随机化方案，并分别在训练运行点和未见频率下测试，以检验频率迁移。
5. **动作块输出：** 在加入推理停顿的情况下比较 $K=1$ 与 $K=5$。

## 5. 结论与未来工作

本文提出 FoRe-VLA，通过输出归因而不是仅通过网络架构分解力感知操作策略。

现有慢—快力策略虽然缓存视觉—语言前缀，却仍然在高频循环中保留动作生成器，因此每次更新需要完整的流匹配去噪过程。在 RTX 4090 上，该过程需要 76 ms，即 13 Hz。

我们的观察是，高频分支只需生成动作中可归因于力条件的部分。我们通过在共享视觉—语言前缀和共享流匹配噪声下，分别使用实测力窗口和学习式空力条件查询冻结的时序力感知教师，为该分量获得监督标签。

由此得到的目标足够简单，因此一个具有 44.7 M 参数、仅需一次确定性前向传播的学生即可完成预测，达到 417 Hz，使高频路径的计算时间缩短 31.8 倍。

在生成目标时随机化慢速更新周期、推理延迟和抖动，并让学生以实测上下文年龄作为条件，使同一个学生能够部署在训练中未见过的频率下。

本文存在若干需要明确说明的限制。完整力输出减去空力输出是策略空间中的条件差异，而不是力的因果归因。视觉和状态同时作为两个分支的条件，并可能携带接触线索，因此我们只声称该偏差是教师在完整力条件与空力条件下的输出变化。

该构造的性能上限还受到教师限制。教师产生的任何模仿误差都会被继承，因此我们没有在学生上加入行为克隆损失。

最重要的限制是，本文的监督完全来自离线数据。Compliant Residual DAgger 和 LIFT 均表明，接触反馈会导致与策略相关的分布偏移，而离线数据无法覆盖该偏移；在线修正轨迹对于接触丰富的残差学习十分重要。

本文的反事实目标来自示范，因此继承了这一限制。其优势是不需要额外机器人交互，也不需要人工修正即可生成目标。离线反事实目标是否足够，或是否必须与有限的在线训练阶段结合，是我们认为最重要的未来研究问题。

对于需要离散策略切换的交互，加性修正也可能不够充分。异步组合还要求显式处理延迟、稳定性和安全限制。

最后，当前报告的延迟和分解结果均为离线结果。任务层面的结论，特别是以 100 Hz 执行是否能够相对于 30 Hz 改善接触结果，仍需要按照实验部分给出的判据在真实硬件上验证。
