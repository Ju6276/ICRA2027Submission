# ForceDelta-VLA 中文翻译稿

> 对应当前英文主稿，用于逐段审阅。公式与统计范围以英文稿为准；文献引用见英文版本。首页右侧 rollout 图片由作者后续补入。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力感知视觉—语言—动作（VLA）策略改善了接触密集型操作，但通常在同一个生成式动作预测中耦合任务层运动与接触相关调整。示范没有提供将该预测分解为可复用参考动作与修正的显式标签。我们提出 ForceDelta-VLA，一种修正蒸馏框架，通过冻结教师的成对力条件预测与学习式无力输入预测构造显式力修正目标。单独的延迟修正目标补偿参考动作差异和参考状态变化。训练通过异步调度重放，使用执行时可获得的缓存任务上下文。所得轻量策略根据近期力历史和机器人状态调整参考动作，在两次参考动作更新之间响应接触变化，无需重新生成完整动作块。在九项单臂和双臂接触密集型任务上，ForceDelta-VLA 的平均成功率为 82.2%，ForceVLA 为 54.4%。相较 ForceVLA，两个平台的成功试验平均峰值接触力均降低约 26%。

**图 1：ForceDelta-VLA 概览。** 冻结教师提供力修正与延迟修正目标，用于训练轻量学生策略。执行时，学生联合预测两项修正，调整慢教师提供的参考动作。

## 1. 引言

接触密集型操作要求动作随接触发展而调整。例如，在 USB 插入过程中，接近方向在相邻控制周期内可能仍然有效，而局部对齐需要响应新观测到的接触状态。力感知 VLA 将交互反馈用于动作生成，但通常同时预测任务运动与接触相关调整。将慢通路预测的参考动作与快速更新的修正分开，可以使接触反馈在不等待新完整动作块的情况下影响执行（图 1）。

从示范中学习这种修正并不容易。示范记录最终动作，没有指明哪部分属于参考运动，哪部分属于接触相关调整。多个加性分解都可以重建同一个标签。因此，修正通路除了需要快速架构，还需要明确的监督目标。

现有异步策略分离计算或传感器更新频率；残差强化学习与纠正式模仿学习则从奖励或额外在线交互获得修正信号。本文仅利用已有示范解决监督问题，同时保留异步执行。

我们提出 ForceDelta-VLA，学习可复用参考动作的力修正和延迟修正。冻结教师的成对力条件预测与学习式无力输入预测提供力修正监督。单独的延迟目标补偿此前参考动作与当前无力输入预测之间的差异，并包含参考状态变化。训练得到的轻量策略根据近期力历史和机器人状态，独立于参考动作生成更新这两项修正。

我们在五项单臂和四项双臂真实机器人任务上评估 ForceDelta-VLA，涵盖接触建立、精密插入、摩擦主导运动和受约束操作。

本文贡献如下：

1. 提出**无力输入修正蒸馏（force-agnostic correction distillation）**，仅使用原始数据集，通过冻结教师的匹配力条件预测与学习式无力输入预测，构造显式力修正目标。
2. 构建**状态一致的双修正策略（state-consistent dual-correction policy）**，在共同动作坐标系中分别学习力修正与延迟修正，并复用任务上下文，独立于参考动作生成更新两项修正。延迟目标同时包含参考动作差异和参考状态对齐。
3. 相较 ForceVLA，在九项单臂和双臂任务上将平均成功率从 **54.4% 提升至 82.2%**，并将两个平台上成功试验的平均峰值接触力均降低约 **26%**。

## 2. 相关工作

### 2.1 力感知操作策略

通用 VLA（如 $\pi_{0.5}$）从视觉、语言和机器人状态预测动作，不使用实测力/力矩反馈。力感知 VLA 通过多模态融合、提示、时序记忆、混合力—位置控制或力感知优化整合力/力矩信息；力中心模仿学习则从示范中联合建模运动与交互力。这些方法在统一策略输出中保留任务运动和物理适应。FD-VLA 从视觉与状态蒸馏潜在力表征，用于不依赖实测 wrench 反馈的推理；ForceDelta-VLA 使用实测 wrench 反馈更新动作修正。

### 2.2 多频率与结构化操作策略

视觉运动方法学习异步 RGB 与力/力矩流，按交互阶段调整扩散推理频率，或引入快速修正及混合力—位置通路。VLA 方法也分离模态更新频率。FAVLA 使用慢速语义骨干和可变频率的力条件动作专家重新生成动作块；DAM-VLA 按各传感器频率刷新模态专用缓存，由 100 Hz 动作头读取。RTC 通过 inpainting 保持异步生成动作块之间的连续性。ForceDelta-VLA 则复用参考动作，独立于参考动作生成更新修正。

### 2.3 残差策略学习

修正策略学习用学习得到的修正增强标称控制器，已用于接触密集型操作。残差强化学习与纠正式模仿学习通过奖励或在线人工纠正获得监督，包括对冻结视觉策略进行触觉条件适配，以及用 DAgger 训练反应式力专家。其他模仿学习方法直接规定加性修正结构。ForceDelta-VLA 从成对的力条件与无力输入教师预测导出目标。

### 2.4 策略蒸馏

机器人策略蒸馏通常将迭代扩散或流采样压缩为快速完整动作生成器；引导蒸馏通过成对预测迁移条件更新。ForceDelta-VLA 蒸馏动作修正，并保留生成式策略来预测参考动作。其学习式无力输入模式借鉴缺失模态表征的研究，区分不可用条件与具有物理意义的零值。

## 3. 方法

**图 2：修正蒸馏（训练第三阶段）。** (a) 冻结的视觉语言模型（VLM）在参考查询 $k$ 时生成 $E_k$。教师查询复用该上下文，并共享 $S_t$ 和 $\epsilon_k$。教师预测与保存的参考动作定义两项修正目标，其中延迟目标包含参考状态对齐。(b) 学生使用由 $E_k$ 池化得到的上下文 $(\bar E_k,\bar m_k)$。$\Phi$ 的两次计算共享权重，并在各自输入中加入同一个可学习查询 token $q_{\mathrm{corr}}$；仅延迟计算屏蔽力 token。每个输出头读取对应计算在 $q_{\mathrm{corr}}$ 位置的输出。教师目标通过 $\mathcal L_{\mathrm{distill}}$ 监督学生的两项输出。

### 3.1 总览与符号

ForceDelta-VLA 将参考动作生成与接触修正分开（图 2）。慢速生成式通路在不使用实测力输入的情况下预测动作块。我们将其称为参考动作：此前预测、供快速通路施加力修正和延迟修正的运动。动作样本、视觉—语言上下文及查询状态被保存以供复用。轻量通路读取该缓存、当前机器人状态与近期 wrench 历史，预测力修正和延迟修正。执行器将两项修正与参考动作组合。

训练分三阶段：首先训练时序力条件 VLA 教师；然后冻结教师，学习提供参考动作的无力输入模式；最后固定两种模式，通过成对预测定义修正目标，并用采样得到的异步调度提供缓存输入以训练修正策略。

离线数据集 $\mathcal D$ 包含遥操作轨迹。时刻 $t$ 的记录包括视觉观测 $V_t$、语言指令 $L$、机器人状态 $S_t$、示范动作 $A_t^*$ 和因果 wrench 历史：

$$
\mathcal F_t=\{(s,\mathbf w(s))\mid t-T_F\leq s\leq t\}.
$$

$T_F$ 是历史窗口长度，$\mathbf w(s)=[\mathbf f(s);\boldsymbol\tau(s)]\in\mathbb R^6$ 为每臂末端外部 wrench。位姿动作与 wrench 使用对应机器人基坐标系。力条件教师 $T_\theta$ 将 $(V_t,L,S_t,\mathcal F_t)$ 映射为相对于 $S_t$ 的 $H$ 步动作块。以下位姿动作与修正使用其归一化位姿动作坐标，差值按动作标准差缩放。

令 $\mathcal P$ 提取动作位姿分量，则命令位姿分解为参考动作及两个学习式修正：

$$
\mathcal P(A^{\mathrm{cmd}})=\mathcal P(A^{\mathrm{ref}})
+\Delta\hat A^{\mathrm{force}}+\Delta\hat A^{\mathrm{delay}}.
$$

力修正学习成对教师预测之差。延迟修正补偿此前与当前参考预测之间的差异，并包含两者参考状态不同造成的补偿。两项仅修改位姿，夹爪指令沿用参考动作。

位姿坐标包括平移和连续六维旋转表示。在这些坐标中添加修正后，通过 Gram–Schmidt 投影得到合法旋转。双臂任务拼接每臂输入与位姿动作，联合预测。表示和归一化细节见补充材料。

### 3.2 教师与无力输入参考策略

#### 时序力条件教师

以 ForceVLA 为基础，将瞬时力嵌入替换为 FAVLA 风格的因果时序卷积网络（TCN），编码 wrench 历史：

$$z_t^F=E_T^F(\mathcal F_t).$$

在 $\mathcal D$ 上训练教师，预测

$$A_{T,t,1:H}^{\mathrm{cond}}=T_\theta(V_t,L,S_t,z_t^F;\epsilon),$$

其中 $\epsilon$ 为初始 flow noise。训练结束后冻结 $\theta$。

#### 无力输入参考动作模式

不可用的 wrench 历史与物理测量得到的零 wrench 不同。学习式无力输入模式表示前者：使用学习 token $z_{\varnothing F}$ 和秩为 $\rho$ 的 adapter $(W_{\mathrm{in}},W_{\mathrm{out}})$ 替代 wrench 历史。可训练参数为 $\phi=\{z_{\varnothing F},W_{\mathrm{in}},W_{\mathrm{out}}\}$。Adapter 修改动作专家的位姿通道：

$$v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h),$$

$h$ 是动作专家隐状态，$v^p$ 是位姿通道的流输出，$\sigma$ 为逐点非线性函数。Adapter 仅在无力输入模式启用。固定 $\theta$，按教师原有 flow-matching 目标训练 $\phi$，学习无力输入参考策略。参考动作预测为

$$A_{T,t,1:H}^{\mathrm{ref}}=T_{\theta,\phi}^{\mathrm{ref}}(V_t,L,S_t,z_{\varnothing F};\epsilon).$$

### 3.3 成对修正目标

目标构造使用三个预测。保存的参考动作是在此前查询状态 $S_k$ 下预测的，执行时复用该动作。当前无力输入预测在 $S_t$ 下计算，不使用实测力输入。当前力条件预测在同一个 $S_t$ 下计算，并使用近期力历史。后两项预测仅用于构造训练目标，均不会在修正执行过程中替换已保存的参考动作。

令 $t$ 为修正时刻，$k$ 为采样调度下此时可用的最新参考动作查询。查询 $k$ 在 $t_k$ 从状态 $S_k$ 启动，使用 flow noise $\epsilon_k$；$E_k$ 为从 $(V_k,L)$ 得到的视觉—语言前缀。其无力输入动作块定义带时间戳的参考动作 $A_k^{\mathrm{ref}}=A_{T,k}^{\mathrm{ref}}$。为匹配部署修正通路的信息，目标构造复用 $E_k$，将机器人状态与力历史更新到时刻 $t$：

$$
\begin{aligned}
A_{T,t|k,1:K}^{\mathrm{cond}}&=T_\theta(E_k,S_t,z_t^F;\epsilon_k)_{1:K},\\
A_{T,t|k,1:K}^{\mathrm{ref}}&=T_{\theta,\phi}^{\mathrm{ref}}(E_k,S_t,z_{\varnothing F};\epsilon_k)_{1:K}.
\end{aligned}
$$

$T(E_k,\cdot)$ 复用缓存前缀，不编码当前图像。参考动作及两次当前教师查询共享 $E_k$ 与 $\epsilon_k$。对 $j=1,\ldots,K\leq H$，力修正目标为

$$
\Delta A_{T,t|k,j}^{\mathrm{force}}=
\mathcal P(A_{T,t|k,j}^{\mathrm{cond}})-\mathcal P(A_{T,t|k,j}^{\mathrm{ref}}).
$$

该目标监督力修正输出头。

力目标比较当前状态下的两个预测，而执行时复用的是此前生成的参考动作。延迟目标补偿剩余的预测差异和参考状态变化。对时间为 $t_j=t+(j-1)/f_A$ 的 $K$ 个修正样本（$f_A$ 为块内动作采样频率），延迟目标为

$$
\Delta A_{T,k\rightarrow t,j}^{\mathrm{delay}}=
\mathcal P(A_{T,t|k,j}^{\mathrm{ref}})-\mathcal P(A_k^{\mathrm{ref}}(t_j))+\Gamma(S_t,S_k).
$$

参考状态对齐将当前预测表示为相对于已有参考动作生成时状态 $S_k$ 的动作。其中 $A_k^{\mathrm{ref}}(t_j)$ 为在 $t_j$ 处插值得到的已有参考动作；$\Gamma(S_t,S_k)$ 将参考状态之差转换为归一化动作坐标。延迟输出头学习包含 $\Gamma$ 的完整目标。

具体计算时，令 $\widetilde S^p$ 为归一化位姿坐标（每臂九维），$\sigma_S^p$、$\sigma_A^p$ 为训练集相应标准差，则

$$
\Gamma(S_t,S_k)=\frac{\sigma_S^p+\varepsilon}{\sigma_A^p+\varepsilon}
\odot(\widetilde S_t^p-\widetilde S_k^p),\qquad\varepsilon=10^{-6}.
$$

令 $\mathcal R(S,a)$ 将以 $S$ 为基准的相对动作 $a$ 转换为绝对机器人命令。两项目标逐步对齐坐标基准：

$$
\begin{aligned}
&\mathcal R\left(S_k,\mathcal P(A_k^{\mathrm{ref}}(t_j))+
\Delta A_{T,t|k,j}^{\mathrm{force}}+\Delta A_{T,k\rightarrow t,j}^{\mathrm{delay}}\right)\\
&\qquad\approx\mathcal R\left(S_t,\mathcal P(A_{T,t|k,j}^{\mathrm{cond}})\right).
\end{aligned}
$$

限幅前，代入目标恢复相对于 $S_k$ 表达的力条件动作块。状态与动作在逐分量差值构造前都转换为相同的平移加六维旋转表示，因此除 $\varepsilon$ 外，对齐恒等式在该表示中精确成立；恢复动作差值后再执行 Gram–Schmidt 投影。

### 3.4 修正策略与训练

#### 输入与预测

每次参考动作查询保存动作块 $A_k^{\mathrm{ref}}$、查询状态 $S_k$，以及带有效性掩码的视觉—语言前缀池化表示 $(\bar E_k,\bar m_k)$。学习式投影器将池化上下文转换为任务上下文 token $Z_k^{\mathrm{ctx}}$。时间戳插值得到与修正输出对齐的参考动作片段 $A_k^{\mathrm{ref}}(t_{1:K})$。因果力编码器将 $\mathcal F_t$ 映射为 $u_t^F$；时序特征 $\xi_{t,k}$ 编码缓存年龄与插值相位。条件集合为

$$
\mathcal C_t=[P_Z(Z_k^{\mathrm{ctx}});u_t^F;P_S(S_t);
P_A(\operatorname{vec}(A_k^{\mathrm{ref}}(t_{1:K})));P_\xi(\xi_{t,k})].
$$

投影将输入映射到共享 token 维度，学习式类型嵌入标识各条件。$P_A$ 将展平的 $K$ 步参考动作片段映射为一个 token。延迟输出使用屏蔽力 token 后的 $\mathcal C_t^{\mathrm{maskF}}=\operatorname{Mask}_F(\mathcal C_t)$。池化、投影与时序特征细节见补充材料。

共享注意力模块 $\Phi$ 通过学习查询 $q_{\mathrm{corr}}$ 聚合条件 token。使用同一套参数进行两次前向计算：力修正使用全部条件，延迟修正屏蔽力 token。独立输出头分别预测两项 $K$ 步修正：

$$
\begin{aligned}
\Delta\hat A^{\mathrm{force}}_{t,1:K}&=W_{\mathrm{force}}\Phi([\mathcal C_t;q_{\mathrm{corr}}])[q_{\mathrm{corr}}],\\
\Delta\hat A^{\mathrm{delay}}_{t,1:K}&=W_{\mathrm{delay}}\Phi([\mathcal C_t^{\mathrm{maskF}};q_{\mathrm{corr}}])[q_{\mathrm{corr}}].
\end{aligned}
$$

掩码防止延迟修正输出直接依赖力 token。两个输出来自共享模块的两次独立前向计算。

#### 异步调度重放

训练需要使策略接触到在结果可用时已经陈旧的参考动作。因此，在提取目标前采样查询周期与推理延迟，在策略优化期间重放同一固定调度。查询时间戳加入抖动，以覆盖非格点插值。训练时刻沿用记录的状态—动作时间戳，$\mathcal F_t$ 保留因果窗口中的全部 wrench 样本。具体采样分布见补充材料。令 $t_i$、$\bar t_i=t_i+L_i$ 分别为查询启动及可用时刻，当前参考动作索引为

$$k(t)=\arg\max_{i:\bar t_i\leq t}t_i.$$

当前更新提供缓存池化上下文、对齐参考动作片段及用于计算时序特征和逐步目标的时间戳。调度决定优化中覆盖的参考动作年龄、插值相位和查询延迟。

采用归一化时序权重 $w_j\propto\beta^{j-1}$（$0<\beta\leq1$）以及归一化坐标均方损失 $\ell_{\mathrm{pose}}$：

$$
\mathcal L_{\mathrm{distill}}=\sum_{j=1}^K w_j\left[
\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{force}}_{t,j},\Delta A^{\mathrm{force}}_{T,t|k,j})+
\lambda_{\mathrm{delay}}\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{delay}}_{t,j},\Delta A^{\mathrm{delay}}_{T,k\rightarrow t,j})\right].
$$

$\lambda_{\mathrm{delay}}$ 平衡力条件与延迟修正监督。

### 3.5 异步部署

部署时，教师与学生独立运行（图 3）。教师以无力输入模式定期更新缓存参考动作。每次学生查询读取最新完成的参考动作更新，并在修正执行期间始终保持参考动作与修正的配对。教师计算下一段参考动作时，学生继续发起查询；新参考可用后启动的查询可以采用该参考。

每个控制周期，执行器从结果已完成且仍然有效的学生查询中，选择启动时间最新的一次。对当前有效步 $j$，在 $t_j$ 处插值得到参考位姿，分别对两项修正限幅，再按式（1）组合归一化位姿。随后，使用所选参考动作的查询状态 $S_k$ 将结果转换为绝对命令。过期步骤被跳过；有效修正步保持到下一个时间戳或更新的有效结果可用时。时间戳选择规则见补充材料。

**图 3：部署示意。** 连续两次学生查询复用同一参考动作块。选中的修正步与对应时间戳处的参考位姿组合。

## 4. 实验

### 4.1 实验设置

**硬件、任务与数据采集。** 两个平台包含五项单臂和四项双臂任务（图 4、图 5）。单臂平台为 7 自由度 Franka Emika Panda，配备腕部和外部 Intel RealSense D405 相机，使用 3Dconnexion SpaceMouse 采集示范。双臂平台为 X Square Robot，每臂配备腕部相机，另有共享外部相机，通过主从遥操作采集示范。与 ForceVLA 一致，两平台均使用基于机器人关节力矩的 wrench 估计，不额外安装腕部力/力矩传感器。

每任务采集 200 条示范。多视角图像、状态与命令动作以 30 Hz 记录，带时间戳的 wrench 估计以 100 Hz 记录。

**基线与评估协议。** 对比不使用力/力矩输入的 $\pi_{0.5}$、力感知完整动作策略 ForceVLA、力矩感知 VLA TA-VLA，以及慢—快视觉—力策略 ImplicitRDP。ForceVLA 也是教师骨干。额外直接执行第一阶段 Temporal Teacher，作为架构受控对照。各方法使用相同任务示范、初始状态分布、控制器设置与安全限幅。每方法每任务评估 20 次，报告成功率、峰值接触力与完成时间。

**实现细节。** 教师编码 100 ms wrench 历史，预测 $H=50$、采样频率 30 Hz 的动作块。修正策略使用因果力编码器、共享注意力模块及独立力/延迟输出头，预测 $K=5$ 个位姿修正。部署时，两条通路在各自的 NVIDIA RTX 4090 GPU 上并发运行，参考动作以约 6 Hz 更新。机器人控制指令的发送频率限制为 100 Hz，该控制频率不等同于修正查询频率。训练与优化细节见补充材料。

### 4.2 主要结果

**定量结果。** ForceDelta-VLA 平均成功率为 82.2%，ForceVLA 为 54.4%，Temporal Teacher 为 70.6%（表 I）。直接执行教师提供架构受控参照，用于考察修正框架超出时序力编码器本身的收益。提升覆盖九项任务，相对 ForceVLA 在 USB 插入、按按钮和插头插入上最大。相对 Temporal Teacher，两项插入任务分别提升 25 和 20 个百分点；开柜门、按按钮、单臂擦白板和拔插头均提升 5 个百分点。因此，本次评估中修正框架对两项插入任务的额外收益最大；按按钮相对 ForceVLA 的提升主要已由 Temporal Teacher 获得。

**表 I：每种方法每项任务 20 次试验的成功率（%）；平均值为九任务均值。**

| 方法 | 翻物体 | 插 USB | 开柜门 | 按按钮 | 单臂擦白板 | 拔插头 | 插插头 | 拉抽屉 | 双臂擦白板 | 平均值 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| $\pi_{0.5}$ | 55 | 35 | 50 | 45 | 70 | 40 | 30 | 50 | 50 | 47.2 |
| ForceVLA | 65 | 40 | 60 | 50 | 70 | 50 | 35 | 60 | 60 | 54.4 |
| TA-VLA | 65 | 35 | 60 | 60 | 75 | 55 | 40 | 60 | 65 | 57.2 |
| ImplicitRDP | 50 | 25 | 45 | 35 | 65 | 55 | 30 | 55 | 45 | 45.0 |
| Temporal Teacher | 75 | 55 | 75 | 80 | 85 | 75 | 50 | 70 | 70 | 70.6 |
| **ForceDelta-VLA (Ours)** | **90** | **80** | **80** | **85** | **90** | **80** | **70** | **80** | **85** | **82.2** |

**接触力与完成时间。** 相较 ForceVLA，单臂和双臂平台汇集成功试验的平均峰值力分别减少 4.2 N 和 4.3 N，降幅均约 26%（表 II）；完成时间分别缩短 5.2 s 和 5.9 s。ImplicitRDP 的这两项条件指标也优于 ForceVLA，但成功率为 45.0%。ForceDelta-VLA 相对 ImplicitRDP 的成功率优势为 37.2 个百分点，同时降低峰值力与完成时间。

**表 II：各平台内跨任务汇集成功试验的峰值力与完成时间，均值 ± 标准差。**

| 方法 | 单臂峰值力（N）↓ | 双臂峰值力（N）↓ | 单臂完成时间（s）↓ | 双臂完成时间（s）↓ |
|---|---:|---:|---:|---:|
| $\pi_{0.5}$ | 18.9 $\pm$ 8.3 | 20.4 $\pm$ 9.1 | 30.4 $\pm$ 11.2 | 33.1 $\pm$ 12.8 |
| ForceVLA | 16.0 $\pm$ 4.7 | 16.8 $\pm$ 6.2 | 29.1 $\pm$ 6.5 | 31.7 $\pm$ 9.4 |
| TA-VLA | 16.5 $\pm$ 6.4 | 15.9 $\pm$ 4.8 | 28.2 $\pm$ 9.7 | 30.0 $\pm$ 7.1 |
| ImplicitRDP | 15.2 $\pm$ 5.1 | 14.8 $\pm$ 4.9 | 27.1 $\pm$ 7.2 | 27.4 $\pm$ 7.8 |
| Temporal Teacher | 15.1 $\pm$ 5.8 | 14.7 $\pm$ 4.3 | 27.8 $\pm$ 8.8 | 29.2 $\pm$ 6.9 |
| **ForceDelta-VLA (Ours)** | $\mathbf{11.8}\pm 3.4$ | $\mathbf{12.5}\pm 2.9$ | $\mathbf{23.9}\pm 5.4$ | $\mathbf{25.8}\pm 7.6$ |

**定性结果。** 图 6 展示按按钮轨迹中的平移修正变化：持续接触前较小，按压时增大，峰值接触附近出现瞬态，释放时减小。图中展示四个交互阶段，曲线为机器人估计的末端力分量及平移修正幅值；竖线标记三次接触转换，较早的自由空间图像位于横轴断点前。

### 4.3 运行耗时与参考动作延迟

测量参考动作与修正前向耗时，并评估额外参考动作延迟下的性能。

**推理耗时分离。** 修正前向耗时 2.43 ms，低于笛卡尔目标更新的 10 ms 间隔（表 III），两个生成式通路均超过该间隔。该计算耗时差异使修正 worker 能复用参考动作，无需每次修正都等待新生成式预测。

**表 III：按按钮部署负载下，工作站 batch-one 前向耗时，重复测量的均值 ± 标准差。**

| 方法/路径 | 前向延迟（ms）↓ |
|---|---:|
| ImplicitRDP | 12.86 $\pm$ 0.23 |
| Temporal Teacher | 176.4 $\pm$ 5.9 |
| ForceDelta reference action | 189.7 $\pm$ 6.8 |
| ForceDelta correction | **2.43** $\pm$ 0.12 |

**参考动作延迟敏感性。** 在 USB 插入和插头插入上增加 0、100、200 ms 参考动作延迟，每任务、方法和延迟条件测试 10 次。随着延迟增加，ForceDelta-VLA 成功率降幅及平均峰值力增幅小于 Temporal Teacher（图 7）。这一趋势与修正通路在延迟参考动作更新之间仍有用的解释一致。性能仍随参考动作年龄增加而下降，说明近期力反馈未消除对参考动作新鲜度的依赖。图 7 左为成功率，右为成功试验峰值力；蓝色实线圆点表示 ForceDelta-VLA，灰色虚线方块表示 Temporal Teacher。

### 4.4 消融实验

消融考察三个问题：如何定义修正目标，分别预测修正还是预测完整动作，以及训练如何适配异步执行（表 IV）。评估任务为 USB 插入、单臂擦白板、插头插入和拉抽屉。

**变体协议。** 执行消融保持训练模型不变：Reference action only 关闭两项修正；w/o force correction 关闭力输出；w/o learned delay correction 关闭完整延迟输出，包括状态参考补偿；Zeroed correction input 提供全零力历史，同时保留力分支。其余变体重新训练受影响策略，具体改变随下文结果说明。

**表 IV：四项任务的消融，按平台分组；峰值力为均值 ± 标准差。**

| 变体 | 单臂成功率（%）↑ | 双臂成功率（%）↑ | 单臂峰值力（N）↓ | 双臂峰值力（N）↓ |
|---|---:|---:|---:|---:|
| *执行/组件消融* |  |  |  |  |
| Reference action only | 62.5 | 50.0 | 16.7 $\pm$ 6.9 | 18.2 $\pm$ 7.6 |
| w/o force correction | 70.0 | 57.5 | 15.4 $\pm$ 5.8 | 17.1 $\pm$ 6.3 |
| w/o learned delay correction | 77.5 | 67.5 | 13.2 $\pm$ 4.1 | 14.5 $\pm$ 5.0 |
| Zeroed correction input | 65.0 | 55.0 | 15.9 $\pm$ 6.4 | 17.7 $\pm$ 7.0 |
| **ForceDelta-VLA (full)** | **85.0** | **75.0** | $\mathbf{11.7}\pm 3.1$ | $\mathbf{12.8}\pm 3.8$ |
| *监督/分解消融* |  |  |  |  |
| Demonstration-derived correction target | 72.5 | 57.5 | 14.6 $\pm$ 5.2 | 16.5 $\pm$ 6.1 |
| Zero-wrench teacher query | 65.0 | 55.0 | 16.1 $\pm$ 6.7 | 17.3 $\pm$ 6.5 |
| w/o schedule replay | 77.5 | 62.5 | 13.8 $\pm$ 4.7 | 15.6 $\pm$ 5.8 |
| Single combined-correction head | 75.0 | 65.0 | 13.5 $\pm$ 4.3 | 14.9 $\pm$ 4.9 |
| Fast full-action distillation | 67.5 | 52.5 | 15.2 $\pm$ 5.6 | 17.9 $\pm$ 7.4 |
| **ForceDelta-VLA (full)** | **85.0** | **75.0** | $\mathbf{11.7}\pm 3.1$ | $\mathbf{12.8}\pm 3.8$ |

**修正监督应如何定义？** Demonstration-derived correction target 将力目标替换为同一查询状态下的示范动作减去无力输入教师预测。单臂/双臂成功率从 85.0%/75.0% 降至 72.5%/57.5%。本次评估支持使用成对教师偏差监督。Zero-wrench teacher query 用全零 wrench 查询同时构造参考动作和修正目标，成功率降至 65.0%/55.0%；该变体评估的是两项构造替换的共同影响。

**快速策略应预测什么？** 关闭力修正后，两平台子集成功率分别降低 15 和 17.5 个百分点，峰值力增加 3.7 N 和 4.3 N。输入置零比关闭输出更差，因为力分支仍施加预测。Single combined-correction head 用一个 head 预测两目标之和，成功率降至 75.0%/65.0%。Fast full-action distillation 在相同小型学生架构、输入与更新调度下直接蒸馏教师力条件位姿动作，成功率分别落后修正预测 17.5 和 22.5 个百分点，支持该学生围绕参考动作预测修正。

**异步适配有何作用？** 关闭完整学习式延迟输出使两子集成功率均下降 7.5 个百分点。w/o schedule replay 在缓存上下文目标提取中省略采样异步调度，成功率降至 77.5%/62.5%。结果支持补偿参考动作差异，以及训练时覆盖异步条件。

### 4.5 未见物体泛化

对图 8 的任务，分别将训练物体替换为未见 USB 端口、纸盒，以及白板和板擦。图中从左至右为 USB 插入、翻物体和双臂擦白板。

**表 V：未见物体泛化结果，峰值力为均值 ± 标准差。**

| 方法 | 成功率（%）↑ | 峰值力（N）↓ |
|---|---:|---:|
| ImplicitRDP | 30.0 | 45.9 ± 10.5 |
| Temporal Teacher | 70.0 | 21.8 ± 5.2 |
| **ForceDelta-VLA (Ours)** | 80.0 | 13.6 ± 3.7 |

物体替换后，ForceDelta-VLA 成功率比 Temporal Teacher 高 10 个百分点，成功试验峰值力由 21.8 N 降至 13.6 N（表 V）。修正框架收益在未见物体上仍然存在。

### 4.6 局限性

失败轨迹呈现三类局限：指向可行接触区域之外的参考动作可能使轨迹超出有界修正范围；参考动作年龄超出训练调度时，缓存局部计划可能与持续变化的机器人—环境状态不一致；卡滞或错误侧接触可能需要回退、重新接近或策略切换等非局部恢复，超出有界局部修正范围。

## 5. 结论与未来工作

本文提出面向接触密集型操作的修正蒸馏框架 ForceDelta-VLA。成对的力条件与无力输入教师预测定义力修正目标，延迟修正目标补偿参考动作差异及参考状态对齐。修正策略通过异步调度重放训练，在参考动作更新之间更新位姿修正。在九项真实机器人任务上，ForceDelta-VLA 的平均成功率为 82.2%，ForceVLA 和 Temporal Teacher 分别为 54.4% 和 70.6%。两个平台的成功试验平均峰值接触力均比 ForceVLA 低约 26%。

方法依赖局部有效的任务上下文，以及能够在修正限幅内调整的参考动作。需要改变接近方式或执行恢复运动的交互可能超出这一范围。未来将研究适用于这些情况的参考动作刷新与恢复策略。
