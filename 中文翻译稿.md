# ForceDelta-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力感知视觉—语言—动作（VLA）策略改善了接触密集型操作，但通常仍在同一个生成式动作预测中耦合任务层运动与接触相关调整。离线示范没有提供将该预测分解为可复用 reference 和显式 correction 的标签。我们提出 ForceDelta-VLA，一种从成对完整力条件与学习式空力预测中构造力响应目标的残差蒸馏框架，无需任务奖励，也无需额外纠正或在线示范。另一残差在重放部署时序下补偿 reference 陈旧性。所得残差策略复用缓存任务上下文和带时间戳的空力 reference，同时利用近期力历史更新位姿修正，无需重新生成完整动作。在九项接触密集型单臂和双臂任务上，ForceDelta-VLA 达到 82.2% 的平均成功率；ForceVLA 和 fast--slow 视觉—力基线 ImplicitRDP 分别为 54.4% 和 72.8%。相较 ImplicitRDP，两个平台上的峰值接触力均降低约 19%。


## 1. 引言

力感知视觉—语言—动作（VLA）策略通过以交互反馈为条件生成动作，提高了接触密集型操作性能。然而，这类策略通常在同一个生成式动作预测中同时表示任务层运动与接触相关调整。部署时，相邻控制周期可以复用任务层行为，而物理修正应随新观测到的接触状态及时变化。这种耦合使修正缺少显式监督目标和可独立执行的输出形式（见图 1）。

力响应加性残差在保留无显式力输入 reference 的任务层行为时，提供显式的位姿空间修正。离线示范无法唯一确定该分解，因为示范只包含执行动作，并未标注 reference 或 residual。直接使用动作监督可能把接触相关行为分配给 reference，或让 residual 重复任务层运动。

从示范动作中减去缓存参考会混合物理修正与参考陈旧性；零力查询则表示一种有效物理状态，而非缺失力条件。这些歧义促使我们利用预训练力感知策略及其已有离线数据构造显式修正目标。

现有异步策略分离计算或传感器更新频率。残差强化学习和纠正式模仿学习则通过任务奖励或额外在线交互获得修正信号。本文研究如何离线构造可独立于生成式动作推理执行的修正目标。

ForceDelta-VLA 学习空力 reference，从冻结 teacher 的预测中分别构造力修正与陈旧性修正目标，并将二者蒸馏为独立于生成式动作推理执行的残差策略。

我们在五项单臂和四项双臂真实机器人任务上评估 ForceDelta-VLA，任务涵盖接触建立、精密插入、摩擦主导运动和受约束操作。本方法的平均成功率为 82.2%，ForceVLA 和 ImplicitRDP 分别为 54.4% 和 72.8%；相较 ImplicitRDP，两个平台上的峰值接触力均降低约 19%。

本文贡献如下：

1. 提出**空力模式残差蒸馏**，利用冻结力感知 teacher 的匹配完整力条件与学习式空力预测构造显式力响应修正目标，无需任务奖励，也无需额外的纠正或在线示范；
2. 提出**参考状态一致的双残差分解**，分别监督力条件修正与缓存 reference 陈旧性，避免时序 reference 不匹配被吸收到力残差中；
3. 构建**可独立执行残差通路**，复用缓存任务上下文和空力 reference，并根据当前力反馈更新位姿修正。


## 2. 相关工作

### 2.1 力感知与多频率操作策略

力感知 VLA 通过多模态融合、提示、时序记忆或力感知优化整合力/力矩信息；力中心模仿学习则从示范中联合建模运动与交互力。这些方法改善了接触敏感预测，但通常仍在统一策略输出中共同表示任务运动和物理适应。

视觉运动方法学习异步 RGB 与力/力矩流，按交互阶段调整扩散推理频率，或引入快速修正与混合力—位置通路。VLA 方法也会复用缓存语义特征、通过纠正 rollout 训练反应式专家，或按传感器频率刷新模态专用潜在状态。这些设计改变物理反馈进入控制的时间或位置。本文关注可独立执行加性修正的离线监督。

### 2.2 残差策略学习

残差策略学习用学习得到的修正增强标称控制器，并已用于接触密集型操作。现有方法通过强化学习或在线人工纠正获得残差监督，其中也包括针对冻结视觉策略的触觉条件残差适配。其他模仿学习方法则直接规定加性修正结构。这些方法通过奖励、纠正式交互或预设分解定义 residual。ForceDelta-VLA 则从匹配的完整力条件与空力 teacher 预测中离线构造残差目标。

### 2.3 策略蒸馏

学习式缺失模态表征能够区分不可用条件与具有物理意义的零值，从而支持为力条件建立独立的空力运行点。近期机器人策略蒸馏方法将迭代扩散或流策略压缩为单步动作生成器，以实现更快的闭环控制。Guidance distillation 则利用成对预测迁移依赖模型的条件更新。本文保留生成式 VLA 作为 reference，并蒸馏其力偏差和另一项参考状态修正。

## 3. 方法

### 3.1 问题定义

我们考虑由遥操作轨迹组成的离线数据集 $\mathcal D$。在时刻 $t$，每条轨迹提供视觉观测 $V_t$、语言指令 $L$、机器人状态 $S_t$、示范动作 $A_t^*$，以及因果 wrench 历史

$$
\mathcal F_t=\{(s,\mathbf w(s))\mid t-T_F\leq s\leq t\},
$$

其中，$T_F$ 是历史窗口长度，$\mathbf w(s)=[\mathbf f(s);\boldsymbol\tau(s)]\in\mathbb R^6$ 是作用于末端的外部 wrench。位姿动作和 wrench 测量均使用对应的机器人基坐标系。力条件 teacher $T_\theta$ 将 $(V_t,L,S_t,\mathcal F_t)$ 映射为相对于 $S_t$ 的 $H$ 步动作块。下文的位姿动作与残差使用其归一化 pose-action 坐标，位姿差按动作通道标准差进行尺度归一化。

本文的目标是为力响应修正构造显式监督，并将其蒸馏为可以独立于生成式 teacher 计算的残差策略。令 $\mathcal P(\cdot)$ 提取动作中的位姿分量。我们采用参考状态一致的双残差分解，将命令位姿分解为一个 reference 与两个加性修正：

$$
\mathcal P(A^{\mathrm{cmd}})=\mathcal P(A^{\mathrm{ref}})
+\Delta\hat A^{\mathrm{force}}
+\Delta\hat A^{\mathrm{stale}},
$$

力条件项建模 teacher 的力条件偏差，陈旧性项补偿缓存 reference 所对应的较早机器人状态。两项修正都只作用于位姿；夹爪指令沿用 reference。

其中，位姿分量 $\mathcal P(A)=[p;r]$ 包含三维平移 $p$ 和连续六维旋转表示 $r$。平移直接相加；旋转修正在 teacher 的六维动作坐标中相加，再通过 Gram--Schmidt 正交化映射为合法旋转矩阵。

### 3.2 方法概述

训练分为图 2 所示的三个阶段。首先训练时序力条件 VLA teacher。随后冻结 teacher，并学习提供参考动作的空力模式。最后冻结两种模式，构造力修正和陈旧性修正目标，并训练残差策略。

### 3.3 力条件教师与空力参考

#### 时序力条件教师

教师基于 ForceVLA，将其瞬时力嵌入替换为因果时序编码：

$$
z_t^F=E_T^F(\mathcal F_t).
$$

教师在 $\mathcal D$ 上训练并预测

$$
A_{T,t,1:H}^{\mathrm{full}}=T_\theta(V_t,L,S_t,z_t^F;\epsilon).
$$

其中 $\epsilon$ 为初始流噪声。训练完成后冻结 $\theta$。

#### 空力参考模式

零力是有效物理测量，不能表示力历史缺失。我们引入学习式空力 token $z_{\varnothing F}$ 和 rank-$\rho$ 适配器 $\phi=\{z_{\varnothing F},W_{\mathrm{in}},W_{\mathrm{out}}\}$。适配器仅修改动作专家的位姿通道：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h),
$$

其中，$h$ 是 action expert 的隐藏状态，$v^p$ 是其位姿通道的 flow 输出，$\sigma$ 是逐点非线性函数。该 adapter 只在空力模式下启用。保持 $\theta$ 固定，在 teacher 原有的 flow-matching 目标下优化 $\phi$，由此定义 teacher 的无显式力输入运行点。

所得参考预测为

$$
A_{T,t,1:H}^{\mathrm{null}}
=T_{\theta,\phi}^{\mathrm{null}}(V_t,L,S_t,z_{\varnothing F};\epsilon).
$$

### 3.4 参考状态一致的双残差蒸馏

#### 修正目标

对于相同的 $(V_t,L,S_t)$，完整力条件查询与空力查询复用视觉—语言前缀和初始噪声 $\epsilon$。对于 $j=1,\ldots,K\leq H$，由 teacher 定义的力目标为

$$
\Delta A_{T,t,j}^{\mathrm{force}}
=\mathcal P\!\left(A_{T,t,j}^{\mathrm{full}}\right)
-\mathcal P\!\left(A_{T,t,j}^{\mathrm{null}}\right).
$$

该目标用于监督力残差 head。

对于 schedule replay 样本 $(t,k)$，residual 查询预测在 $t_j=t+(j-1)/f_A$ 执行的 $K$ 个步骤，其中 $f_A$ 是动作 waypoint 频率。缓存查询 $k$ 从状态 $S_k$ 发起，并提供按时间对齐的空力 reference $A_k^{\mathrm{ref}}(\cdot)$。令 $\widetilde S^p$ 表示归一化后的九维位姿坐标，$\sigma_S^p$ 和 $\sigma_A^p$ 表示训练集上相应通道的标准差。缓存状态差按下式转换到归一化 action 坐标：

$$
\Gamma(S_t,S_k)
=\frac{\sigma_S^p+\varepsilon}{\sigma_A^p+\varepsilon}
\odot(\widetilde S_t^p-\widetilde S_k^p),
\qquad \varepsilon=10^{-6}.
$$

陈旧性目标为

$$
\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}
=\mathcal P\!\left(A_{T,t,j}^{\mathrm{null}}\right)
-\mathcal P\!\left(A_k^{\mathrm{ref}}(t_j)\right)
+\Gamma(S_t,S_k).
$$

令 $\mathcal R(S,a)$ 表示将以状态 $S$ 为基准的相对动作 $a$ 转换为绝对机器人命令。两项目标在每个预测步上对齐两个坐标基准：

$$
\begin{aligned}
&\mathcal R\!\left(S_k,\mathcal P(A_k^{\mathrm{ref}}(t_j))
+\Delta A_{T,t,j}^{\mathrm{force}}
+\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}\right)\\
&\qquad\approx\mathcal R\!\left(S_t,
\mathcal P(A_{T,t,j}^{\mathrm{full}})\right).
\end{aligned}
$$

#### 残差策略

每个 reference 数据包包含由视觉—语言前缀 $E_k$ 及其 mask $m_k$ 池化得到的 $M$ 个 intent token，即 $Z_k^{\mathrm{intent}}=\operatorname{Pool}_M(E_k,m_k)$。对于时刻 $t$ 发起的 residual 查询，通过时间戳插值得到 reference sample $A_k^{\mathrm{ref}}(t)$。因果编码器将 $\mathcal F_t$ 映射为 $u_t^F$，已知的重基准项 $\Gamma(S_t,S_k)$ 直接提供给 student。缓存年龄和 reference 插值位置表示为

$$
\xi_{t,k}=\left[\min\!\left(\frac{t-t_k}{c},1\right);\alpha_{t,k}\right].
$$

残差策略采用有序条件序列

$$
\mathcal C_t=[P_Z(Z_k^{\mathrm{intent}});u_t^F;P_S(S_t);
P_A(A_k^{\mathrm{ref}}(t));P_\Gamma(\Gamma(S_t,S_k));P_\xi(\xi_{t,k})].
$$

其中，$P_Z,P_A,P_S,P_\Gamma$ 和 $P_\xi$ 将各自输入映射到共享 token 维度。令 $\mathcal C_t^{\mathrm{maskF}}=\operatorname{Mask}_F(\mathcal C_t)$ 表示保持序列结构不变、但屏蔽力 token 后的条件序列。

共享注意力模块 $\Phi$ 和学习式查询 $q_{\mathrm{res}}$ 预测两个长度为 $K$ 的修正块：

$$
\Delta\hat A^{\mathrm{force}}_{t,1:K}
=W_{\mathrm{force}}\Phi([\mathcal C_t;q_{\mathrm{res}}])[q_{\mathrm{res}}],
$$

$$
\Delta\hat A^{\mathrm{stale}}_{t,1:K}
=W_{\mathrm{stale}}
\Phi([\mathcal C_t^{\mathrm{maskF}};q_{\mathrm{res}}])[q_{\mathrm{res}}].
$$

该 mask 避免陈旧性输出直接依赖实测力。两次计算共享 $\Phi$，并使用独立的输出投影。

#### 陈旧性感知时序重放

在离线残差策略训练中，我们通过采样 query 间隔和 packet latency 重放异步执行。训练时刻沿用数据中记录的 state--action 时间戳，而 $\mathcal F_t$ 保留因果窗口内的全部 wrench 样本。令 $t_i$ 和 $\bar t_i$ 分别表示查询 $i$ 的发起和发布时刻，则当前有效查询为

$$
k(t)=\arg\max_{i:\bar t_i\leq t}t_i.
$$

所选查询提供缓存 intent、当前 reference sample、时序特征和 $\Gamma(S_t,S_k)$。其带时间戳的动作块用于构造逐步陈旧性目标。重新采样调度使 reference age、插值相位和 packet latency 发生变化。

采用归一化时序权重 $w_j\propto\beta^{j-1}$（$0<\beta\leq1$）和归一化坐标均方损失 $\ell_{\mathrm{pose}}$，蒸馏目标为

$$
\mathcal L_{\mathrm{distill}}=\sum_{j=1}^{K}w_j\left[
\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{force}}_{t,j},\Delta A^{\mathrm{force}}_{T,t,j})
+\lambda_{\mathrm{stale}}\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{stale}}_{t,j},\Delta A^{\mathrm{stale}}_{T,k\rightarrow t,j})
\right].
$$

$\lambda_{\mathrm{stale}}$ 用于平衡力条件监督与陈旧性监督。

### 3.5 独立残差执行

Reference worker 与 residual worker 相互独立运行。reference 查询完成后，worker 发布带时间戳的空力动作块、缓存 intent 和查询状态。每个 residual 查询读取当前有效的 reference 数据包以及当前状态和因果 wrench 历史。其带时间戳的 $K$ 步修正块始终与该 reference 数据包绑定，因此新发布的 reference 只供随后发起的 residual 查询使用。

执行时跳过已经过期的修正步骤，其余步骤不经插值而依次执行。reference 位姿通过时间戳插值得到。力条件修正与陈旧性修正分别限幅，在归一化 delta-action 坐标中完成组合，再根据 reference 查询状态转换为绝对机器人指令。

## 4. 实验

### 4.1 实验设置

#### 硬件、任务与数据采集

实验包括两个平台上的 5 项单臂任务和 4 项双臂任务（见任务表）。单臂平台采用七自由度 Franka Emika Panda，配备腕部和外部 Intel RealSense D405 相机。示范通过 3Dconnexion SpaceMouse 遥操作采集。双臂实验采用 X Square Robot 平台，每条机械臂配备一个腕部相机，并设置一个共享外部相机。示范通过 Leader--Follower 遥操作采集。与 ForceVLA [1] 一致，两个平台均使用机器人自身提供的 wrench 估计，不额外安装腕部力/力矩传感器。

每项任务均采集 200 条示范。多视角图像、机器人状态和命令动作以 30 Hz 记录，带时间戳的 wrench 估计以 100 Hz 记录。

| 平台 | 任务 | 交互形式 |
|---|---|---|
| 单臂 | 翻物体（Object Flipping） | 借助表面接触完成重定向 |
| 单臂 | 插 USB（USB Insertion） | 精密插入 |
| 单臂 | 开柜门（Cabinet Opening） | 摩擦主导的关节约束运动 |
| 单臂 | 按按钮（Button Pressing） | 离散接触驱动 |
| 单臂 | 擦白板（Whiteboard Wiping） | 持续表面接触 |
| 双臂 | 拔插头（Plug Removal） | 双臂协调的受约束拔出 |
| 双臂 | 插插头（Plug Insertion） | 双臂协调对准与插入 |
| 双臂 | 拉抽屉（Drawer Opening） | 双臂协调的摩擦主导运动 |
| 双臂 | 擦白板（Whiteboard Wiping） | 固定白板并执行擦除 |

#### 基线与评估协议

本文比较 4 个外部基线：$\pi_{0.5}$、ForceVLA、TA-VLA 和 ImplicitRDP。Temporal Teacher 直接 rollout 第一阶段 teacher，构成残差蒸馏的架构匹配对照。每种方法在每项任务上评估 2 个独立训练 seed，每个 seed 测试 10 次。指标包括成功率、峰值接触力和完成时间。单臂峰值力按全部试验中的 $\max_t\|\mathbf f(t)\|_2$ 计算，双臂峰值力按 $\max_{t,a\in\{L,R\}}\|\mathbf f_a(t)\|_2$ 计算。完成时间仅基于成功试验统计。

#### 实现细节

两个平台均通过带比例—微分反馈的笛卡尔阻抗控制器执行策略指令。出于安全考虑，两个平台的笛卡尔指令接口均限制在最高 100 Hz。时序力条件 teacher 使用 8 张 NVIDIA A100 GPU 进行全参数微调。所有真实机器人推理和 runtime 测量均使用一张 48 GB NVIDIA RTX 4090D GPU。

### 4.2 主要结果

#### 定性结果

<span style="color:red">代表性 rollout 可视化将任务观测与力曲线、residual magnitude 曲线对齐，并标出接触开始时刻。在这些 rollout 中，蒸馏得到的力残差在自由空间运动阶段保持较小，并在接触开始及持续交互阶段增大，这与其作为局部接触修正而非第二个完整任务策略的作用一致。</span>

#### 任务性能

ForceDelta-VLA 的平均成功率为 82.2%（见表 II）。$\pi_{0.5}$、ForceVLA 和 TA-VLA 分别达到 47.2%、54.4% 和 57.2%。ImplicitRDP 达到 72.8%，架构匹配的 Temporal Teacher 达到 68.9%。相较 ImplicitRDP，提升最大的任务是 USB 插入（80% 对 55%）、插插头（70% 对 50%）和拔插头（80% 对 65%）。ForceDelta-VLA 与 ImplicitRDP 在翻物体（90%）、按按钮（85%）和单臂擦白板（90%）上的成功率相同。

时序力条件将 ForceVLA 的平均成功率从 54.4% 提升到 68.9%，ForceDelta-VLA 进一步达到 82.2%。相较 Temporal Teacher，ForceDelta-VLA 将单臂和双臂完成时间分别缩短 19.2% 和 21.7%。其完成时间在两个平台上也均略低于 ImplicitRDP。

| 方法 | 翻物体 | 插 USB | 开柜门 | 按按钮 | 单臂擦白板 | 拔插头 | 插插头 | 拉抽屉 | 双臂擦白板 | 平均值 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| $\pi_{0.5}$ | 55 | 35 | 50 | 45 | 70 | 40 | 30 | 50 | 50 | 47.2 |
| ForceVLA | 65 | 40 | 60 | 50 | 70 | 50 | 35 | 60 | 60 | 54.4 |
| TA-VLA | 65 | 35 | 60 | 60 | 75 | 55 | 40 | 60 | 65 | 57.2 |
| ImplicitRDP | **90** | 55 | 70 | **85** | **90** | 65 | 50 | 75 | 75 | 72.8 |
| Temporal Teacher | 75 | 55 | 70 | 80 | 85 | 65 | 50 | 70 | 70 | 68.9 |
| **ForceDelta-VLA (Ours)** | **90** | **80** | **80** | **85** | **90** | **80** | **70** | **80** | **85** | **82.2** |

#### 接触质量

ForceDelta-VLA 在两个平台的所有方法中均取得最低的峰值接触力（见表 III）。相较 Temporal Teacher，单臂任务的峰值力从 15.3 N 降至 11.6 N，双臂任务从 16.1 N 降至 12.3 N，分别降低 24.2% 和 23.6%。相较 ImplicitRDP，两个平台上的峰值力均降低约 19%。其余 VLA 基线均表现出更高的峰值力。

**表 III：峰值力与完成时间。** 峰值力使用全部试验，完成时间使用成功试验；数值为均值 $\pm$ 标准差。

| 方法 | 单臂峰值力 ↓ | 双臂峰值力 ↓ | 单臂完成时间 ↓ | 双臂完成时间 ↓ |
|---|---:|---:|---:|---:|
| $\pi_{0.5}$ | 18.6 $\pm$ 6.1 | 19.4 $\pm$ 6.7 | 31.5 $\pm$ 10.2 | 46.0 $\pm$ 9.8 |
| ForceVLA | 16.9 $\pm$ 5.4 | 17.8 $\pm$ 5.8 | 30.0 $\pm$ 9.3 | 43.5 $\pm$ 9.0 |
| TA-VLA | 16.1 $\pm$ 5.0 | 17.0 $\pm$ 5.4 | 28.4 $\pm$ 8.5 | 40.8 $\pm$ 8.2 |
| ImplicitRDP | 14.4 $\pm$ 4.2 | 15.2 $\pm$ 4.6 | 24.5 $\pm$ 7.0 | 34.5 $\pm$ 7.0 |
| Temporal Teacher | 15.3 $\pm$ 4.7 | 16.1 $\pm$ 5.0 | 28.7 $\pm$ 8.7 | 41.5 $\pm$ 8.5 |
| **ForceDelta-VLA (Ours)** | $\mathbf{11.6}\pm 3.2$ | $\mathbf{12.3}\pm 3.6$ | $\mathbf{23.2}\pm 6.5$ | $\mathbf{32.5}\pm 6.5$ |

#### 失败分析

在表 II 所含的 180 次 ForceDelta-VLA 测试中，我们观察到 32 次失败，并将其分为三类。其中 14 次属于*参考轨迹失败*，即参考轨迹超出有界 residual 的可修正范围。7 次源于*参考轨迹过度陈旧*，其 reference age 超出训练采用的延迟范围。其余 11 次需要*非局部恢复*，例如在卡滞或错误侧接触后执行回退、重新接近或策略切换。

### 4.3 独立残差执行

两个实验用于检验独立残差执行。Runtime profiling 量化 reference 与 residual 通路之间实际实现的频率分离；reference-delay 实验则检验当缓存 reference 逐渐陈旧时，这种频率分离能否改善接触行为。

#### 实际频率分离

我们在按按钮任务上使用部署 GPU 对所有路径进行 profiling（见表 IV）。ForceDelta residual 的单次独立 forward 需要 2.43 ms，空力 reference 需要 908.69 ms。当两个 ForceDelta worker 共享一张 GPU 时，residual 的实际发布率为 18.19 Hz，是 1.06 Hz reference rate 的 17.2 倍。ImplicitRDP 的 reactive packet 发布率为 78.10 Hz。这些测量区分了独立 forward 成本与当前部署条件下的实际吞吐率。

| 方法/路径 | 推理延迟 (ms) ↓ | Packet 更新率 (Hz) ↑ |
|---|---:|---:|
| ImplicitRDP reactive | 12.86 $\pm$ 0.23 | **78.10** |
| Temporal Teacher | 934.90 $\pm$ 13.32 | 1.03 |
| ForceDelta reference | 908.69 $\pm$ 15.01 | 1.06 |
| ForceDelta residual | **2.43** $\pm$ 0.12 | 18.19 |

#### 延迟 reference 鲁棒性

我们在 USB 插入和插插头任务上施加 0、100 和 200 ms 的额外 packet-publication delay；每个任务、方法和延迟条件测试 10 次。ForceDelta-VLA 的性能下降比 Temporal Teacher 更缓慢：从 0 ms 增加到 200 ms 时，二者成功率分别下降 15 和 25 个百分点，峰值接触力则分别增加约 30% 和 60%。这表明独立 residual 更新提高了系统对 reference 延迟可用的鲁棒性（见图 4）。

**图 4：两项插入任务对额外 reference-packet delay 的敏感性。** 左：成功率；右：峰值力。蓝色实线与圆形标记表示 ForceDelta-VLA，灰色虚线与方形标记表示 Temporal Teacher。

### 4.4 消融实验

我们首先在保持已训练模型不变的情况下，于执行阶段移除力修正和陈旧性修正。随后重新训练改变残差目标构造、空力模式学习和 schedule replay 的变体（见表 V）。示范导出变体将同一查询状态下的示范动作减去空力模式 teacher 预测，并以所得差值作为残差目标。

**表 V：四项代表性任务上的消融结果：单臂 USB 插入和擦白板，以及双臂插插头和打开抽屉。成功率和峰值力分别按两个平台报告。**

| 变体 | 单臂成功率（%）↑ | 双臂成功率（%）↑ | 单臂峰值力（N）↓ | 双臂峰值力（N）↓ |
|---|---:|---:|---:|---:|
| Reference only | 62.5 | 50.0 | 17.4 $\pm$ 5.1 | 18.1 $\pm$ 5.6 |
| w/o force residual | 70.0 | 57.5 | 15.8 $\pm$ 4.5 | 16.6 $\pm$ 4.9 |
| w/o staleness residual | 77.5 | 67.5 | 13.7 $\pm$ 3.9 | 14.4 $\pm$ 4.2 |
| **ForceDelta-VLA (full)** | **85.0** | **75.0** | $\mathbf{11.4}\pm 3.1$ | $\mathbf{12.0}\pm 3.5$ |
| Demonstration-derived residual target | 72.5 | 60.0 | 14.9 $\pm$ 4.3 | 15.7 $\pm$ 4.7 |
| Zero-force condition | 67.5 | 55.0 | 15.6 $\pm$ 4.6 | 16.4 $\pm$ 5.0 |
| w/o schedule replay | 80.0 | 65.0 | 12.9 $\pm$ 3.6 | 14.1 $\pm$ 4.1 |

## 5. 结论与未来工作

本文提出 ForceDelta-VLA，从冻结的力感知 teacher 中导出力修正与陈旧性修正，并将二者蒸馏为独立于完整动作生成执行的残差策略。在九项真实机器人任务上，ForceDelta-VLA 的平均成功率达到 82.2%，并在两个平台的所有评估方法中取得最低的峰值接触力。

该方法继承离线 teacher 的误差与数据覆盖限制，有界 residual 也无法表达非局部策略切换。机器人估计的 wrench 限制了模型对细微接触变化的敏感性。当前单 GPU JAX 部署因生成式 reference inference 的资源竞争而限制 residual 吞吐率。优化推理或采用独立计算资源可以提高更新频率。未来工作将加入纠正性 rollout 与非局部恢复策略。

## 附录 A：单臂任务 Rollout

**图 5：五项单臂任务的代表性 rollout 序列。** 从上到下依次为翻物体、按按钮、插 USB、开柜门和擦白板。
