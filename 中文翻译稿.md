# ForceDelta-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力反馈能够揭示图像中不可观测的接触动态，但力感知视觉—语言—动作（VLA）策略的反应式路径仍需生成完整动作块，因而更新成本较高。我们提出 ForceDelta-VLA，以**反事实残差蒸馏**取代完整动作的重复生成。

对于相同的视觉观测、指令和机器人状态，我们对冻结的时序力感知教师进行两次查询：事实查询使用实测力历史，反事实查询使用经过适配的空力条件。两次查询共享视觉—语言前缀和初始流噪声，其动作空间差异为力响应修正提供显式目标。反事实预测作为慢速参考，轻量级因果学生则结合原生频率力历史、缓存任务上下文、机器人状态和参考年龄，只更新位姿残差。时序随机化训练使学生接触异步执行中出现的上下文陈旧。

我们在八项接触密集型单臂和双臂任务上评估 ForceDelta-VLA，报告任务成功率、峰值接触力和任务完成时间。

<!-- 投稿前：完成机器人实验后，在摘要中加入最主要的任务结果。 -->

## 1. 引言

视觉—语言—动作（VLA）模型为机器人操作提供了语义泛化能力，但接触密集型任务要求反馈同时具备物理信息与时间响应性。视觉观测可能无法揭示接触开始、滑移、冲击或受约束运动，而力/力矩测量能够直接表征这些交互状态。因此，力感知 VLA 将力/力矩反馈作为动作生成的条件。剩下的瓶颈在于，新观测到的力测量仍然绑定于完整动作块的重新生成，因此物理反馈的代价取决于完整生成器，而不是力真正所需的那部分修正。

近期的慢—快架构通过在力响应路径中复用视觉—语言上下文来降低**感知**成本。但快速路径仍然是完整动作预测器：每次更新都会把慢速策略已经表示的标称运动，与力引起的变化一并重新合成。因此，缓存前缀去掉的是视觉—语言推理，而不是动作生成；而仅靠频率分离，并不能规定动作中的哪一部分应当归属反应式分支。

残差形式通过组合慢速更新的参考与力条件修正来分离这两种功能。其核心问题是如何从离线示范中辨识修正量：每条示范只监督执行动作，并不提供参考分量与残差分量的分解。直接行为克隆因而允许退化分解，例如慢速分支保留接触相关行为，或快速分支重复生成标称策略。要使反应式路径专门学习修正，需要显式的残差目标。

为此，我们提出**反事实残差蒸馏**。对同一观测，冻结的力感知 VLA 教师被查询两次。两次查询共享图像、指令、机器人状态、视觉—语言前缀和流匹配噪声，唯一的差别在于提供实测力历史还是适配后的空力条件。成对预测之差即为修正目标；空力条件预测直接作为慢速参考，因此不需要训练独立的标称策略。紧凑的因果学生从原生频率力历史、缓存任务上下文、机器人状态以及当前参考的年龄中蒸馏该目标，并在随机化的慢速更新时序下训练，使同一个学生可以异步执行，而无需重新运行生成式动作专家。

本文贡献如下：

1. 提出**反事实残差蒸馏**：将冻结 VLA 教师在事实力条件和空力反事实条件下的成对预测，转化为力响应修正的显式监督信号；
2. 构建**受控的反事实查询**：适配后的空力模式通过共享视觉—语言前缀和共享流噪声与事实查询耦合，从而把力条件的作用与采样方差、以及零填充干预带来的性能退化分离开；
3. 提出**仅更新残差的反应式路径**：从原生频率力信号异步更新位姿修正，而不是迭代地重新生成完整动作。

我们在八项接触密集型单臂与双臂任务上评估 ForceDelta-VLA。相对 $\pi_{0.5}$，平均成功率提升 **[X]** 个百分点；相对 ForceVLA 提升 **[Y]** 个百分点，同时峰值接触力更低、任务完成时间更短。

<!-- 投稿前：机器人实验完成后替换 [X] 与 [Y]。峰值力与完成时间只在表格确实报告时才写。 -->

## 2. 相关工作

### 2.1 力感知视觉—语言—动作策略

力/力矩反馈能够揭示图像中可能含糊的接触开始、载荷和受约束运动。ForceVLA 通过力感知混合专家融合模块引入六轴力/力矩测量；ForceVLA2 将阶段级力提示、直接力通路和混合力—位置控制相结合；TA-VLA 则研究时序力矩特征应在预训练 VLA 解码器的何处注入。这些方法在融合深度和控制接口上有所不同，但都利用物理反馈来调节完整动作块的生成。

这种形式增强了策略的接触感知能力，却没有将缓慢变化的任务规划与新力观测引起的修正分离。因此，每次力更新仍与动作生成路径耦合，示范数据也没有为动作中的修正分量提供显式标签。

### 2.2 慢—快力响应控制

慢—快策略根据语义感知和物理交互的不同时间尺度重新组织计算。FAVLA 缓存慢速视觉—语言上下文，重复运行力条件动作专家，并根据预测的力变化调节其更新频率。LIFT 同样复用视觉—语言前缀，但将近期力记忆注入使用在线纠正训练的反应式动作流。DAM-VLA 则维护按各模态原生频率更新的独立潜变量缓存，由动作专家异步读取。

相关的非 VLA 策略也在视觉运动控制层面处理这一频率失配。ManipForce 从异步 RGB 与力/力矩数据流中学习频率感知表示，ImplicitRDP 则在视觉—力扩散策略中引入因果慢—快结构。这些方法保留了高频物理反馈，但反应式路径仍然预测完整动作；仅仅分离更新频率并不能确定动作中的哪一部分应交给快速修正。

### 2.3 残差学习与条件蒸馏

残差策略保留基础控制器并学习加性修正，但需要一种信号将修正与完整执行动作区分开来。ResiP 从任务奖励中获得这一信号，在冻结的动作分块模仿策略之上使用强化学习训练闭环残差。Compliant Residual DAgger 则收集在线动觉增量纠正，并利用这些额外示范训练力条件残差。两类方法都依赖原始离线行为克隆标签之外的信息来定义残差。

条件建模为构造这种监督提供了另一条路径。学习式缺失模态 token 能够区分不可用观测与具有物理意义的零值。在生成模型中，成对的条件预测与无条件预测被用于分离条件对生成结果的影响，无分类器引导蒸馏进一步将这种成对计算迁移到成本更低的预测器中。这些思想为直接从预训练条件策略中导出动作空间修正提供了基础，而无需奖励优化或重新采集纠正示范。

## 3. 方法

### 3.1 问题定义

我们研究接触密集型操作，其中策略接收图像 $V$、语言指令 $L$、机器人状态 $S$ 和带时间戳的力/力矩数据流 $F$。训练和部署期间均有力输入，且力数据独立于视觉—语言推理更新。我们从一个预训练的力条件 VLA 教师 $T_\theta$ 出发，并全程冻结该教师。

动作在末端执行器坐标系中表示为

$$
A=[p\in\mathbb{R}^3;\ r\in\mathbb{R}^6;\ g\in\mathbb{R}]\in\mathbb{R}^{10},
$$

其中 $r$ 为连续六维旋转表示，$g$ 为夹爪指令，$A^p=[p;r]\in\mathbb{R}^9$ 为位姿分量。

令 $t_k$ 为最近一次慢速更新的观测时间，$t$ 为当前控制时刻。我们把控制命令分解为参考量与修正量：

$$
A^{\mathrm{cmd}}(t)=A^{\mathrm{ref}}(t)+\Delta A(t),\qquad
\Delta A(t)\in\mathbb{R}^9,
$$

其中 $A^{\mathrm{ref}}(t)$ 由 $t_k$ 时生成的动作块插值得到，修正量只作用于位姿；抓取切换是语义性的而非反应性的，因此夹爪通道保留在慢速分支。

该分解的价值在于两项的代价不同：参考量在语义时间尺度上变化，由缓存动作块插值得到，因此新的力测量只需重新计算 $\Delta A$。困难在于这一分解无法由数据辨识——示范只监督执行动作，任何求和等于该动作的 $(A^{\mathrm{ref}},\Delta A)$ 对行为克隆都同样成立。没有任何机制阻止参考分支吸收接触相关行为、而修正分支去复现标称运动；一旦如此，反应式分支的开销就与它本应替代的策略相同。为此，我们利用冻结的教师为 $\Delta A$ 构造显式目标。

### 3.2 反事实残差目标

我们的目标量是：教师的输出在"以实测力为条件"与"以无力为条件"之间发生的变化。获得它只需对一个冻结模型查询两次，但要让这个差可以作为监督，必须满足三个条件：反事实分支本身仍是一个称职的策略，否则该差衡量的是退化而不是力；力条件必须是两次查询之间唯一不同的量，否则该差会被其它变化主导；目标只能依赖学生在动作时刻已经可以获得的信息。下面依次说明。

#### 力条件教师

教师沿用 ForceVLA 的 $\pi_0$ 风格流匹配策略，将其瞬时力输入替换为因果时序编码器。在每个教师推理时刻 $t_k$，从带时间戳的原生频率流中读取固定时长窗口

$$
\mathcal F_k=\{F(\tau)\mid t_k-w\leq\tau\leq t_k\},
$$

而不是按视觉观测频率采样力，从而使接触开始、冲击瞬态和恢复落在条件信号内部。有效性 mask 区分未填满的窗口与真实测得的零力。因果时序卷积网络把该窗口编码为单个 token

$$
z_k^F=E_T^F(\mathcal F_k),
$$

它在原有融合模块中替换瞬时力嵌入，动作生成通路保持不变。事实预测为

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F)\in\mathbb{R}^{H\times10}.
$$

#### 一个称职的反事实

为在不提供当前测量的情况下查询同一教师，我们用可学习 token $z_{\varnothing F}\in\mathbb{R}^{D_F}$ 替换 $z_k^F$。由于它在样本间共享，它表示缺少实例特定的力观测，而不是物理零力：它可以携带数据集级别的先验，但无法携带当前力历史。这也把该查询与零填充输入区分开——后者对于用真实力训练出的网络属于分布外输入，其输出变化会把力的作用与"对无效输入的反应"混在一起。

仅替换该 token 仍会使策略处于退化状态，因为教师是在有力可用的条件下训练的。因此我们允许反事实分支恢复：保持教师权重全部冻结，与 $z_{\varnothing F}$ 一起优化一个修正流速度位姿通道的 rank-$\rho$ 适配器

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h),
$$

其中 $h$ 为动作专家隐状态，$\sigma$ 为 SiLU，训练目标为教师的动作回归损失

$$
\mathcal L_{\varnothing}=d\!\left(T_\theta(V_k,L,S_k,z_{\varnothing F}),A_k^{\mathrm{expert}}\right).
$$

该适配器只在提供 $z_{\varnothing F}$ 时启用，因此事实分支与原教师逐位相同，比较不会因为"两边都获得了额外容量"而被混淆。于是该过程得到同一网络的适配空力**模式**，而不是第二个策略；部署时同一模式作为慢速分支，因此不需要蒸馏任何标称策略。

#### 隔离力条件

教师是随机采样器，因此两次独立查询之间的差异会同时来自采样噪声和力条件。我们移除其余所有变化来源：视觉—语言前缀及其 key/value cache 只计算一次并复用，且用同一次采样 $\epsilon$ 初始化两次流积分：

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F;\epsilon),
$$

$$
A_{T,k}^{\mathrm{null}}=T_\theta(V_k,L,S_k,z_{\varnothing F};\epsilon),
\quad \epsilon\sim\mathcal N(0,I).
$$

消融实验给出 $\epsilon$ 独立采样时的结果。复用前缀还把提取开销减半，因为视觉—语言前向是其中的主导项。

反事实预测作为参考：

$$
A_{T,k}^{\mathrm{ref}}=A_{T,k}^{\mathrm{null}},
$$

**反事实残差**为两者在位姿通道上的差：

$$
\Delta A_{T,k}^{\mathrm{cf}}
=\left(A_{T,k}^{\mathrm{full}}\right)^p
-\left(A_{T,k}^{\mathrm{null}}\right)^p
\in\mathbb{R}^{H\times9}.
$$

该式有两点关系到应当如何解读它。它定义在模型层面而不是环境层面：空力分支带有自己的适配器，且视觉与状态同时作为两次查询的条件、本身也可能揭示接触，因此该残差是教师在此替换下的完整模式变化，而不是可唯一归因于力的效应。它同时对学生的信息是因果的：教师的整个动作块都由 $t_k$ 这一个观测生成，目标的第 $j$ 步是教师对已经观测到的力的响应，没有任何未来测量进入标签。

### 3.3 残差策略

#### 反应式分支能看到什么

每次慢速更新发布参考动作块 $A_{T,k}^{\mathrm{null}}$，并从同一次前向传播中给出任务上下文的紧凑摘要。视觉—语言前缀 $H_k\in\mathbb{R}^{S\times d}$ 含数百个 token，通过带 mask 的均值池化压缩为 $M$ 个连续区间：

$$
Z_k^{\mathrm{intent}}=\mathrm{Pool}_M(H_k,m_k)\in\mathbb{R}^{M\times d}.
$$

池化是固定的而非学习的，因此部署时可以从在线前缀无参数地复现同一摘要；学生内部再用学习式投影把这些区间映射到自身宽度。

于是反应式分支以传感器频率读取力，但只通过 $Z_k^{\mathrm{intent}}$ 读取视觉，其时效等于上一次慢速更新。这种不对称是有意的，且来自反事实残差的定义：前缀在两次查询之间保持固定，因此目标中不含任何"更新的图像可以解释"的变化。给学生提供新图像不会降低其损失，而在反应式路径上增加视觉编码器则会抵消支撑该分解的代价优势。取而代之，我们通过时间特征告知学生其上下文有多陈旧。

#### 结构

学生用一次确定性前向，把力历史和缓存上下文映射为一小段位姿修正。与教师同族的因果 TCN 把原生频率窗口 $\mathcal F_t$ 编码为 $Z_t^F$，有效性 mask 在其输入处施加。其余输入构成一个无序条件集合，各自投影到共同宽度并加上学习式类型嵌入：

$$
X_t=[\widetilde Z_k^{\mathrm{intent}};Z_t^F;P_S(S_t);P_A(A^{\mathrm{ref}}(t));P_\Gamma(\gamma_t);q],
$$

其中 $\gamma_t$ 为时间特征，$q$ 为学习式查询。由于这些条件不存在有意义的顺序，模型不使用位置编码。一个带 grouped-query attention 的单层 pre-norm 模块处理该集合，条件之间相互注意，$q$ 读取它们而不被它们看到；线性头把查询状态映射为动作块：

$$
\Delta\hat A_{t,1:K}=W_{\mathrm{res}}H_t[q]\in\mathbb{R}^{K\times9}.
$$

$W_{\mathrm{res}}$ 零初始化使未训练的学生成为恒等控制器，因此训练从 $A^{\mathrm{cmd}}=A^{\mathrm{ref}}$ 出发，而不是从它的随机扰动出发。

输出按动作周期 $\tau_A$ 间隔的 $K$ 步而非单步修正，使学生的更新频率与执行器解耦：若一次更新迟到、或执行器快于学生，上一动作块中较后的步对其所预测的时刻仍然是有效命令。

#### 目标函数

只有学生被训练。令 $w_j\propto\beta^{j-1}$ 为教师目标前 $K$ 步上的归一化权重，一项直接监督修正量

$$
\mathcal L_{\mathrm{res}}=\sum_{j=1}^{K}w_jd(\Delta\hat A_{t,j},\Delta A_{T,j}^{\mathrm{cf}}),
$$

另一项要求组合后的命令复现事实教师

$$
\mathcal L_{\mathrm{comp}}=\sum_{j=1}^{K}w_jd((A_j^{\mathrm{ref}})^p+\Delta\hat A_{t,j},(A_{T,j}^{\mathrm{full}})^p),
$$

合为

$$
\mathcal L=\lambda_{\mathrm{res}}\mathcal L_{\mathrm{res}}+\lambda_{\mathrm{comp}}\mathcal L_{\mathrm{comp}}.
$$

两项约束的对象不同：前者使学生专注于修正量，后者惩罚那些在与学生实际叠加的参考组合之后仍然存在的误差。衰减 $\beta<1$ 使第一步（学生跟得上时执行的那一步）权重高于较后的步（只在跟不上时才执行）。这里不需要参考模仿项，因为参考直接来自冻结教师；也不施加行为克隆项，因为把学生回归到示范动作会重新引入它本就被构造出来排除掉的总动作。

### 3.4 时序随机化训练与异步执行

参考量的年龄取决于慢速策略的频率和推理延迟，而二者在实践中都不固定。以单一运行点训练的学生只会学到该运行点，因此我们把时序当作一个分布而不是一个设定。目标按完整观测频率提取一次，每个 epoch 重新抽取慢速更新周期（取自 $[1/f_{\max},1/f_{\min}]$）、推理延迟 $\ell$（取自 $[\ell_{\min},\ell_{\max}]$）和逐次更新抖动（取自 $[-\eta,\eta]$）。在 $t_k$ 发布的数据包只有到 $t_k+\ell$ 才可用，此前的控制时刻被排除，在途中被更新数据包超越的包被丢弃。由于学生不会遇到年龄为零的上下文，它也就不会在真机无法达到的条件上被训练。

我们不让学生去推断运行点，而是直接告知它：

$$
\gamma_t=\left[\frac{t-t_k}{c},\ \alpha_t\right],\qquad
\alpha_t=\frac{t-t_j}{\tau_A}\in[0,1),
$$

其中 $c$ 是覆盖随机化区间的固定尺度，$t_j$ 为 $t$ 之前的动作块航点。两个特征并不冗余：前者在一个慢速周期内持续累积，报告语义上下文有多陈旧；后者在其内部周期性变化，确定命令在一个动作区间内的位置。

部署时三个回路并发运行。教师以空力模式运行，发布包含参考动作块、池化意图区间、观测时间戳以及该包变为可用时刻的数据包。学生读取最新的数据包、力窗口和状态，发布带有有效区间标记的命令块。执行回路在控制时刻对该块采样并施加命令组合，同时把参考量 rebase 到其所属数据包观测到的状态，而不是当前状态。把发布与消费分离，使执行器不会阻塞在任一模型上：学生更新迟到时退化为执行上一块中较后的一步，而这正是残差输出所监督的情形；当没有未过期的块时，执行器保持当前命令而不做外推。

反应式路径的代价由此直接得出。缓存前缀式慢—快策略在该回路中保留动作专家，每次更新需付出 $N_{\mathrm{flow}}$ 次去噪，即 $\mathcal O(N_{\mathrm{flow}}L_{\mathrm{AE}}d_{\mathrm{AE}}^2)$（深度 $L_{\mathrm{AE}}$、宽度 $d_{\mathrm{AE}}$）。我们的一次更新是在小网络上做一次确定性前向，即 $\mathcal O(L_Sd_S^2)$，同时减少步数与深度—宽度乘积。

## 4. 实验

### 4.1 实验设置

#### 硬件平台

单臂平台由一台七自由度 Franka Emika Panda 机械臂和两台 Intel RealSense D405 相机构成。双臂任务使用独立的双臂实验平台，其机械臂、末端执行器、力/力矩传感器及相机配置将在硬件信息确定后补充。

#### 任务与数据采集

单臂平台执行按按钮、翻转物体、插入 USB 和打开厨房门，双臂平台执行插入插头、拔出插头、擦白板和打开抽屉。每项单臂和双臂任务分别采集 200 条和 300 条遥操作示范。每条轨迹以 30 Hz 记录同步的多视角图像、机器人状态和命令动作，同时以 100 Hz 记录带时间戳的六轴力/力矩测量。

| 类型 | 任务 | 交互状态 |
|---|---|---|
| 单臂 | 按按钮 | 建立接触、力阈值 |
| 单臂 | 翻转物体 | 贴靠表面的接触重构 |
| 单臂 | 插入 USB | 几何约束下的精密插入 |
| 单臂 | 打开厨房门 | 关节机构约束运动 |
| 双臂 | 插入插头 | 精密插入、力闭合 |
| 双臂 | 拔出插头 | 几何约束下拔出 |
| 双臂 | 擦白板 | 对另一只手持表面的持续接触 |
| 双臂 | 打开抽屉 | 双臂协调约束运动 |

### 4.2 基线、指标与实现细节

对比方法包括：不使用力的 $\pi_{0.5}$、使用瞬时力融合的 ForceVLA、在解码器中引入力矩历史的 TA-VLA，以及采用结构化慢—快处理的视觉—力扩散策略 ImplicitRDP。

主要评估指标为任务成功率。对于每种方法和每项任务，我们评估两个训练 seed，每个 seed 测试 10 次，报告各项任务的成功率，以及四项单臂任务和四项双臂任务分别的平均值。此外在成功试验上报告两项交互指标：峰值接触力（该次试验中记录到的最大力幅值）和任务完成时间，二者均按任务组取平均。

所有模型均使用两张 NVIDIA A100 GPU，以 bfloat16 精度训练。教师采用 AdamW 全量微调 50,000 步，全局 batch size 为 32，峰值学习率为 $2\times10^{-5}$，weight decay 为 0.01，使用 1,000 步 warmup、cosine decay 和 1.0 的梯度裁剪。教师以 30 Hz 预测长度为 $H=50$ 的动作块，并编码 100 Hz 力/力矩数据流中最近 100 ms 的测量；流匹配推理使用 $N_{\mathrm{flow}}=10$ 步。

随后冻结教师，使用 $10^{-4}$ 的学习率训练空力 token 和 rank-32 空力适配器 10,000 步。视觉—语言前缀池化为 $M=16$ 个 token。残差学生包含一个 pre-norm attention block，并预测 $K=5$ 步修正；学生使用 AdamW 训练 30,000 步，全局 batch size 为 128，学习率为 $3\times10^{-4}$。损失权重设为 $\lambda_{\mathrm{res}}=\lambda_{\mathrm{comp}}=1$，$\beta=0.5$。时序随机化从 5--15 Hz 采样慢速策略频率，从 50--300 ms 采样延迟，并从 $[-10,10]$ ms 采样调度抖动。

### 4.3 定量结果

主表比较 $\pi_{0.5}$、ForceVLA、TA-VLA、ImplicitRDP 和 ForceDelta-VLA 在八项任务上的成功率，并分别汇总单臂与双臂任务的平均成功率。第二张表在成功试验上报告峰值接触力和任务完成时间，同样按单臂与双臂分组，数值越低越好。当前机器人结果尚未提供，因此英文原稿中的任务数值仍为空，本文档不代填任何实验数据。

### 4.4 定性结果

我们展示单臂和双臂任务中具有代表性的成功与失败轨迹。每段图像序列同时给出同步的力/力矩曲线和预测残差，以呈现策略在物理交互过程中如何修正慢速参考。

### 4.5 消融实验

消融包括：

1. **预测目标：**比较重新生成完整动作的快速分支、直接使用示范动作训练的残差学生，以及使用成对反事实残差目标训练的完整方法；
2. **力表示：**比较瞬时力、降采样至 30 Hz 的力历史，以及原生 100 Hz 因果力历史；
3. **反事实构造：**比较零力输入、学习式空力 token、带空力适配器的学习式 token，以及独立流噪声与共享流噪声。

## 5. 结论

本文提出 ForceDelta-VLA：一种以反事实残差蒸馏取代反应式完整动作生成的力感知 VLA。同一冻结教师在事实力条件与适配空力反事实条件下的成对预测定义残差目标，反事实预测同时作为慢速参考。轻量级因果学生根据原生频率力历史、缓存任务上下文、机器人状态和参考年龄，只更新位姿修正。这一分解将反应式路径从迭代生成策略转化为确定性残差预测器。

ForceDelta-VLA 中的反事实定义在模型层面，而不是因果干预。由于空力查询使用学习式 token 和空力专用适配器，并且视觉与状态也可能揭示接触，所得残差不是唯一的力贡献。该方法还会继承教师误差和离线示范的覆盖限制；需要离散策略变化的交互也可能无法由加性位姿修正表示。未来工作应将反事实残差蒸馏与少量在线修正结合，并分析存在延迟的异步组合中的稳定性和安全性。
