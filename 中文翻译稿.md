# ForceDelta-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力感知视觉—语言—动作（VLA）策略改善了接触密集型操作，但通常仍在同一个生成式动作预测中耦合任务层运动与接触相关调整。离线示范没有提供将该预测分解为可复用 reference 和 correction 的显式标签。我们提出 ForceDelta-VLA：在缓存上下文下，将冻结 teacher 的力条件预测与学习式力输入缺失预测配对，由两者之差构造 teacher 定义的力响应修正目标；该过程仅使用原始离线示范数据集。学习式陈旧性残差与解析参考状态重定位共同补偿缓存 reference 不匹配，残差策略采用异步调度重放训练。所得残差策略复用缓存任务上下文和带时间戳的力输入缺失 reference，同时利用近期力历史更新位姿修正，无需重新生成完整动作。在九项接触密集型单臂和双臂任务上，ForceDelta-VLA 达到 82.2% 的平均成功率，ForceVLA 为 54.4%。相较 ForceVLA，两个平台的成功试验平均峰值接触力均降低约 26%。


## 1. 引言

力感知视觉—语言—动作（VLA）策略通过以交互反馈为条件生成动作，提高了接触密集型操作性能。然而，这类策略通常在同一个生成式动作预测中同时表示任务层运动与接触相关调整。部署时，相邻控制周期可以复用任务层行为，而物理修正应随新观测到的接触状态及时变化。这种耦合使修正缺少显式监督目标和可独立执行的输出形式（见图 1）。

力响应加性残差在保留力输入缺失 reference 行为的同时，提供显式的位姿空间修正。标准离线示范只包含执行动作；多个 reference--residual 组合可以重建同一动作标签，因此直接动作监督无法确定任务层运动和接触相关调整应分别由哪个分量承担。

从示范动作中减去缓存 reference 还会混合力条件偏差与 reference 陈旧性；零力查询则表示一种有效物理状态，而非缺失力条件。这些歧义促使我们在匹配查询条件下利用冻结的力感知 teacher 构造修正目标。

现有异步策略分离计算或传感器更新频率，残差强化学习和纠正式模仿学习则通过任务奖励或额外在线交互获得修正信号。本文研究一个互补问题：离线构造修正监督，同时保留异步执行。

ForceDelta-VLA 在缓存上下文下构造两个匹配的 teacher 查询：力条件查询使用当前力历史，力输入缺失查询使用学习式 missing-force token；二者共享缓存的视觉—语言前缀、当前机器人状态与 flow noise。其动作差定义 teacher 监督的力响应偏差，另一目标补偿缓存 reference 的状态与时间不匹配。两个修正被蒸馏为独立于生成式动作推理执行的残差策略。

我们在五项单臂和四项双臂真实机器人任务上评估 ForceDelta-VLA，任务涵盖接触建立、精密插入、摩擦主导运动和受约束操作。本方法的平均成功率为 82.2%，ForceVLA、TA-VLA 和 ImplicitRDP 分别为 54.4%、57.2% 和 45.0%。相较 ForceVLA，两个平台的成功试验平均峰值接触力均降低约 26%。

本文贡献如下：

1. 提出**力输入缺失残差蒸馏**，利用冻结力感知 teacher 在缓存上下文下的匹配力条件预测与学习式力输入缺失预测，仅利用原始离线数据集构造 teacher 定义的显式力响应修正目标；
2. 提出**参考状态一致的双残差分解**，分别监督力条件修正与缓存 reference 陈旧性，并解析地重定位已知状态位移；
3. 构建**可独立执行残差通路**，复用缓存任务上下文和力输入缺失 reference，并根据近期力历史更新位姿修正。


## 2. 相关工作

### 2.1 力感知操作策略

力感知 VLA 通过多模态融合、提示、时序记忆、混合力—位置控制或力感知优化整合力/力矩信息；力中心模仿学习则从示范中联合建模运动与交互力。这些方法通常仍在统一策略输出中共同表示任务运动和物理适应。FD-VLA 则从视觉与状态蒸馏潜在力表征，用于不依赖实测 wrench 反馈的推理；ForceDelta-VLA 保留实测 wrench 输入，并蒸馏 teacher 的力条件动作偏差。

### 2.2 多频率与结构化操作策略

视觉运动方法学习异步 RGB 与力/力矩流，按交互阶段调整扩散推理频率，或引入快速修正与混合力—位置通路。VLA 方法也会分离模态更新频率。FAVLA 使用慢速语义 backbone 和可变频率的力条件 action expert 重新生成动作块；DAM-VLA 按各传感器频率刷新模态专用 latent buffer，并由 100 Hz action head 读取。Real-Time Chunking（RTC）则面向 flow-based action-chunk policy 的推理延迟：它异步生成下一动作块，并对其中尚未承诺执行的动作进行 inpainting，使其与正在执行的动作块保持连续。ForceDelta-VLA 处理一个互补问题：保留生成式 VLA 输出作为缓存 reference，为显式的力条件加性修正及 reference-staleness correction 构造离线监督，而无需在每次快速更新时重新生成完整动作块。

### 2.3 残差策略学习

残差策略学习用学习得到的修正增强标称控制器，并已用于接触密集型操作。残差强化学习和纠正式模仿学习通过奖励或在线人工纠正获得监督，应用包括冻结视觉策略的触觉条件适配，以及利用 DAgger 训练的反应式力专家。其他模仿学习方法直接规定加性修正结构。ForceDelta-VLA 则从匹配的力条件与力输入缺失 teacher 预测中导出 residual target。

### 2.4 策略蒸馏

学习式缺失模态表征能够区分不可用条件与具有物理意义的零值，从而支持建立独立的力输入缺失运行点。机器人策略蒸馏通常将迭代扩散或流采样压缩为快速的完整动作生成器；guidance distillation 则利用成对预测迁移条件更新。ForceDelta-VLA 保留生成式输出作为任务层 reference，并蒸馏动作空间中的力偏差与参考状态修正，而非完整动作生成器。

## 3. 方法

### 3.1 问题定义

我们考虑由遥操作轨迹组成的离线数据集 $\mathcal D$。在时刻 $t$，每条轨迹提供视觉观测 $V_t$、语言指令 $L$、机器人状态 $S_t$、示范动作 $A_t^*$，以及因果 wrench 历史

$$
\mathcal F_t=\{(s,\mathbf w(s))\mid t-T_F\leq s\leq t\},
$$

其中，$T_F$ 是历史窗口长度，$\mathbf w(s)=[\mathbf f(s);\boldsymbol\tau(s)]\in\mathbb R^6$ 是作用于末端的外部 wrench。位姿动作和 wrench 测量均使用对应的机器人基坐标系。力条件 teacher $T_\theta$ 将 $(V_t,L,S_t,\mathcal F_t)$ 映射为相对于 $S_t$ 的 $H$ 步动作块。下文的位姿动作与残差使用其归一化 pose-action 坐标，位姿差按动作通道标准差进行尺度归一化。

本文的目标是为力响应修正构造显式监督，并将其蒸馏为可以独立于生成式 teacher 计算的残差策略。令 $\mathcal P(\cdot)$ 提取动作中的位姿分量。我们将命令位姿分解为一个 reference、两个学习式修正和一个解析重定位项：

$$
\mathcal P(A^{\mathrm{cmd}})=\mathcal P(A^{\mathrm{ref}})
+\Delta\hat A^{\mathrm{force}}
+\Delta\hat A^{\mathrm{stale}}+\Gamma(S_t,S_k),
$$

力残差建模 teacher 的力条件偏差，学习式陈旧性残差建模剩余的缓存 reference 差异，$\Gamma$ 将参考查询状态 $S_k$ 重定位到当前状态 $S_t$。这三项都只作用于位姿；夹爪指令沿用 reference。

其中，位姿分量 $\mathcal P(A)=[p;r]$ 包含三维平移 $p$ 和连续六维旋转表示 $r$。平移直接相加；旋转修正在 teacher 的六维动作坐标中相加，再通过 Gram--Schmidt 正交化映射为合法旋转矩阵。

### 3.2 方法概述

训练分为图 2 所示的三个阶段。首先训练时序力条件 VLA teacher。随后冻结 teacher，并学习提供参考动作的力输入缺失模式。最后冻结两种模式，在缓存上下文下构造力修正和陈旧性修正目标，并训练残差策略。

### 3.3 力条件教师与力输入缺失参考

#### 时序力条件教师

教师基于 ForceVLA，将其瞬时力嵌入替换为 FAVLA 风格的因果时序卷积网络（TCN），用于编码 wrench 历史：

$$
z_t^F=E_T^F(\mathcal F_t).
$$

教师在 $\mathcal D$ 上训练并预测

$$
A_{T,t,1:H}^{\mathrm{cond}}=T_\theta(V_t,L,S_t,z_t^F;\epsilon).
$$

其中 $\epsilon$ 为初始流噪声。训练完成后冻结 $\theta$。

#### 力输入缺失参考模式

我们区分“不提供 wrench 历史”和“测得的 wrench 恰好为零”。学习式力输入缺失模式用于前一种情况，由 missing-force token $z_{\varnothing F}$ 和 rank-$\rho$ 适配器 $\phi=\{z_{\varnothing F},W_{\mathrm{in}},W_{\mathrm{out}}\}$ 构成。适配器仅修改动作专家的位姿通道：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h),
$$

其中，$h$ 是 action expert 的隐藏状态，$v^p$ 是其位姿通道的 flow 输出，$\sigma$ 是逐点非线性函数。该 adapter 只在力输入缺失模式下启用。保持 $\theta$ 固定，在 teacher 原有的 flow-matching 目标下优化 $\phi$，由此定义 teacher 的力输入缺失运行点。

所得参考预测为

$$
A_{T,t,1:H}^{\mathrm{miss}}
=T_{\theta,\phi}^{\mathrm{miss}}(V_t,L,S_t,z_{\varnothing F};\epsilon).
$$

### 3.4 参考状态一致的双残差蒸馏

#### 修正目标

对于 schedule replay 样本 $(t,k)$，令 $E_k$ 表示 reference 查询 $k$ 在 $t_k$ 以 $(V_k,L,S_k)$ 为输入启动时生成的视觉—语言前缀，$\epsilon_k$ 表示该查询的 flow noise。查询发布带时间戳的力输入缺失动作块 $A_k^{\mathrm{ref}}(\cdot)$，其中 $A_k^{\mathrm{ref}}=A_{T,k}^{\mathrm{miss}}$。为使监督信息的时间来源与部署时 residual 通路一致，target generation 复用 $E_k$，同时将机器人状态和力历史更新为时刻 $t$ 的取值：

$$
A_{T,t|k,1:K}^{\mathrm{cond}}
=T_\theta(E_k,S_t,z_t^F;\epsilon_k)_{1:K},
$$

$$
A_{T,t|k,1:K}^{\mathrm{miss}}
=T_{\theta,\phi}^{\mathrm{miss}}
(E_k,S_t,z_{\varnothing F};\epsilon_k)_{1:K}.
$$

这里，$T(E_k,\cdot)$ 表示复用缓存的视觉—语言前缀，不再编码当前图像。reference 查询、力条件查询和力输入缺失查询共享 $E_k$ 与 $\epsilon_k$，因此目标不依赖 student 不可见的当前图像，也不引入独立 flow noise。对于 $j=1,\ldots,K\leq H$，由 teacher 定义的力条件偏差目标为

$$
\Delta A_{T,t|k,j}^{\mathrm{force}}
=\mathcal P\!\left(A_{T,t|k,j}^{\mathrm{cond}}\right)
-\mathcal P\!\left(A_{T,t|k,j}^{\mathrm{miss}}\right).
$$

该目标用于监督力响应残差 head。

residual 查询预测在 $t_j=t+(j-1)/f_A$ 执行的 $K$ 个动作样本，其中 $f_A$ 是动作块内部的动作采样频率。令 $\widetilde S^p$ 表示归一化后的九维位姿坐标，$\sigma_S^p$ 和 $\sigma_A^p$ 表示训练集上相应通道的标准差。缓存状态差按下式转换到归一化 action 坐标：

$$
\Gamma(S_t,S_k)
=\frac{\sigma_S^p+\varepsilon}{\sigma_A^p+\varepsilon}
\odot(\widetilde S_t^p-\widetilde S_k^p),
\qquad \varepsilon=10^{-6}.
$$

陈旧性目标为

$$
\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}
=\mathcal P\!\left(A_{T,t|k,j}^{\mathrm{miss}}\right)
-\mathcal P\!\left(A_k^{\mathrm{ref}}(t_j)\right).
$$

令 $\mathcal R(S,a)$ 表示将以状态 $S$ 为基准的相对动作 $a$ 转换为绝对机器人命令。两项目标在每个预测步上对齐两个坐标基准：

$$
\begin{aligned}
&\mathcal R\!\left(S_k,\mathcal P(A_k^{\mathrm{ref}}(t_j))
+\Delta A_{T,t|k,j}^{\mathrm{force}}
+\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}+\Gamma(S_t,S_k)\right)\\
&\qquad\approx\mathcal R\!\left(S_t,
\mathcal P(A_{T,t|k,j}^{\mathrm{cond}})\right).
\end{aligned}
$$

限幅前，代入两个目标得到经 $\Gamma$ 重定位的力条件动作块。平移重定位在归一化欧氏坐标下除 $\varepsilon$ 外是精确的；旋转部分在六维坐标中相加并做 Gram--Schmidt 投影，因此是近似的。

#### 残差策略

每个 reference query 缓存视觉—语言上下文的紧凑表示。$m_k$ 标记 $E_k$ 中可用图像视角与非 padding 语言位置；$E_k$ 不包含状态、力或动作 token。首先将前缀划分为 $M=16$ 个位置区间，在各区间内做带 mask 的均值池化，再由 $Q=2$ 个学习式查询聚合有效池化 token：

$$
(\bar E_k,\bar m_k)=\operatorname{Pool}_M(E_k,m_k),\qquad
Z_k^{\mathrm{intent}}=P_{\mathrm{intent}}(\bar E_k,\bar m_k).
$$

$A_k^{\mathrm{ref}}$、$Z_k^{\mathrm{intent}}$ 和 $S_k$ 共同构成带时间戳的 reference update，最新可用更新缓存给快速路径。残差策略假设该上下文在一个 reference interval 内局部有效。时间戳插值提供 $A_k^{\mathrm{ref}}(t)$，因果编码器将 $\mathcal F_t$ 映射为 $u_t^F$。缓存年龄和插值相位编码为

$$
\xi_{t,k}=\left[\min\!\left(\frac{t-t_k}{c},1\right);\alpha_{t,k}\right],
$$

其中 $c$ 为年龄尺度，$\alpha_{t,k}$ 是当前时刻在相邻 reference 样本之间的位置。条件集合为

$$
\mathcal C_t=[P_Z(Z_k^{\mathrm{intent}});u_t^F;P_S(S_t);
P_A(A_k^{\mathrm{ref}}(t));P_\xi(\xi_{t,k})].
$$

各投影将输入映射到共享 token 维度。模型不使用位置编码，而用学习式类型嵌入标识条件；将条件 token 及其类型嵌入一起置换时，residual-query 输出保持不变。$\Gamma(S_t,S_k)$ 在策略外计算，不作为条件输入。$\mathcal C_t^{\mathrm{maskF}}=\operatorname{Mask}_F(\mathcal C_t)$ 表示屏蔽力 token 后的同一条件集合。

共享注意力模块 $\Phi$ 和学习式查询 $q_{\mathrm{res}}$ 预测两个长度为 $K$ 的修正块：

$$
\Delta\hat A^{\mathrm{force}}_{t,1:K}
=W_{\mathrm{force}}\Phi([\mathcal C_t;q_{\mathrm{res}}])[q_{\mathrm{res}}],
$$

$$
\Delta\hat A^{\mathrm{stale}}_{t,1:K}
=W_{\mathrm{stale}}
\Phi([\mathcal C_t^{\mathrm{maskF}};q_{\mathrm{res}}])[q_{\mathrm{res}}].
$$

该 mask 避免陈旧性输出直接依赖实测力。在缓存视觉上下文局部有效的前提下，staleness head 针对机器人状态与 reference 时间不匹配进行修正。两次计算共享 $\Phi$，并使用独立的输出投影。

#### 陈旧性感知时序重放

在提取缓存上下文目标之前，我们采样包含 query 间隔与 reference-query latency 的异步调度，并在残差策略优化期间重放这一固定调度。训练时刻沿用数据中记录的 state--action 时间戳，而 $\mathcal F_t$ 保留因果窗口内的全部 wrench 样本。令 $t_i$ 和 $\bar t_i$ 分别表示查询 $i$ 的启动时刻和结果可用时刻，则当前有效的 reference index 为

$$
k(t)=\arg\max_{i:\bar t_i\leq t}t_i.
$$

所选 reference update 提供缓存 intent、当前 reference sample 和时序特征；其带时间戳的动作块用于构造逐步陈旧性目标。该调度确定训练样本中的 reference age、插值相位和 reference-query latency。

采用归一化时序权重 $w_j\propto\beta^{j-1}$（$0<\beta\leq1$）和归一化坐标均方损失 $\ell_{\mathrm{pose}}$，蒸馏目标为

$$
\mathcal L_{\mathrm{distill}}=\sum_{j=1}^{K}w_j\left[
\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{force}}_{t,j},\Delta A^{\mathrm{force}}_{T,t|k,j})
+\lambda_{\mathrm{stale}}\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{stale}}_{t,j},\Delta A^{\mathrm{stale}}_{T,k\rightarrow t,j})
\right].
$$

$\lambda_{\mathrm{stale}}$ 用于平衡力条件监督与陈旧性监督。

### 3.5 独立残差执行

Reference worker 与 residual worker 相互独立运行。reference 查询完成后，worker 发布一个带时间戳的 reference update，其中包含力输入缺失动作块、缓存 intent 和查询状态。每个 residual 查询读取当前缓存的 reference 以及当前状态和因果 wrench 历史。其带时间戳的 $K$ 步修正块始终与预测时使用的 reference update 绑定，因此新发布的 reference 只供随后启动的 residual 查询使用。

执行时跳过已经过期的修正步骤，其余步骤不经插值而依次执行。reference 位姿通过时间戳插值得到。每次残差查询根据当前状态与所绑定 reference 的查询状态计算 $\Gamma(S_t,S_k)$。力残差独立限幅，$\Delta\hat A^{\mathrm{stale}}+\Gamma(S_t,S_k)$ 的和作为陈旧性修正整体限幅；两者在归一化 delta-action 坐标中与 reference 组合，再根据 reference 查询状态转换为绝对机器人指令。

## 4. 实验

### 4.1 实验设置

#### 硬件、任务与数据采集

实验包括两个平台上的 5 项单臂任务和 4 项双臂任务（见任务表）。单臂平台采用 7 自由度 Franka Emika Panda，配备腕部和外部 Intel RealSense D405 相机。示范通过 3Dconnexion SpaceMouse 遥操作采集。双臂实验采用 X Square Robot 平台，每条机械臂配备一个腕部相机，并设置一个共享外部相机。示范通过 Leader--Follower 遥操作采集。与 ForceVLA [1] 一致，两个平台均使用机器人自身提供的 wrench 估计，不额外安装腕部力/力矩传感器。

每项任务均采集 200 条示范。多视角图像、机器人状态和命令动作以 30 Hz 记录，带时间戳的 wrench 估计以 100 Hz 记录。

| 平台 | 任务 | 交互形式 |
|---|---|---|
| 单臂 | 翻物体（Object Flipping） | 借助表面接触完成重定向 |
| 单臂 | 插 USB（USB Insertion） | 精密插入 |
| 单臂 | 开柜门（Cabinet Opening） | 摩擦主导的关节约束运动 |
| 单臂 | 按按钮（Button Pressing） | 离散接触驱动 |
| 单臂 | 擦白板（Whiteboard Wiping） | 持续表面接触 |
| 双臂 | 拔插头（Plug Removal） | 双臂协调的受约束拔出 |
| 双臂 | 插插头（Plug Insertion） | 双臂协调对准与插入 |
| 双臂 | 拉抽屉（Drawer Opening） | 双臂协调的摩擦主导运动 |
| 双臂 | 擦白板（Whiteboard Wiping） | 固定白板并执行擦除 |

#### 基线与评估协议

本文选择互补的基线进行比较。$\pi_{0.5}$ 代表不显式使用力反馈的通用 VLA；ForceVLA 同时也是本文 teacher 的基础，构成直接的力感知完整动作基线；TA-VLA 代表另一种 torque-aware VLA 设计；ImplicitRDP 则提供 VLA 系列之外的端到端 slow--fast 视觉—力对照。Temporal Teacher 作为受控对照，直接 rollout 本文第一阶段 teacher，用于评估相同时序力条件基础上的残差框架收益。快速完整动作生成与 reference-anchored residual prediction 之间的受控比较，由消融实验中架构匹配的 Fast full-action distillation 提供。每种方法在每项任务上进行 20 次评估。指标包括成功率、峰值接触力和完成时间。对于每个成功试验，单臂峰值力按 $\max_t\|\mathbf f(t)\|_2$ 计算，双臂峰值力按 $\max_{t,a\in\{L,R\}}\|\mathbf f_a(t)\|_2$ 计算；完成时间同样仅基于成功试验统计。峰值力与完成时间按平台汇总成功试验，因此各任务的权重取决于相应方法在该任务上的成功次数。

#### 实现细节

两个平台均以 100 Hz 向带比例—微分反馈的笛卡尔阻抗控制器发送组合位姿命令。时序力条件 teacher 使用 8 张 NVIDIA A100 GPU 进行全参数微调。真实机器人部署使用一台配备 2 张 24 GB NVIDIA RTX 4090 GPU 的工作站，reference 与 residual worker 分别在一张 GPU 上并发运行。

### 4.2 主要结果

#### 定性结果

在该代表性按按钮轨迹中，平移修正在持续接触前较小，按压时增大，在接触力峰值附近出现瞬态，并在释放时减小（见图 5）。

**图 5：Button Pressing 的代表性 rollout。** 4 个交互阶段的外部相机与腕部相机观测，与机器人估计的末端力分量和平移残差幅值在时间上对齐；竖线标出所选图像帧。

#### 结果分析

ForceDelta-VLA 的平均成功率为 82.2%，ForceVLA 为 54.4%（表 II）。提升覆盖九项任务，在 USB Insertion、Button Pressing 和 Plug Insertion 上最大，这三项任务均要求完成局部接触操作。相较 Temporal Teacher，USB Insertion 和 Plug Insertion 分别提升 25 和 20 个百分点，而 Cabinet Opening、Button Pressing、单臂 Whiteboard Wiping 和 Plug Removal 均提升 5 个百分点。因此，在本次评估中，残差框架在两项插入任务上带来的额外收益最大；Button Pressing 相较 ForceVLA 的提升则主要已由 Temporal Teacher 获得。

相较 ForceVLA，单臂和双臂平台汇总的成功试验平均峰值力分别减少 4.2 N 和 4.3 N，降幅均约为 26%（表 III）；完成时间分别缩短 5.2 s 和 5.9 s。ImplicitRDP 的这两项条件统计指标也优于 ForceVLA，但其成功率仅为 45.0%。ForceDelta-VLA 相较 ImplicitRDP 的成功率提升 37.2 个百分点，同时降低峰值力和完成时间。

**表 II：每种方法、每项任务评估 20 次的成功率（%）；平均值为九项任务的均值。**

| 方法 | 翻物体 | 插 USB | 开柜门 | 按按钮 | 单臂擦白板 | 拔插头 | 插插头 | 拉抽屉 | 双臂擦白板 | 平均值 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| $\pi_{0.5}$ | 55 | 35 | 50 | 45 | 70 | 40 | 30 | 50 | 50 | 47.2 |
| ForceVLA | 65 | 40 | 60 | 50 | 70 | 50 | 35 | 60 | 60 | 54.4 |
| TA-VLA | 65 | 35 | 60 | 60 | 75 | 55 | 40 | 60 | 65 | 57.2 |
| ImplicitRDP | 50 | 25 | 45 | 35 | 65 | 55 | 30 | 55 | 45 | 45.0 |
| Temporal Teacher | 75 | 55 | 75 | 80 | 85 | 75 | 50 | 70 | 70 | 70.6 |
| **ForceDelta-VLA (Ours)** | **90** | **80** | **80** | **85** | **90** | **80** | **70** | **80** | **85** | **82.2** |

**表 III：峰值力与完成时间。** 峰值力与完成时间均基于各平台内跨任务汇集的成功试验统计；数值为均值 $\pm$ 标准差。

| 方法 | 单臂峰值力（N）↓ | 双臂峰值力（N）↓ | 单臂完成时间（s）↓ | 双臂完成时间（s）↓ |
|---|---:|---:|---:|---:|
| $\pi_{0.5}$ | 18.9 $\pm$ 8.3 | 20.4 $\pm$ 9.1 | 30.4 $\pm$ 11.2 | 33.1 $\pm$ 12.8 |
| ForceVLA | 16.0 $\pm$ 4.7 | 16.8 $\pm$ 6.2 | 29.1 $\pm$ 6.5 | 31.7 $\pm$ 9.4 |
| TA-VLA | 16.5 $\pm$ 6.4 | 15.9 $\pm$ 4.8 | 28.2 $\pm$ 9.7 | 30.0 $\pm$ 7.1 |
| ImplicitRDP | 15.2 $\pm$ 5.1 | 14.8 $\pm$ 4.9 | 27.1 $\pm$ 7.2 | 27.4 $\pm$ 7.8 |
| Temporal Teacher | 15.1 $\pm$ 5.8 | 14.7 $\pm$ 4.3 | 27.8 $\pm$ 8.8 | 29.2 $\pm$ 6.9 |
| **ForceDelta-VLA (Ours)** | $\mathbf{11.8}\pm 3.4$ | $\mathbf{12.5}\pm 2.9$ | $\mathbf{23.9}\pm 5.4$ | $\mathbf{25.8}\pm 7.6$ |

#### 定性失败模式

对 ForceDelta-VLA 失败轨迹的检查显示三种观察到的局限。第一，指向可行接触区域之外的 reference 可能使轨迹超出有界 residual 的修正范围。第二，reference age 超过训练调度的覆盖范围时，缓存的局部规划可能与持续变化的机器人—环境状态不一致。第三，卡滞或错误侧接触可能需要回退、重新接近或策略切换等非局部恢复，这些行为可能超出有界局部修正的范围。

### 4.3 独立残差执行

我们测量 reference 与 residual 通路的模型前向延迟，并评估额外 reference-availability delay 下的任务性能。

#### 推理耗时分离

Residual 前向耗时为 2.43 ms，低于 Cartesian target updates 之间的 10 ms 间隔（见表 IV），两个生成式通路均超过该间隔。这种计算耗时差异使 residual worker 可以复用缓存 reference，而无需在每次修正查询时等待新的生成式预测。

**表 IV：部署工作站上按按钮任务的 batch-one 前向耗时，报告重复测量的均值 ± 标准差。**

| 方法/路径 | 前向延迟（ms）↓ |
|---|---:|
| ImplicitRDP | 12.86 $\pm$ 0.23 |
| Temporal Teacher | 176.4 $\pm$ 5.9 |
| ForceDelta reference | 189.7 $\pm$ 6.8 |
| ForceDelta residual | **2.43** $\pm$ 0.12 |

#### Reference 延迟敏感性

我们在 USB 插入和插头插入任务上施加 0、100 和 200 ms 的额外 reference-availability delay；每个任务、方法和延迟条件测试 10 次。随着延迟增加，ForceDelta-VLA 的成功率降幅和平均峰值力增幅均小于 Temporal Teacher（见图 6），与残差通路在延迟 reference 更新之间仍发挥作用的预期一致。性能仍随 reference age 增大而下降，表明近期力反馈并未消除对 reference 新鲜度的依赖。

**图 6：两项插入任务对额外 reference-availability delay 的敏感性。** 左：成功率；右：峰值力。蓝色实线与圆形标记表示 ForceDelta-VLA，灰色虚线与方形标记表示 Temporal Teacher。

### 4.4 消融实验

我们将消融分为两组（见表 V）。**执行/组件消融**保持已训练模型不变：**Reference only** 不使用 learned corrections 或 state rebasing；**w/o force residual** 关闭 force output；**w/o learned staleness residual** 保留 $\Gamma(S_t,S_k)$，只关闭 learned staleness output；**Zeroed residual input** 将实测力历史替换为全零张量，同时保留 force branch。

**监督/分解消融**重新训练受影响的变体：**Demonstration-derived residual target** 将同一查询状态下的示范动作减去力输入缺失模式的 teacher 预测；**Zero-wrench teacher query** 在构造 reference 和监督目标时，以输入全零 wrench 历史的 teacher 查询替代学习式力输入缺失模式；**w/o schedule replay** 在 residual 训练中不采样异步查询周期和 reference-query latency schedule；**Single combined-residual head** 在保留 deterministic rebasing 的情况下，使用一个 head 预测两个 learned targets 之和；**Fast full-action distillation** 保持相同的小型 student 输入与更新调度，但直接蒸馏冻结 teacher 的力条件位姿动作。

**表 V：USB Insertion、单臂 Whiteboard Wiping、Plug Insertion 和 Drawer Opening 上的消融，按平台分组。峰值力报告均值 ± 标准差。**

| 变体 | 单臂成功率（%）↑ | 双臂成功率（%）↑ | 单臂峰值力（N）↓ | 双臂峰值力（N）↓ |
|---|---:|---:|---:|---:|
| *执行/组件消融* |  |  |  |  |
| Reference only | 62.5 | 50.0 | 16.7 $\pm$ 6.9 | 18.2 $\pm$ 7.6 |
| w/o force residual | 70.0 | 57.5 | 15.4 $\pm$ 5.8 | 17.1 $\pm$ 6.3 |
| w/o learned staleness residual | 77.5 | 67.5 | 13.2 $\pm$ 4.1 | 14.5 $\pm$ 5.0 |
| Zeroed residual input | 65.0 | 55.0 | 15.9 $\pm$ 6.4 | 17.7 $\pm$ 7.0 |
| **ForceDelta-VLA (full)** | **85.0** | **75.0** | $\mathbf{11.7}\pm 3.1$ | $\mathbf{12.8}\pm 3.8$ |
| *监督/分解消融* |  |  |  |  |
| Demonstration-derived residual target | 72.5 | 57.5 | 14.6 $\pm$ 5.2 | 16.5 $\pm$ 6.1 |
| Zero-wrench teacher query | 65.0 | 55.0 | 16.1 $\pm$ 6.7 | 17.3 $\pm$ 6.5 |
| w/o schedule replay | 77.5 | 62.5 | 13.8 $\pm$ 4.7 | 15.6 $\pm$ 5.8 |
| Single combined-residual head | 75.0 | 65.0 | 13.5 $\pm$ 4.3 | 14.9 $\pm$ 4.9 |
| Fast full-action distillation | 67.5 | 52.5 | 15.2 $\pm$ 5.6 | 17.9 $\pm$ 7.4 |
| **ForceDelta-VLA (full)** | **85.0** | **75.0** | $\mathbf{11.7}\pm 3.1$ | $\mathbf{12.8}\pm 3.8$ |

关闭力残差后，单臂和双臂任务子集的成功率分别下降 15 和 17.5 个百分点，峰值力分别增加 3.7 N 和 4.3 N（表 V）。关闭学习式陈旧性残差后，即使保留 $\Gamma(S_t,S_k)$，两个子集的成功率仍均下降 7.5 个百分点。因此，解析坐标重定位不能恢复学习式缓存参考修正的作用。将 wrench 输入置零比关闭力输出的表现更差：前者仍计算力分支并施加其预测，因此不属于移除分支的对照。

在 student 架构、输入和更新调度一致的条件下，快速完整动作蒸馏的成功率比残差预测分别低 17.5 和 22.5 个百分点。这一比较支持围绕缓存 reference 预测修正，而非将完整 teacher 动作压缩到相同 student。将 teacher 差分目标替换为示范导出目标也使两个子集的成功率下降；该替换将成对 teacher 预测之间的力偏差改为示范与 teacher 之间的偏差。将两个学习目标合并到一个 head 后，即使保留目标之和及解析重定位，成功率仍下降。省略 schedule replay 在双臂子集上造成更大的成功率损失，表明训练调度与异步部署下的表现有关。Zero-wrench 查询消融同时改变 reference 和 target 构造，因此其性能下降评估的是这两项替换的共同影响。

### 4.5 未见物理条件下的泛化

针对图 7 所示任务，我们分别将训练物体替换为未见的 USB port、纸盒，以及白板和板擦。

**图 7：泛化评估。从左到右：USB Insertion、Object Flipping 和双臂 Whiteboard Wiping。**

**表 VI：未见物理条件下的泛化结果。峰值力报告均值 ± 标准差。**

| 方法 | 成功率（%）↑ | 峰值力（N）↓ |
|---|---:|---:|
| ImplicitRDP | 30.0 | 45.9 ± 10.5 |
| Temporal Teacher | 70.0 | 21.8 ± 5.2 |
| **ForceDelta-VLA (Ours)** | 80.0 | 13.6 ± 3.7 |

在物体替换条件下，ForceDelta-VLA 的成功率比 Temporal Teacher 高 10 个百分点，成功试验峰值力由 21.8 N 降至 13.6 N（表 VI）。残差框架的提升因此在未见物体上仍有体现。由于每次替换同时改变外观和接触属性，该实验评估的是物体层面的迁移，而非对单一物理参数的敏感性。

## 5. 结论与未来工作

ForceDelta-VLA 从缓存上下文下的 teacher 预测构造力残差与陈旧性残差目标，并使蒸馏后的修正独立于生成式 reference 执行。解析状态重定位将缓存 reference 与当前机器人状态对齐。在九项真实机器人任务上，本方法达到 82.2% 的平均成功率，比 ForceVLA 高 27.8 个百分点。相较 ForceVLA，两个平台的成功试验平均峰值接触力均降低约 26%，同时在所评估方法中取得最低的成功试验平均完成时间。Residual 前向耗时为 2.43 ms。消融中，替换 teacher 差分监督、合并残差 head 或省略 schedule replay 均使成功率下降。

当不准确的 reference 或所需恢复动作超出有界修正范围时，性能受到限制。快速通路也依赖缓存视觉上下文在一个 reference interval 内局部有效。依赖机器人估计的 wrench 还可能限制对微弱接触的敏感性。未来工作包括置信度触发的 reference refresh、轻量级当前视觉更新、更丰富的接触感知，以及利用纠正性 rollout 训练的恢复策略。
