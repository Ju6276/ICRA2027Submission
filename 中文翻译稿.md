# ForceDelta-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力反馈能够揭示图像中不可观测的接触动态，但现有力感知视觉—语言—动作（VLA）策略每次更新物理反馈时仍需重新调用完整动作生成器。我们提出 ForceDelta-VLA，以**反事实残差蒸馏**取代反应式完整动作预测。

时序力感知教师训练完成并冻结后，我们对相同的视觉观测、指令和机器人状态进行两次查询：事实查询使用实测力历史，反事实查询使用经过适配的空力模式。两次查询共享视觉—语言前缀和初始流噪声，其位姿差异为力响应修正提供显式目标。空力模式预测作为参考，轻量级因果学生则结合带时间戳的力历史、缓存任务上下文、机器人状态和当前参考，只预测位姿残差。

我们在八项接触密集型单臂和双臂任务上评估 ForceDelta-VLA，报告任务成功率、峰值接触力和任务完成时间。


## 1. 引言

视觉—语言—动作（VLA）模型为机器人操作提供了语义泛化能力，但接触密集型任务要求反馈同时具备物理信息与时间响应性。视觉观测可能无法揭示接触开始、滑移、冲击或受约束运动，而力/力矩测量能够直接表征这些交互状态。因此，力感知 VLA 将力/力矩反馈作为动作生成的条件。然而，即使物理交互只需要局部修正，引入一条新观测到的力测量仍需再次预测完整动作。

近期的慢—快架构在力响应路径中复用视觉—语言上下文。但快速路径仍然是完整动作预测器：每次更新都会把慢速策略已经表示的标称运动，与力引起的变化一并重新合成。因此，缓存前缀避免了重复的视觉—语言编码，却没有避免动作生成；而仅靠频率分离，也不能规定动作中的哪一部分应当归属反应式分支。

残差形式通过组合慢速更新的参考与力条件修正来分离这两种功能。其核心问题是如何从离线示范中辨识修正量：每条示范只监督执行动作，并不提供参考分量与残差分量的分解。直接行为克隆因而允许退化分解，例如慢速分支保留接触相关行为，或快速分支重复生成标称策略。要使反应式路径专门学习修正，需要显式的残差目标。

为此，我们提出**反事实残差蒸馏**。力感知 VLA 教师训练完成后被冻结，并在同一观测上接受两次查询。成对查询共享图像、指令、机器人状态、视觉—语言前缀和初始流噪声；一次使用实测力历史，另一次使用适配后的空力模式。两次预测的位姿差异定义修正目标，空力模式预测则作为参考。紧凑的因果学生从最新力历史、缓存任务上下文、机器人状态和当前参考中蒸馏该目标，从而异步生成修正，而无需重新运行生成式动作专家。

本文贡献如下：

1. 提出**反事实残差蒸馏**：一种离线学习目标，可直接从预训练力感知 VLA 中导出显式的动作空间修正目标，无需任务奖励或额外采集纠正示范；
2. 构建**学习式空力参考模式**：联合优化空力 token 和模式专用适配器，并通过共享视觉—语言前缀和初始流噪声耦合事实力与空力预测，从而获得稳定的模型级条件差异；
3. 提出**慢—快策略分解**：VLA 教师负责生成语义参考动作，紧凑的残差策略则异步预测力条件位姿修正，无需重复执行生成式动作推理。


## 2. 相关工作

### 2.1 力感知视觉—语言—动作策略

力/力矩反馈能够揭示图像中可能含糊的接触开始、载荷和受约束运动。ForceVLA 通过力感知混合专家融合模块引入六轴力/力矩测量；ForceVLA2 将阶段级力提示、直接力通路和混合力—位置控制相结合；TA-VLA 则研究时序力矩特征应在预训练 VLA 解码器的何处注入。这些方法在融合深度和控制接口上有所不同，但都利用物理反馈来调节完整动作块的生成。

这种形式增强了策略的接触感知能力，却没有将缓慢变化的任务规划与新力观测引起的修正分离。因此，每次力更新仍与动作生成路径耦合，示范数据也没有为动作中的修正分量提供显式标签。

### 2.2 慢—快力响应控制

慢—快策略根据语义感知和物理交互的不同时间尺度重新组织计算。FAVLA 缓存慢速视觉—语言上下文，重复运行力条件动作专家，并根据预测的力变化调节其更新频率。LIFT 同样复用视觉—语言前缀，但将近期力记忆注入使用在线纠正训练的反应式动作流。DAM-VLA 则维护按各模态原生频率更新的独立潜变量缓存，由动作专家异步读取。

相关的非 VLA 策略也在视觉运动控制层面处理这一频率失配。ManipForce 从异步 RGB 与力/力矩数据流中学习频率感知表示，ImplicitRDP 则在视觉—力扩散策略中引入因果慢—快结构。这些方法保留了高频物理反馈，但反应式路径仍然预测完整动作；仅仅分离更新频率并不能确定动作中的哪一部分应交给快速修正。

### 2.3 残差学习与条件蒸馏

残差策略保留基础控制器并学习加性修正，但需要一种信号将修正与完整执行动作区分开来。ResiP 从任务奖励中获得这一信号，在冻结的动作分块模仿策略之上使用强化学习训练闭环残差。Compliant Residual DAgger 则收集在线动觉增量纠正，并利用这些额外示范训练力条件残差。两类方法都依赖原始离线行为克隆标签之外的信息来定义残差。

条件建模为构造这种监督提供了另一条路径。学习式缺失模态 token 能够区分不可用观测与具有物理意义的零值。在生成模型中，成对的条件预测与无条件预测被用于分离条件对生成结果的影响，无分类器引导蒸馏进一步将这种成对计算迁移到成本更低的预测器中。这些思想为直接从预训练条件策略中导出动作空间修正提供了基础，而无需奖励优化或重新采集纠正示范。

## 3. 方法

### 3.1 问题定义

给定一个由遥操作采集的接触密集型操作轨迹数据集 $\mathcal D$。在时刻 $t$，每条轨迹包含视觉观测 $V_t$、语言指令 $L$、机器人状态 $S_t$、带时间戳的力/力矩历史 $\mathcal F_t$，以及示范动作 $A_t^*$。动作表示为

$$
A=[p\in\mathbb{R}^3;\ r\in\mathbb{R}^6;\ g\in\mathbb{R}]\in\mathbb{R}^{10},
$$

其中 $p$ 为平移，$r$ 为连续六维旋转表示，$g$ 为夹爪指令，$A^p=[p;r]\in\mathbb{R}^9$ 为位姿分量。

令 $T_\theta$ 表示在 $\mathcal D$ 上训练、用于预测长度为 $H$ 的动作块的力条件 VLA。我们的目标是从 $T_\theta$ 导出一个包含两个分量的分解策略：参考动作 $A^{\mathrm{ref}}$ 表示任务层运动，残差策略 $\pi_\psi^\Delta$ 根据最新力历史和缓存任务上下文预测位姿修正。具体为

$$
\Delta\hat A(t)=\pi_\psi^\Delta(\mathcal F_t,S_t,A^{\mathrm{ref}}(t),Z^{\mathrm{intent}}),
$$

$$
(A^{\mathrm{cmd}}(t))^p=(A^{\mathrm{ref}}(t))^p+\Delta\hat A(t),\qquad
g^{\mathrm{cmd}}(t)=g^{\mathrm{ref}}(t),
$$

示范只监督 $A_t^*$，并不包含 $A^{\mathrm{ref}}$ 或残差修正的标签。因此，本文需要解决的学习问题是：如何利用 $\mathcal D$ 和训练完成的教师构造修正目标，并在不使用任务奖励或额外纠正示范的情况下学习 $\pi_\psi^\Delta$。

ForceDelta-VLA 通过图 2 所示的三阶段流程解决这一问题。首先训练时序力条件教师；随后冻结教师并学习空力参考模式；最后冻结教师的两种模式，构造成对残差目标，并将其蒸馏到紧凑的残差策略中。

### 3.2 时序力条件教师

教师以 ForceVLA 的流匹配策略为基础，将瞬时力输入替换为因果时序编码器。在每个教师推理时刻 $t_k$，编码器从带时间戳的原生频率数据流中读取固定时长窗口

$$
\mathcal F_k=\{F(\tau)\mid t_k-w\leq\tau\leq t_k\},
$$

同时使用有效性 mask 标记轨迹开始处尚不可用的采样。因果时序卷积网络把该窗口编码为

$$
z_k^F=E_T^F(\mathcal F_k),
$$

它在原有融合模块中替换瞬时力嵌入，动作生成通路保持不变。教师在示范上全量微调，其事实预测为

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F;\epsilon)\in\mathbb{R}^{H\times10}.
$$

其中 $\epsilon$ 为流匹配动作生成器的初始噪声。此阶段完成后，$\theta$ 保持冻结。

### 3.3 学习空力参考模式

简单地把力设为零并不适合定义参考：零是具有物理意义的测量，不能明确表示力条件缺失。为在不提供当前力历史的情况下查询同一教师，我们用可学习 token $z_{\varnothing F}\in\mathbb{R}^{D_F}$ 替换 $z_k^F$。该 token 在样本间共享，因此表示缺少实例特定的力历史，而不是某一次物理零力测量。

空力模式由学习式 token 和轻量级空力专用适配器共同组成。这两个组件并不相同，但在第二阶段联合训练：token 表示条件缺失，适配器则恢复该条件下的动作预测能力。记 $\phi=\{z_{\varnothing F},W_{\mathrm{in}},W_{\mathrm{out}}\}$。保持教师参数 $\theta$ 冻结，rank-$\rho$ 适配器只修正流速度的位姿通道：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h),
$$

其中 $h$ 为动作专家隐状态，$\sigma$ 为 SiLU。保持 $\theta$ 固定，使用教师的流匹配目标优化 $\phi$：

$$
\mathcal L_{\mathrm{null}}
=\mathbb E_{\mathcal D,\tau,\epsilon}
\left[\ell_{\mathrm{FM}}\!\left(
T_{\theta,\phi}^{\mathrm{null}},A_{k:k+H-1}^{*};\tau,\epsilon\right)\right].
$$

适配器仅在空力模式中启用；事实力模式仍使用训练完成的教师参数与计算。所得空力模式在部署时作为缓慢更新的参考策略。

### 3.4 反事实残差蒸馏

#### 成对目标构造

由于教师采用流匹配，两次独立采样会包含与条件模式无关的变化。因此，对每个训练样本复用视觉—语言前缀，并使用相同的初始噪声 $\epsilon$：

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F;\epsilon),
$$

$$
A_{T,k}^{\mathrm{null}}=T_{\theta,\phi}^{\mathrm{null}}(V_k,L,S_k,z_{\varnothing F};\epsilon),
\quad \epsilon\sim\mathcal N(0,I).
$$

参考目标为

$$
A_{T,k}^{\mathrm{ref}}=A_{T,k}^{\mathrm{null}},
$$

位姿通道上的**反事实残差**为

$$
\Delta A_{T,k}^{\mathrm{cf}}
=\left(A_{T,k}^{\mathrm{full}}\right)^p
-\left(A_{T,k}^{\mathrm{null}}\right)^p
\in\mathbb{R}^{H\times9}.
$$

这里的“反事实”指同一教师在匹配观测和采样噪声下两种运行模式的比较。它是模型层面的条件差异，而不是接触效应的因果归因：空力模式包含适配参数，视觉和机器人状态本身也可能包含接触信息。

#### 快速残差策略

每次慢速更新时，空力模式教师发布参考动作块 $A_{T,k}^{\mathrm{null}}$ 和紧凑的任务表示。给定视觉—语言前缀 $H_k\in\mathbb{R}^{S\times d}$ 及其 mask $m_k$，我们将连续 token 组池化为 $M$ 个意图 token：

$$
Z_k^{\mathrm{intent}}=\mathrm{Pool}_M(H_k,m_k)\in\mathbb{R}^{M\times d}.
$$

该表示使残差策略无需运行第二个视觉—语言骨干，即可使用教师的任务上下文。在当前控制时刻 $t$，因果 TCN 将最新力窗口编码为 $Z_t^F$。残差策略接收

$$
X_t=[\widetilde Z_k^{\mathrm{intent}};Z_t^F;P_S(S_t);P_A(A^{\mathrm{ref}}(t));q],
$$

其中 $P_S$ 和 $P_A$ 为学习式投影，$q$ 为学习式输出查询，类型嵌入用于区分各输入组。紧凑的注意力模块将这些条件映射为短位姿修正块：

$$
\Delta\hat A_{t,1:K}=W_{\mathrm{res}}H_t[q]\in\mathbb{R}^{K\times9}.
$$

$W_{\mathrm{res}}$ 采用零初始化，因此学生初始预测零修正，不改变参考动作。

#### 蒸馏目标

令 $w_j\propto\beta^{j-1}$ 为成对目标前 $K$ 步上的归一化权重，$d$ 为位姿空间回归损失，我们最小化

$$
\mathcal L_{\mathrm{distill}}=\sum_{j=1}^{K}w_jd(\Delta\hat A_{k,j},\Delta A_{T,k,j}^{\mathrm{cf}}).
$$

递减权重使动作块中更早执行的修正获得更高权重。

### 3.5 异步部署

部署时，空力模式教师周期性更新参考动作块和意图 token。在相邻两次更新之间，残差策略读取最新的力历史与机器人状态，通过一次确定性前向预测短修正块，并将其与插值得到的参考位姿组合。因此，语义动作生成保留在慢速分支，而力条件位姿修正无需重新运行流匹配动作生成器即可更新。

## 4. 实验

实验围绕三个问题展开：所提出的策略分解能否相较无力输入、力条件和慢—快基线提升任务性能；学习得到的残差能否改善接触过程中的物理交互；以及成对目标、时序力表示和空力模式构造分别对最终性能有何贡献。

### 4.1 实验设置

#### 硬件平台

单臂平台由一台七自由度 Franka Emika Panda 机械臂和两台 Intel RealSense D405 相机构成。协调操作任务使用独立的双臂实验平台。

#### 任务与数据采集

单臂平台执行按按钮、翻转物体、插入 USB 和打开柜门，双臂平台执行插入插头、拔出插头、擦白板和打开抽屉。每项单臂和双臂任务分别采集 200 条和 300 条遥操作示范。每条轨迹以 30 Hz 记录同步的多视角图像、机器人状态和命令动作，同时以 100 Hz 记录带时间戳的六轴力/力矩测量。

| 类型 | 任务 | 交互状态 |
|---|---|---|
| 单臂 | 按按钮 | 建立接触、力阈值 |
| 单臂 | 翻转物体 | 贴靠表面的接触重构 |
| 单臂 | 插入 USB | 几何约束下的精密插入 |
| 单臂 | 打开柜门 | 关节机构约束运动 |
| 双臂 | 插入插头 | 精密插入、力闭合 |
| 双臂 | 拔出插头 | 几何约束下拔出 |
| 双臂 | 擦白板 | 对另一只手持表面的持续接触 |
| 双臂 | 打开抽屉 | 双臂协调约束运动 |

### 4.2 基线、指标与实现细节

对比方法包括：不使用力的 $\pi_{0.5}$、使用瞬时力融合的 ForceVLA、在解码器中引入力矩历史的 TA-VLA，以及采用结构化慢—快处理的视觉—力扩散策略 ImplicitRDP。

主要评估指标为任务成功率。对于每种方法和每项任务，我们评估两个训练 seed，每个 seed 测试 10 次，报告各项任务的成功率，以及四项单臂任务和四项双臂任务分别的平均值。此外，我们在所有试验上报告峰值接触力，并在成功试验上报告任务完成时间。峰值接触力定义为成功或终止前记录到的最大力幅值；两项交互指标均按单臂与双臂任务组分别取平均。

所有模型均使用两张 NVIDIA A100 GPU，以 bfloat16 精度训练。教师采用 AdamW 全量微调 50,000 步，全局 batch size 为 32，峰值学习率为 $2\times10^{-5}$，weight decay 为 0.01，使用 1,000 步 warmup、cosine decay 和 1.0 的梯度裁剪。教师以 30 Hz 预测长度为 $H=50$ 的动作块，并编码 100 Hz 力/力矩数据流中最近 100 ms 的测量；流匹配推理使用 $N_{\mathrm{flow}}=10$ 步。

随后冻结教师，使用 $10^{-4}$ 的学习率联合训练空力 token 和 rank-32 空力专用适配器 10,000 步。视觉—语言前缀池化为 $M=16$ 个 token。残差学生包含一个 pre-norm attention block，并预测 $K=5$ 步修正；学生使用 AdamW 训练 30,000 步，全局 batch size 为 128，学习率为 $3\times10^{-4}$，并设置 $\beta=0.5$。

### 4.3 结果

#### 定量结果

主表比较 $\pi_{0.5}$、ForceVLA、TA-VLA、ImplicitRDP 和 ForceDelta-VLA 在八项任务上的成功率，并分别汇总单臂与双臂任务的平均成功率。第二张表在所有试验上报告峰值接触力，并在成功试验上报告任务完成时间；两项指标均按单臂与双臂分组，数值越低越好。

#### 定性结果

我们展示两个平台上具有代表性的成功与失败轨迹。每段图像序列与实测力/力矩曲线、参考动作和预测残差对齐，以呈现残差策略在建立接触、受约束运动和恢复过程中何时修正参考动作。

#### 结果分析

结果分析从两个方面展开。第一，对比 USB 和插头插入等精密接触任务，与柜门和抽屉开启等关节机构运动任务上的性能变化。第二，将峰值力和完成时间的变化，与接触事件附近预测残差的时刻和幅值联系起来。该分析用于判断力条件修正在何处提供了超出参考策略的作用。

#### 失败分析

我们将失败试验划分为三类：参考策略继承的感知或任务选择错误、超出连续位姿修正范围的接触偏差，以及需要离散策略切换的交互。对于每一类失败，我们按方法报告其出现频率，并展示一条代表性轨迹。第一类用于检验参考动作本身错误时残差学习的能力边界；后两类用于揭示加性修正模型的局限。

### 4.4 消融实验

消融包括：

1. **预测目标：**比较预测事实教师完整动作的快速策略、以 $(A_k^*)^p-(A_{T,k}^{\mathrm{null}})^p$ 为监督的残差策略，以及使用成对教师残差目标训练的完整方法；
2. **力表示：**比较瞬时力、降采样至 30 Hz 的力历史，以及原生 100 Hz 因果力历史；
3. **反事实构造：**比较零力输入、学习式空力 token、带空力适配器的学习式 token，以及独立流噪声与共享流噪声。

## 5. 结论

本文提出 ForceDelta-VLA：一种以反事实残差蒸馏取代反应式完整动作生成的力感知 VLA。冻结教师在事实力模式与适配空力模式下的成对预测定义残差目标，空力模式预测同时作为参考。轻量级因果学生根据带时间戳的力历史、缓存任务上下文、机器人状态和当前参考预测位姿修正。这一分解将反应式路径从迭代生成策略转化为确定性残差预测器。

ForceDelta-VLA 中的反事实定义在模型层面，而不是因果干预。空力查询包含学习得到的参数，视觉与机器人状态也保留接触线索；因此，所得残差不能被解释为可唯一归因于力的贡献。离线蒸馏还会继承教师误差，并受到示范数据覆盖范围的限制。此外，加性位姿残差无法表示离散的操作策略切换。

未来工作将引入少量在线修正，为残差策略扩展离散恢复决策，并针对存在推理延迟的异步组合建立稳定性与安全性保证。
