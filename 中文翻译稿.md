# ForceDelta-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力反馈能够揭示图像中不可观测的接触动态，但力感知视觉—语言—动作（VLA）策略的反应式路径仍需生成完整动作块，因而更新成本较高。我们提出 ForceDelta-VLA，以**反事实残差蒸馏**取代完整动作的重复生成。

对于相同的视觉观测、指令和机器人状态，我们对冻结的时序力感知教师进行两次查询：事实查询使用实测力历史，反事实查询使用经过适配的空力条件。两次查询共享视觉—语言前缀和初始流噪声，其动作空间差异为力响应修正提供显式目标。反事实预测作为慢速参考，轻量级因果学生则结合原生频率力历史、缓存任务上下文、机器人状态和参考年龄，只更新位姿残差。时序随机化训练使学生接触异步执行中出现的上下文陈旧。

我们在八项单臂和双臂任务上，使用任务成功率、交互安全性、接触恢复、残差保真度和运行时间评估 ForceDelta-VLA，并单独分析反应式路径的计算成本。

<!-- 投稿前：完成机器人实验后，在摘要中加入最主要的任务结果。 -->

## 1. 引言

视觉—语言—动作（VLA）模型为机器人操作提供了语义泛化能力，但接触密集型任务要求反馈同时具备物理信息与时间响应性。视觉观测可能无法揭示接触开始、滑移、冲击或受约束运动，而力/力矩测量能够直接表征这些交互状态。因此，力感知 VLA 将力/力矩反馈作为动作生成的条件。然而，如果每次力更新都需要执行多模态推理和迭代动作生成，其响应频率仍然受到限制。

近期的慢—快架构通过在力响应路径中复用视觉—语言上下文来降低这一成本。不过，快速路径通常仍以重新生成完整动作为训练目标，其中包括已经由慢速策略表示的标称运动。这种耦合使物理反馈的计算成本取决于完整动作生成器，而非所需修正本身的复杂度。

残差形式通过组合慢速更新的参考与力条件修正来分离这两种功能。其核心问题是如何从离线示范中辨识修正量：每条示范只监督执行动作，并不提供参考分量与残差分量的分解。直接行为克隆因而允许退化分解，例如慢速分支保留接触相关行为，或快速分支重复生成标称策略。要使反应式路径专门学习修正，需要显式的残差目标。

为此，我们提出**反事实残差蒸馏**。冻结的力感知 VLA 教师接受成对输入：两次查询共享图像、指令、机器人状态、视觉—语言前缀和流噪声，但分别使用实测力历史与适配空力条件。成对预测之差定义修正目标，空力条件预测则提供慢速参考。紧凑的因果学生根据力历史和缓存任务上下文蒸馏该目标，从而无需重新运行生成式动作专家即可异步更新残差。

为适应慢速策略频率和推理延迟的变化，训练时让学生接触随机化的上下文年龄与调度抖动。部署时，教师周期性更新参考和任务上下文，学生则根据最新力测量只更新位姿修正。

本文贡献如下：

1. 提出**反事实残差蒸馏**：将冻结 VLA 教师在事实力条件和空力反事实条件下的成对预测，转化为力响应修正的显式监督信号；
2. 构建**经过适配的空力教师模式**：通过共享视觉—语言前缀和流噪声与事实查询耦合，同时提供受控的反事实基准与慢速参考，而无需训练独立的标称策略；
3. 提出**仅预测残差的力反应式 VLA 架构**：紧凑因果学生只更新位姿修正，从而避免反应式路径中的迭代完整动作生成。

## 2. 相关工作

### 2.1 力感知视觉—语言—动作策略

力/力矩反馈能够揭示图像中可能含糊的接触开始、载荷和受约束运动。ForceVLA 通过力感知混合专家融合模块引入六轴力/力矩测量；ForceVLA2 将阶段级力提示、直接力通路和混合力—位置控制相结合；TA-VLA 则研究时序力矩特征应在预训练 VLA 解码器的何处注入。这些方法在融合深度和控制接口上有所不同，但都利用物理反馈来调节完整动作块的生成。

这种形式增强了策略的接触感知能力，却没有将缓慢变化的任务规划与新力观测引起的修正分离。因此，每次力更新仍与动作生成路径耦合，示范数据也没有为动作中的修正分量提供显式标签。

### 2.2 慢—快力响应控制

慢—快策略根据语义感知和物理交互的不同时间尺度重新组织计算。FAVLA 缓存慢速视觉—语言上下文，重复运行力条件动作专家，并根据预测的力变化调节其更新频率。LIFT 同样复用视觉—语言前缀，但将近期力记忆注入使用在线纠正训练的反应式动作流。DAM-VLA 则维护按各模态原生频率更新的独立潜变量缓存，由动作专家异步读取。

相关的非 VLA 策略也在视觉运动控制层面处理这一频率失配。ManipForce 从异步 RGB 与力/力矩数据流中学习频率感知表示，ImplicitRDP 则在视觉—力扩散策略中引入因果慢—快结构。这些方法保留了高频物理反馈，但反应式路径仍然预测完整动作；仅仅分离更新频率并不能确定动作中的哪一部分应交给快速修正。

### 2.3 残差学习与条件蒸馏

残差策略保留基础控制器并学习加性修正，但需要一种信号将修正与完整执行动作区分开来。ResiP 从任务奖励中获得这一信号，在冻结的动作分块模仿策略之上使用强化学习训练闭环残差。Compliant Residual DAgger 则收集在线动觉增量纠正，并利用这些额外示范训练力条件残差。两类方法都依赖原始离线行为克隆标签之外的信息来定义残差。

条件建模为构造这种监督提供了另一条路径。学习式缺失模态 token 能够区分不可用观测与具有物理意义的零值。在生成模型中，成对的条件预测与无条件预测被用于分离条件对生成结果的影响，无分类器引导蒸馏进一步将这种成对计算迁移到成本更低的预测器中。这些思想为直接从预训练条件策略中导出动作空间修正提供了基础，而无需奖励优化或重新采集纠正示范。

## 3. 方法

### 3.1 问题定义与总体框架

我们研究接触密集型操作，其中策略接收图像 $V$、语言指令 $L$、机器人状态 $S$ 和带时间戳的力/力矩数据流 $F$。训练和部署期间均有力输入，并且力数据可以独立于视觉—语言推理进行更新。我们从一个预训练的力条件 VLA 教师 $T_\theta$ 出发，并在残差蒸馏期间冻结该教师。用于训练教师的示范包含同步观测和执行动作，但没有将每个动作分解为慢速参考与力响应修正的标签。

动作在末端执行器坐标系中表示为

$$
A=[p\in\mathbb{R}^3;\ r\in\mathbb{R}^6;\ g\in\mathbb{R}]\in\mathbb{R}^{10},
$$

其中 $r$ 为连续六维旋转表示，$g$ 为夹爪指令，$A^p=[p;r]\in\mathbb{R}^9$ 表示位姿分量。

令 $t_k$ 为最近一次慢速更新的观测时间，$t$ 为当前控制时刻。控制命令写为

$$
A^{\mathrm{cmd}}(t)=A^{\mathrm{ref}}(t)+\Delta A(t),\qquad
\Delta A(t)\in\mathbb{R}^9.
$$

$A^{\mathrm{ref}}(t)$ 由 $t_k$ 时生成的动作块插值得到，修正量只作用于位姿，夹爪通道保留在慢速分支。这种分解降低了反应式更新所需的计算量：参考动作在较慢的语义时间尺度上变化，并通过插值得到；当新的力测量到达时，只需重新计算 $\Delta A$。然而，示范数据只监督完整动作，并不能确定其中的参考分量和残差分量。对于行为克隆而言，只要 $(A^{\mathrm{ref}},\Delta A)$ 之和等于示范动作，任意一种分解都与训练数据一致。因此，参考分支可能吸收接触相关行为，而残差分支可能重复生成标称运动。为此，我们利用预训练且冻结的教师 $T_\theta$，为 $\Delta A$ 构造显式监督目标。

### 3.2 反事实残差目标构造

#### 力条件教师

教师在 ForceVLA 的 $\pi_0$ 风格流匹配策略上，将瞬时力表示替换为因果时序编码器。在每个教师推理时刻 $t_k$，从原生频率流读取固定时长窗口

$$
\mathcal F_k=\{F(\tau)\mid t_k-w\leq\tau\leq t_k\},
$$

并使用有效性 mask 区分未填满的窗口与真实零力。因果时序卷积网络得到 $z_k^F=E_T^F(\mathcal F_k)$，随后将其投影到原模型的力融合维度。完整模式预测为

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F)\in\mathbb{R}^{H\times10}.
$$

该编码器能够表示接触开始、冲击瞬态、振荡和恢复等短时动态。

#### 学习空力参考

为在不提供当前测量时查询同一教师，我们引入学习式 token $z_{\varnothing F}\in\mathbb{R}^{D_F}$。它表示缺少实例特定的力观测，而不是物理零力。教师权重保持冻结，仅优化该 token 和一个只在空力模式下修正流速度位姿通道的 rank-$\rho$ 适配器：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h).
$$

其训练目标为

$$
\mathcal L_{\varnothing}=d\!\left(T_\theta(V_k,L,S_k,z_{\varnothing F}),A_k^{\mathrm{expert}}\right).
$$

该过程得到同一教师的适配空力模式，而不是单独训练参考策略；部署时，该模式保留为慢速分支。

#### 成对目标提取

流匹配教师的两次独立查询会同时受到条件和采样噪声影响。我们只计算一次视觉—语言前缀及其 key/value cache，并用同一个噪声样本 $\epsilon$ 初始化两次积分：

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F;\epsilon),
$$

$$
A_{T,k}^{\mathrm{null}}=T_\theta(V_k,L,S_k,z_{\varnothing F};\epsilon),
\quad \epsilon\sim\mathcal N(0,I).
$$

共享 $\epsilon$ 可消除比较中的采样变化，共享前缀则避免离线提取时重复执行视觉—语言前向传播。空力动作块作为参考：

$$
A_{T,k}^{\mathrm{ref}}=A_{T,k}^{\mathrm{null}}.
$$

反事实残差定义为

$$
\Delta A_{T,k}^{\mathrm{cf}}
=\left(A_{T,k}^{\mathrm{full}}\right)^p
-\left(A_{T,k}^{\mathrm{null}}\right)^p
\in\mathbb{R}^{H\times9}.
$$

我们将该量称为教师的反事实残差：它表示教师在事实实测力条件与适配空力反事实条件之间的输出变化。这里的“反事实”定义在模型层面，而不是因果识别意义上的反事实。由于空力专用适配器只在反事实分支启用，并且视觉和状态中仍可能包含接触线索，该残差反映完整的条件模式变化，而不是可唯一归因于力的效应。

### 3.3 高频残差策略

#### 慢速参考与缓存意图

慢速分支是以空力模式运行的冻结教师。每次慢速更新产生参考动作块，并从同一次前向传播中得到快速分支的紧凑上下文。视觉—语言前缀 $H_k\in\mathbb{R}^{S\times d}$ 在 mask 下按 $M$ 个连续区间做均值池化：

$$
Z_k^{\mathrm{intent}}=\mathrm{Pool}_M(H_k,m_k)\in\mathbb{R}^{M\times d}.
$$

固定池化保证部署时可从在线前缀复现同一上下文，学生内部再用学习式投影映射至自己的宽度。两次慢速更新之间，参考按动作周期 $\tau_A$ 从缓存动作块插值。快速分支不接收新图像，视觉信息只通过缓存意图进入。

#### 残差学生

学生的因果 TCN 在传感器频率下读取较短的最新力窗口：

$$
Z_t^F=E_S^F(F_{t-w_S:t}).
$$

其余输入包括缓存意图、当前状态、当前参考动作、时间特征 $\gamma_t$ 和学习式残差查询 $q$。所有输入投影到共同宽度并加入类型嵌入：

$$
X_t=[\widetilde Z_k^{\mathrm{intent}};Z_t^F;P_S(S_t);P_A(A^{\mathrm{ref}}(t));P_\Gamma(\gamma_t);q].
$$

由于这些条件不存在自然顺序，模型不使用位置编码。一个带 grouped-query attention 的单层 pre-norm 模块处理该集合，线性头把查询状态映射为 $K$ 步位姿修正：

$$
\Delta\hat A_{t,1:K}=W_{\mathrm{res}}H_t[q]\in\mathbb{R}^{K\times9}.
$$

$W_{\mathrm{res}}$ 使用零初始化，使训练从 $A^{\mathrm{cmd}}=A^{\mathrm{ref}}$ 开始。输出多个步骤可容忍学生延迟和未知部署频率；标签仍是因果的，因为教师的整个动作块只使用 $t_k$ 时刻可见的信息。

#### 训练目标

只有学生被训练。令 $w_j\propto\beta^{j-1}$ 为归一化步骤权重，则残差损失和组合损失分别为

$$
\mathcal L_{\mathrm{res}}=\sum_{j=1}^{K}w_jd(\Delta\hat A_{t,j},\Delta A_{T,j}^{\mathrm{cf}}),
$$

$$
\mathcal L_{\mathrm{comp}}=\sum_{j=1}^{K}w_jd((A_j^{\mathrm{ref}})^p+\Delta\hat A_{t,j},(A_{T,j}^{\mathrm{full}})^p),
$$

$$
\mathcal L=\lambda_{\mathrm{res}}\mathcal L_{\mathrm{res}}+\lambda_{\mathrm{comp}}\mathcal L_{\mathrm{comp}}.
$$

本文不使用参考模仿损失，因为慢速参考直接来自冻结教师；也不对学生施加示范动作行为克隆损失，因为这会把总动作模仿与反事实残差目标混合起来。

### 3.4 时序随机化训练与执行

以单一慢速频率和延迟训练的学生会适应该运行点。本文先以完整频率提取目标，再在每个 epoch 重新采样慢速更新周期、推理延迟和逐次更新抖动。一个在 $t_k$ 发布的数据包只有到 $t_k+\ell$ 后才可使用；被新数据包超过的在途旧包将被丢弃。

学生接收时间特征

$$
\gamma_t=\left[\frac{t-t_k}{c},\alpha_t\right],\qquad
\alpha_t=\frac{t-t_j}{\tau_A}\in[0,1),
$$

分别表示语义上下文的年龄和命令在动作间隔内的插值位置。

现有缓存前缀慢—快策略每次高频更新仍需执行 $N_{\mathrm{flow}}$ 次动作专家去噪，其复杂度为 $\mathcal O(N_{\mathrm{flow}}L_{\mathrm{AE}}d_{\mathrm{AE}}^2)$；本文只需一次小型网络前向传播，复杂度为 $\mathcal O(L_Sd_S^2)$。

部署时，空力教师在独立线程中发布参考动作块、池化意图、观测时间戳和可用时间。力数据进入环形缓冲区，状态独立更新。学生线程读取最新数据包、力窗口和状态并发布带有效区间的修正动作块；执行线程在控制时刻采样该动作块并组合参考和残差。推理延迟时继续执行上一动作块的后续步骤；没有未过期动作块时保持当前命令，而不做外推。

## 4. 实验

### 4.1 实验设置

#### 硬件平台

单臂平台由一台七自由度 Franka Emika Panda 机械臂和两台 Intel RealSense D405 相机构成。双臂任务使用独立的双臂实验平台，其机械臂、末端执行器、力/力矩传感器及相机配置将在硬件信息确定后补充。

#### 任务与数据采集

单臂平台执行按按钮、翻转物体、插入 USB 和打开厨房门，双臂平台执行插入插头、拔出插头、擦白板和打开抽屉。每项单臂和双臂任务分别采集 200 条和 300 条遥操作示范。每条轨迹以 30 Hz 记录同步的多视角图像、机器人状态和命令动作，同时以 100 Hz 记录带时间戳的六轴力/力矩测量。

| 类型 | 任务 | 交互状态 |
|---|---|---|
| 单臂 | 按按钮 | 建立接触、力阈值 |
| 单臂 | 翻转物体 | 贴靠表面的接触重构 |
| 单臂 | 插入 USB | 几何约束下的精密插入 |
| 单臂 | 打开厨房门 | 关节机构约束运动 |
| 双臂 | 插入插头 | 精密插入、力闭合 |
| 双臂 | 拔出插头 | 几何约束下拔出 |
| 双臂 | 擦白板 | 对另一只手持表面的持续接触 |
| 双臂 | 打开抽屉 | 双臂协调约束运动 |

### 4.2 基线、指标与实现细节

对比方法包括：不使用力的 $\pi_{0.5}$、使用瞬时力融合的 ForceVLA、在解码器中引入力矩历史的 TA-VLA，以及采用结构化慢—快处理的视觉—力扩散策略 ImplicitRDP。

主要评估指标为任务成功率。对于每种方法和每项任务，我们评估两个训练 seed，每个 seed 测试 10 次。主表分别报告各项任务的成功率，以及四项单臂任务和四项双臂任务的平均成功率。

所有模型均使用两张 NVIDIA A100 GPU，以 bfloat16 精度训练。教师采用 AdamW 全量微调 50,000 步，全局 batch size 为 32，峰值学习率为 $2\times10^{-5}$，weight decay 为 0.01，使用 1,000 步 warmup、cosine decay 和 1.0 的梯度裁剪。教师以 30 Hz 预测长度为 $H=50$ 的动作块，并编码 100 Hz 力/力矩数据流中最近 100 ms 的测量；流匹配推理使用 $N_{\mathrm{flow}}=10$ 步。

随后冻结教师，使用 $10^{-4}$ 的学习率训练空力 token 和 rank-32 空力适配器 10,000 步。视觉—语言前缀池化为 $M=16$ 个 token。残差学生包含一个 pre-norm attention block，并预测 $K=5$ 步修正；学生使用 AdamW 训练 30,000 步，全局 batch size 为 128，学习率为 $3\times10^{-4}$。损失权重设为 $\lambda_{\mathrm{res}}=\lambda_{\mathrm{comp}}=1$，$\beta=0.5$。时序随机化从 5--15 Hz 采样慢速策略频率，从 50--300 ms 采样延迟，并从 $[-10,10]$ ms 采样调度抖动。

### 4.3 定量结果

主表比较 $\pi_{0.5}$、ForceVLA、TA-VLA、ImplicitRDP 和 ForceDelta-VLA 在八项任务上的成功率，并分别汇总单臂与双臂任务的平均成功率。当前机器人结果尚未提供，因此英文原稿中的任务数值仍为空，本文档不代填任何实验数据。

### 4.4 定性结果

我们展示单臂和双臂任务中具有代表性的成功与失败轨迹。每段图像序列同时给出同步的力/力矩曲线和预测残差，以呈现策略在物理交互过程中如何修正慢速参考。

### 4.5 消融实验

消融包括：

1. **预测目标：**比较重新生成完整动作的快速分支、直接使用示范动作训练的残差学生，以及使用成对反事实残差目标训练的完整方法；
2. **力表示：**比较瞬时力、降采样至 30 Hz 的力历史，以及原生 100 Hz 因果力历史；
3. **反事实构造：**比较零力输入、学习式空力 token、带空力适配器的学习式 token，以及独立流噪声与共享流噪声。

## 5. 结论

本文提出 ForceDelta-VLA：一种以反事实残差蒸馏取代反应式完整动作生成的力感知 VLA。同一冻结教师在事实力条件与适配空力反事实条件下的成对预测定义残差目标，反事实预测同时作为慢速参考。轻量级因果学生根据原生频率力历史、缓存任务上下文、机器人状态和参考年龄，只更新位姿修正。这一分解将反应式路径从迭代生成策略转化为确定性残差预测器。

ForceDelta-VLA 中的反事实定义在模型层面，而不是因果干预。由于空力查询使用学习式 token 和空力专用适配器，并且视觉与状态也可能揭示接触，所得残差不是唯一的力贡献。该方法还会继承教师误差和离线示范的覆盖限制；需要离散策略变化的交互也可能无法由加性位姿修正表示。未来工作应将反事实残差蒸馏与少量在线修正结合，并分析存在延迟的异步组合中的稳定性和安全性。
