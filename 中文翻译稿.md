# ForceRes-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceRes-VLA：面向接触密集型操作的高频力残差学习**

## 摘要

力反馈能够揭示图像中不可见的接触事件，但视觉—语言—动作（VLA）策略仍难以在传感器频率下利用这些信息。现有慢—快架构虽然缓存了视觉—语言特征，快速分支却仍需反复生成完整动作块，使流匹配推理继续处于关键控制路径上。我们提出 ForceRes-VLA：一种残差蒸馏框架，将快速分支的任务缩小为预测力条件教师相对于经过适配的空力模式所产生的偏差。

我们在共享视觉—语言前缀和初始流噪声的条件下，分别以完整模式和空力模式查询冻结的时序力感知教师。两者在动作空间中的差异提供显式的条件残差监督，而空力模式的预测作为慢速参考，因此无需训练独立的参考策略。轻量级因果学生利用原生频率力历史、缓存的任务上下文、机器人状态和参考年龄，以异步方式更新位姿修正。训练时对慢速策略周期、推理延迟和抖动进行随机化，使学生能够适应不同的部署时序。

在 RTX 4090 上，残差路径每次更新耗时 2.4 ms（417 Hz），而十步缓存前缀动作专家耗时 76.1 ms（13 Hz）。我们在八项单臂和双臂任务上，使用任务成功率、交互安全性、接触恢复、残差保真度和运行时间对该方法进行评估。

<!-- 投稿前：完成机器人实验后，在摘要中加入最主要的任务结果。 -->

## 1. 引言

视觉—语言—动作模型为连接语义指令、视觉观测和机器人动作提供了可扩展的接口。然而，在接触密集型任务中，其性能仍会下降，因为图像无法可靠揭示接触开始、初始滑移、冲击或过大载荷。近期力感知 VLA 通过使用力/力矩或触觉测量来生成动作，缓解了这一可观测性问题。剩余挑战在于时间尺度：力传感器的更新速度通常远高于使用这些数据的多模态策略。

慢—快架构通过间歇运行昂贵的视觉—语言主干，并在反应式分支中复用其上下文，缓解了这种频率不匹配。然而，快速分支通常仍是完整的动作生成器。对于流匹配 VLA，即便视觉—语言前缀已经缓存，每次反应式更新仍需多次运行大型动作专家。我们的测量表明，阻止反应式路径匹配 100 Hz 力数据流的主要因素正是这部分计算，而不是感知计算。

我们由此提出问题：快速分支是否必须重新生成完整动作？语义意图和粗粒度运动在较慢的视觉—语言时间尺度上变化，而新到达的力信号主要要求局部修正。因此，可以将控制策略分解为慢速更新的参考和高频残差。然而，示范数据只提供两者之和，普通行为克隆无法确定哪些行为应属于哪一分支。直接使用示范动作训练小型网络，可能只会再次生成标称运动，而不是专门学习力驱动的修正。

ForceRes-VLA 从冻结的时序力感知教师的成对查询中获得显式残差监督。对于相同的视觉观测、指令和机器人状态，我们分别使用实测力历史和经过适配的空力条件查询教师。两次查询共享视觉—语言前缀和初始流噪声，从而抑制差分中的无关变化。由于空力模式包含学习式 token 和仅用于空力模式的低秩适配器，该差异并不是因果意义或唯一意义上的“力分量”，而是教师完整模式与空力模式之间的**条件策略偏差**。该偏差用于训练轻量级残差学生，同一教师的空力模式则提供慢速参考。学生使用原生频率力历史，并以异步方式运行，无需再次执行生成式动作专家。

为了适应实际调度，我们在训练期间随机化慢速策略周期、推理延迟和抖动，并将缓存上下文的年龄作为学生输入。评估不仅检验成对目标是否在接触阶段表现出结构，还检验高频复现该目标是否能够改善物理交互结果。本文贡献如下：

1. 提出成对的完整/空力教师构造，在控制流采样变化的同时提供显式条件残差目标；
2. 提出仅预测残差的快速策略，将原生频率力历史与慢速教师参考结合，以一次确定性前向传播替代迭代动作生成；
3. 提出时序随机化训练与异步执行，使同一个残差策略适应不同的慢速分支频率和延迟。

我们在八项单臂和双臂任务上比较任务成功率、力安全性、恢复行为、残差保真度和执行成本，并与无力、力感知及慢—快基线比较。残差路径在 RTX 4090 上实测耗时 2.4 ms；机器人实验将进一步确定这一计算优势能否转化为接触行为的改善。

## 2. 相关工作

### 2.1 力感知机器人策略

力和触觉能够揭示视觉中含糊或不可见的交互状态。ForceVLA 和 ForceVLA2 通过多模态融合、力提示和直接力通路将力信息引入 VLA 动作生成；TA-VLA 和 TaF-VLA 分别研究时序力矩编码以及触觉—力表示对齐。其他系统则将触觉推理与混合控制或下游控制结合。这些工作证明了物理反馈的价值，但没有为一个可独立执行的高频分支分离出紧凑的修正目标。ForceRes-VLA 假设训练和部署时均有力输入，并使用力感知 VLA 作为教师。

### 2.2 慢—快与异步控制

频率分离是处理语义推理与物理交互速度差异的常见方法。Reactive Diffusion Policy 将慢速视觉规划与快速触觉控制结合；FAVLA 和 LIFT 在反应式动作专家中复用缓存的视觉—语言上下文。ManipForce、DAM-VLA、AT-VLA、T-Rex、PhaForce 等系统则保留原生频率反馈，或协调不同频率的专家。残差策略在较慢的基础控制器上学习修正，有时还使用在线人工反馈。

ForceRes-VLA 同样采用慢—快执行原则，但快速模块只预测教师定义的残差，而不是重新生成完整动作块。该模块以离线方式训练，并显式建模慢速策略上下文年龄和延迟的变化。

### 2.3 缺失模态条件与成对差分

学习式缺失模态 token 为不可用输入提供显式表示，避免把“缺失”与物理上有意义的零混淆；masked modality projection 则利用仍然可用的模态预测缺失特征。本文使用学习式空 token 和小型空力专用适配器，在冻结教师中建立有效的另一种运行模式，而不是为了让部署策略适应传感器失效。

条件差分也被用于 classifier-free guidance 及其蒸馏，以衡量或增强条件的作用。在具身模型中，模态干预还被用于模型解释、捷径抑制和推理时重加权。本文的目标不同：完整/空力成对查询构造输出空间的训练目标，用于训练一个以不同频率运行的小型模块。由于空力模式包含适配参数，且其他观测本身也可能编码接触，本文将该目标称为条件策略偏差，而不是因果归因或对比学习目标。

## 3. 方法

### 3.1 问题定义与总体框架

我们考虑使用视觉观测 $V$、语言指令 $L$、机器人状态 $S$ 和带时间戳力/力矩流 $F$ 的接触密集型操作。设定包含三个假设：（A1）训练和部署时均有力输入，其频率比视觉—语言推理高一至两个数量级；（A2）已在示范数据上训练力条件教师 $T_\theta$，之后将其冻结；（A3）示范提供执行动作和同步传感器流，但不提供动作分解标签。

动作在末端执行器坐标系中表示为

$$
A=[p\in\mathbb{R}^3;\ r\in\mathbb{R}^6;\ g\in\mathbb{R}]\in\mathbb{R}^{10},
$$

其中 $r$ 是旋转矩阵的前两列，$A^p\in\mathbb{R}^9$ 表示位姿部分。连续旋转表示可以避免在动作差分中引入欧拉角分支切换造成的虚假跳变。

令 $t_k$ 为最近一次慢速更新的观测时间，$t$ 为当前控制时刻。控制命令写为

$$
A^{\mathrm{cmd}}(t)=A^{\mathrm{ref}}(t)+\Delta A(t),\qquad
\Delta A(t)\in\mathbb{R}^9.
$$

$A^{\mathrm{ref}}(t)$ 由 $t_k$ 时生成的动作块插值得到，修正量只作用于位姿，夹爪通道保留在慢速分支。参考在语义时间尺度上变化，因此新力到达时只需重新计算 $\Delta A$。但示范数据无法识别这一分解：任意相加后等于执行动作的参考与残差都符合数据。本文因此从冻结教师构造 $\Delta A$ 的显式目标。

### 3.2 成对完整/空力教师目标

#### 时序力感知教师

教师在 ForceVLA 的 $\pi_0$ 风格流匹配策略上，将瞬时力表示替换为因果时序编码器。在每个教师推理时刻 $t_k$，从原生频率流读取固定时长窗口

$$
\mathcal F_k=\{F(\tau)\mid t_k-w\leq\tau\leq t_k\},
$$

并使用有效性 mask 区分未填满的窗口与真实零力。因果时序卷积网络得到 $z_k^F=E_T^F(\mathcal F_k)$，随后将其投影到原模型的力融合维度。完整模式预测为

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F)\in\mathbb{R}^{H\times10}.
$$

该编码器能够表示接触开始、冲击瞬态、振荡和恢复等短时动态。

#### 经过适配的空力模式

为在不提供当前测量时查询同一教师，我们引入学习式 token $z_{\varnothing F}\in\mathbb{R}^{D_F}$。它表示缺少实例特定的力观测，而不是物理零力。教师权重保持冻结，仅优化该 token 和一个只在空力模式下修正流速度位姿通道的 rank-$\rho$ 适配器：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h).
$$

其训练目标为

$$
\mathcal L_{\varnothing}=d\!\left(T_\theta(V_k,L,S_k,z_{\varnothing F}),A_k^{\mathrm{expert}}\right).
$$

该过程得到同一教师的适配空力模式，而不是单独训练参考策略；部署时，该模式保留为慢速分支。

#### 共享噪声的成对差分

流匹配教师的两次独立查询会同时受到条件和采样噪声影响。我们只计算一次视觉—语言前缀及其 key/value cache，并用同一个噪声样本 $\epsilon$ 初始化两次积分：

$$
A_{T,k}^{\mathrm{full}}=T_\theta(V_k,L,S_k,z_k^F;\epsilon),
$$

$$
A_{T,k}^{\mathrm{null}}=T_\theta(V_k,L,S_k,z_{\varnothing F};\epsilon),
\quad \epsilon\sim\mathcal N(0,I).
$$

共享 $\epsilon$ 可消除比较中的采样变化，共享前缀则避免离线提取时重复执行视觉—语言前向传播。空力动作块作为参考：

$$
A_{T,k}^{\mathrm{ref}}=A_{T,k}^{\mathrm{null}}.
$$

条件残差定义为

$$
\Delta A_{T,k}^{\mathrm{cond}}
=\left(A_{T,k}^{\mathrm{full}}\right)^p
-\left(A_{T,k}^{\mathrm{null}}\right)^p
\in\mathbb{R}^{H\times9}.
$$

该量表示教师在适配空力模式与实测力模式之间的输出变化。由于空力专用适配器只在前者启用，目标反映的是完整模式变化，而不是只替换一个 token 的干预。这是输出空间回归，不是对比学习，也不是因果归因。

### 3.3 高频残差策略

#### 慢速参考与缓存意图

慢速分支是以空力模式运行的冻结教师。每次慢速更新产生参考动作块，并从同一次前向传播中得到快速分支的紧凑上下文。视觉—语言前缀 $H_k\in\mathbb{R}^{S\times d}$ 在 mask 下按 $M$ 个连续区间做均值池化：

$$
Z_k^{\mathrm{intent}}=\mathrm{Pool}_M(H_k,m_k)\in\mathbb{R}^{M\times d}.
$$

固定池化保证部署时可从在线前缀复现同一上下文，学生内部再用学习式投影映射至自己的宽度。两次慢速更新之间，参考按动作周期 $\tau_A$ 从缓存动作块插值。快速分支不接收新图像，视觉信息只通过缓存意图进入。

#### 残差学生

学生的因果 TCN 在传感器频率下读取较短的最新力窗口：

$$
Z_t^F=E_S^F(F_{t-w_S:t}).
$$

其余输入包括缓存意图、当前状态、当前参考动作、时间特征 $\gamma_t$ 和学习式残差查询 $q$。所有输入投影到共同宽度并加入类型嵌入：

$$
X_t=[\widetilde Z_k^{\mathrm{intent}};Z_t^F;P_S(S_t);P_A(A^{\mathrm{ref}}(t));P_\Gamma(\gamma_t);q].
$$

由于这些条件不存在自然顺序，模型不使用位置编码。一个带 grouped-query attention 的单层 pre-norm 模块处理该集合，线性头把查询状态映射为 $K$ 步位姿修正：

$$
\Delta\hat A_{t,1:K}=W_{\mathrm{res}}H_t[q]\in\mathbb{R}^{K\times9}.
$$

$W_{\mathrm{res}}$ 使用零初始化，使训练从 $A^{\mathrm{cmd}}=A^{\mathrm{ref}}$ 开始。输出多个步骤可容忍学生延迟和未知部署频率；标签仍是因果的，因为教师的整个动作块只使用 $t_k$ 时刻可见的信息。

#### 训练目标

只有学生被训练。令 $w_j\propto\beta^{j-1}$ 为归一化步骤权重，则残差损失和组合损失分别为

$$
\mathcal L_{\mathrm{res}}=\sum_{j=1}^{K}w_jd(\Delta\hat A_{t,j},\Delta A_{T,j}^{\mathrm{cond}}),
$$

$$
\mathcal L_{\mathrm{comp}}=\sum_{j=1}^{K}w_jd((A_j^{\mathrm{ref}})^p+\Delta\hat A_{t,j},(A_{T,j}^{\mathrm{full}})^p),
$$

$$
\mathcal L=\lambda_{\mathrm{res}}\mathcal L_{\mathrm{res}}+\lambda_{\mathrm{comp}}\mathcal L_{\mathrm{comp}}.
$$

本文不使用参考模仿损失，因为慢速参考直接来自冻结教师；也不对学生施加示范动作行为克隆损失，因为这会把总动作模仿与条件残差目标混合起来。

### 3.4 时序随机化训练与执行

以单一慢速频率和延迟训练的学生会适应该运行点。本文先以完整频率提取目标，再在每个 epoch 重新采样慢速更新周期、推理延迟和逐次更新抖动。一个在 $t_k$ 发布的数据包只有到 $t_k+\ell$ 后才可使用；被新数据包超过的在途旧包将被丢弃。

学生接收时间特征

$$
\gamma_t=\left[\frac{t-t_k}{c},\alpha_t\right],\qquad
\alpha_t=\frac{t-t_j}{\tau_A}\in[0,1),
$$

分别表示语义上下文的年龄和命令在动作间隔内的插值位置。

现有缓存前缀慢—快策略每次高频更新仍需执行 $N_{\mathrm{flow}}$ 次动作专家去噪，其复杂度为 $\mathcal O(N_{\mathrm{flow}}L_{\mathrm{AE}}d_{\mathrm{AE}}^2)$；本文只需一次小型网络前向传播，复杂度为 $\mathcal O(L_Sd_S^2)$。

部署时，空力教师在独立线程中发布参考动作块、池化意图、观测时间戳和可用时间。力数据进入环形缓冲区，状态独立更新。学生线程读取最新数据包、力窗口和状态并发布带有效区间的修正动作块；执行线程在控制时刻采样该动作块并组合参考和残差。推理延迟时继续执行上一动作块的后续步骤；没有未过期动作块时保持当前命令，而不做外推。

## 4. 实验

### 4.1 评估问题

实验回答四个问题：（Q1）条件残差监督是否比完整动作快速分支带来更高的任务成功率和交互安全性？（Q2）残差是否依赖力输入并集中在接触附近？（Q3）更高更新频率是否减少反应延迟和不利的力瞬态？（Q4）时序随机化是否改善跨慢速策略频率和延迟的迁移？

### 4.2 高频路径的可达到频率

两种设计在同一设备、batch size 为 1 的条件下测量。缓存前缀基线直接使用视觉—语言 key/value cache，并测量在 816 个前缀 token 和 51 个后缀 token 上的动作专家传播；学生测量包含力编码器和意图投影器在内的完整前向传播。

| 高频路径 | 参数量 | 延迟 | 频率 |
|---|---:|---:|---:|
| 动作专家，$N_{\mathrm{flow}}=1$ | 334 M | 7.6 ms | 132 Hz |
| 动作专家，$N_{\mathrm{flow}}=4$ | 334 M | 30.4 ms | 33 Hz |
| 动作专家，$N_{\mathrm{flow}}=10$ | 334 M | 76.1 ms | 13 Hz |
| **残差学生（本文）** | **44.7 M** | **2.4 ms** | **417 Hz** |

在标准 $N_{\mathrm{flow}}=10$ 设置下，计算耗时降低 31.8 倍。动作专家的 13 Hz 与 ForceVLA2 在相同 GPU 等级上报告的 15 Hz 相符。即使只运行一次动作专家传播，也会占用 10 ms 预算的 76%，因此该限制不能简单通过调整去噪步数消除。

### 4.3 任务与评估协议

实验包含八项真实机器人任务：

| 类型 | 任务 | 交互状态 |
|---|---|---|
| 单臂 | 按按钮 | 建立接触、力阈值 |
| 单臂 | 翻转物体 | 贴靠表面的接触重构 |
| 单臂 | 擦白板 | 持续接触、力调节 |
| 单臂 | 打开柜门 | 关节机构约束运动 |
| 双臂 | 插入插头 | 精密插入、力闭合 |
| 双臂 | 拔出插头 | 几何约束下拔出 |
| 双臂 | 擦白板 | 对另一只手持表面的持续接触 |
| 双臂 | 打开抽屉 | 双臂协调约束运动 |

每项任务的示范均包含同步多视角图像、语言指令、机器人状态、命令动作和以原生频率保存的六轴力/力矩数据。评估随机化物体配置、初始姿态和接触条件；只有完成目标且未违反预定力限制的试验才计为成功。所有方法共享数据划分、控制接口、评估初始状态和硬件。试验次数和随机种子将在机器人结果完成后填写。

### 4.4 基线、指标与实现细节

公开基线包括：不使用力的 $\pi_{0.5}$、使用瞬时力融合的 ForceVLA，以及在解码器中注入力矩并汇总力矩历史的 TA-VLA。内部对照包括：在缓存前缀上重新运行教师动作专家的快速分支，以及不使用成对教师目标、直接从示范动作训练相同学生架构的慢—快变体。

任务指标为成功率和阶段完成率；交互指标为峰值力、力冲量、力限制违反次数和扰动恢复率；分解指标为残差误差 $d(\Delta\hat A,\Delta A_T^{\mathrm{cond}})$ 和组合误差 $d(A^{\mathrm{ref}}+\Delta\hat A,A_T^{\mathrm{full}})$。误差在重建的绝对位姿上以物理单位计算，并分别报告平移和旋转。运行指标包括慢速频率、快速频率、端到端延迟和参数量。

教师使用因果时序力编码器，动作 horizon 为 $H=50$、动作频率为 30 Hz；力编码器从 100 Hz 数据流读取 100 ms 窗口。空力适配器 rank 为 $\rho=32$，目标提取使用 $N_{\mathrm{flow}}=10$，前缀池化为 $M=16$ 个区间。学生包含一个注意力模块，输出 $K=5$ 步，$\beta=0.5$。训练时从 5–15 Hz 采样慢速频率，从 50–300 ms 采样延迟，并加入 $\pm10$ ms 抖动。

### 4.5 主结果

主表比较 $\pi_{0.5}$、ForceVLA、TA-VLA、缓存前缀快速分支、无成对目标的直接慢—快模型和 ForceRes-VLA 在八项任务上的成功率与运行频率。当前机器人结果尚未提供，因此英文原稿中的任务数值仍为空，本文档不代填任何实验数据。

结果还将按自由空间运动、接触开始、持续接触和扰动恢复分别分析。自由空间中参考策略与组合策略一致，应被视为残差专门化的证据。

### 4.6 执行频率与接触反应

保持学生参数不变，将其执行频率限制为 13、30、50 和 100 Hz，并测量峰值接触力、力冲量和成功率。匹配接触事件还将在三种配置下比较：仅慢速分支、两分支同步运行于慢速频率，以及完整异步执行。报告从检测到力变化到首次修正命令的延迟、恢复前最大力和返回参考轨迹的时间。该实验区分更新频率与残差本身的作用；只有接触结果随频率改善，延迟优势才具有实际意义。

### 4.7 残差分解分析

我们将 $\lVert\Delta A_T^{\mathrm{cond}}\rVert$ 与实测力和任务阶段对齐。有效分解应在自由运动中较小，并在接触开始、瞬态和恢复附近呈现更大且结构化的偏差。

首先比较条件残差能量与参考陈旧误差：

$$
\sigma_C^2=\mathbb E\lVert\Delta A_T^{\mathrm{cond}}\rVert^2,\qquad
\sigma_R^2=\mathbb E\lVert A^{\mathrm{ref}}-A_T^{\mathrm{null}}\rVert^2.
$$

其次，通过移除学生的力输入，检验其误差改善是否真正依赖力；最后，将这一差距分别在接触阶段和自由空间报告，以检验接触局部性。我们还比较学习式空力条件、零力向量和随机力 token masking，并按平移、旋转和夹爪通道报告教师与示范之间的误差，因为教师误差限制了蒸馏学生的性能上限。

### 4.8 消融实验

消融包括：

1. 瞬时力、降采样历史和原生频率因果历史；
2. 学习式空力条件、零力填充、无低秩适配器的学习式 token，以及独立流噪声；
3. 移除缓存意图、状态、参考 token、时间 token 或多 token 力序列；
4. 固定时序与随机时序，并在训练运行点和未见频率下评估；
5. 在注入推理停顿时比较 $K=1$ 与 $K=5$。

## 5. 结论

本文提出 ForceRes-VLA：一种慢—快力感知策略，其反应式分支预测教师定义的动作残差，而不是重新生成完整动作块。同一冻结教师的完整/空力成对查询提供显式条件残差监督，空力模式预测作为慢速参考。轻量级因果学生根据原生频率力历史、缓存任务上下文、机器人状态和参考年龄更新位姿修正。实测实现将十步缓存前缀动作专家的高频推理时间从 76.1 ms 降至每次残差更新 2.4 ms。

本文有意对该分解作有限解释。由于空力查询使用学习式 token 和空力专用适配器，并且视觉与状态也可能揭示接触，成对差分是教师的条件偏差，而不是因果或唯一的力贡献。因此，它的有效性必须通过接触局部性、力输入消融、组合策略保真度和机器人结果加以验证。该方法还会继承教师误差和离线示范的覆盖限制；需要离散策略变化的交互也可能无法用加性位姿修正表示。未来工作应将残差目标与少量在线修正结合，并分析存在延迟的异步组合中的稳定性和安全性。
