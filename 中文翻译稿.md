# ForceDelta-VLA 中文翻译稿

> 本文档是当前英文论文的中文翻译，用于逐段审阅；不增加英文原稿中不存在的技术主张或实验结论。

## 标题

**ForceDelta-VLA：面向接触密集型操作的力条件动作修正蒸馏**

## 摘要

力感知视觉—语言—动作（VLA）策略改善了接触密集型操作，但通常仍在同一个生成式动作预测中耦合任务层运动与接触相关调整。离线示范没有提供将该预测分解为可复用 reference 和 correction 的显式标签。我们提出 ForceDelta-VLA：在缓存上下文下，将冻结 teacher 的力条件预测与学习式力输入缺失预测配对，由两者之差构造 teacher 定义的力响应修正目标；该过程无需任务奖励、在线纠正 rollout，也无需在原始离线数据集之外增加示范。另一残差在重放部署时序下补偿 reference 陈旧性。所得残差策略复用缓存任务上下文和带时间戳的力输入缺失 reference，同时利用近期力历史更新位姿修正，无需重新生成完整动作。在九项接触密集型单臂和双臂任务上，ForceDelta-VLA 达到 82.2% 的平均成功率；ForceVLA 和 fast--slow 视觉—力基线 ImplicitRDP 分别为 54.4% 和 72.8%。相较 ImplicitRDP，单臂和双臂平台的峰值接触力分别降低约 16% 和 19%。


## 1. 引言

力感知视觉—语言—动作（VLA）策略通过以交互反馈为条件生成动作，提高了接触密集型操作性能。然而，这类策略通常在同一个生成式动作预测中同时表示任务层运动与接触相关调整。部署时，相邻控制周期可以复用任务层行为，而物理修正应随新观测到的接触状态及时变化。这种耦合使修正缺少显式监督目标和可独立执行的输出形式（见图 1）。

力响应加性残差在保留力输入缺失 reference 行为的同时，提供显式的位姿空间修正。标准离线示范只包含执行动作；多个 reference--residual 组合可以重建同一动作标签，因此直接动作监督无法确定任务层运动和接触相关调整应分别由哪个分量承担。

从示范动作中减去缓存 reference 还会混合力条件偏差与 reference 陈旧性；零力查询则表示一种有效物理状态，而非缺失力条件。这些歧义促使我们在匹配查询条件下利用冻结的力感知 teacher 构造修正目标。

现有异步策略分离计算或传感器更新频率，残差强化学习和纠正式模仿学习则通过任务奖励或额外在线交互获得修正信号。本文研究一个互补问题：离线构造修正监督，同时保留异步执行。

ForceDelta-VLA 在缓存上下文下构造两个匹配的 teacher 查询：力条件查询使用当前力历史，力输入缺失查询使用学习式 missing-force token；二者共享缓存的视觉—语言前缀、当前机器人状态与 flow noise。其动作差定义 teacher 监督的力响应偏差，另一目标补偿缓存 reference 的状态与时间不匹配。两个修正被蒸馏为独立于生成式动作推理执行的残差策略。

我们在五项单臂和四项双臂真实机器人任务上评估 ForceDelta-VLA，任务涵盖接触建立、精密插入、摩擦主导运动和受约束操作。本方法的平均成功率为 82.2%，ForceVLA 和 ImplicitRDP 分别为 54.4% 和 72.8%；相较 ImplicitRDP，单臂和双臂平台的峰值接触力分别降低约 16% 和 19%。

本文贡献如下：

1. 提出**力输入缺失残差蒸馏**，利用冻结力感知 teacher 在缓存上下文下的匹配力条件预测与学习式力输入缺失预测，构造 teacher 定义的显式力响应修正目标，无需任务奖励、在线纠正 rollout，也无需在原始离线数据集之外增加示范；
2. 提出**参考状态一致的双残差分解**，分别监督力条件修正与缓存 reference 陈旧性，避免时序 reference 不匹配被吸收到力残差中；
3. 构建**可独立执行残差通路**，复用缓存任务上下文和力输入缺失 reference，并根据近期力历史更新位姿修正。


## 2. 相关工作

### 2.1 力感知操作策略

力感知 VLA 通过多模态融合、提示、时序记忆、混合力—位置控制或力感知优化整合力/力矩信息；力中心模仿学习则从示范中联合建模运动与交互力。这些方法通常仍在统一策略输出中共同表示任务运动和物理适应。FD-VLA 则从视觉与状态蒸馏潜在力表征，用于无力传感器推理；ForceDelta-VLA 保留实测 wrench 输入，并蒸馏 teacher 的力条件动作偏差。

### 2.2 多频率与结构化操作策略

视觉运动方法学习异步 RGB 与力/力矩流，按交互阶段调整扩散推理频率，或引入快速修正与混合力—位置通路。VLA 方法也会分离模态更新频率。FAVLA 使用慢速语义 backbone 和可变频率的力条件 action expert 重新生成动作块；DAM-VLA 按各传感器频率刷新模态专用 latent buffer，并由 100 Hz action head 读取。Real-Time Chunking（RTC）则面向 flow-based action-chunk policy 的推理延迟：它异步生成下一动作块，并对其中尚未承诺执行的动作进行 inpainting，使其与正在执行的动作块保持连续。ForceDelta-VLA 处理一个互补问题：保留生成式 VLA 输出作为缓存 reference，为显式的力条件加性修正及 reference-staleness correction 构造离线监督，而无需在每次快速更新时重新生成完整动作块。

### 2.3 残差策略学习

残差策略学习用学习得到的修正增强标称控制器，并已用于接触密集型操作。现有方法通过强化学习或在线人工纠正获得残差监督，包括冻结视觉策略的触觉条件适配，以及利用 DAgger 训练的反应式力专家。其他模仿学习方法直接规定加性修正结构。ForceDelta-VLA 则从匹配的力条件与力输入缺失 teacher 预测中导出 residual target。

### 2.4 策略蒸馏

学习式缺失模态表征能够区分不可用条件与具有物理意义的零值，从而支持建立独立的力输入缺失运行点。机器人策略蒸馏通常将迭代扩散或流采样压缩为快速的完整动作生成器；guidance distillation 则利用成对预测迁移条件更新。ForceDelta-VLA 保留生成式输出作为任务层 reference，并蒸馏动作空间中的力偏差与参考状态修正，而非完整动作生成器。

## 3. 方法

### 3.1 问题定义

我们考虑由遥操作轨迹组成的离线数据集 $\mathcal D$。在时刻 $t$，每条轨迹提供视觉观测 $V_t$、语言指令 $L$、机器人状态 $S_t$、示范动作 $A_t^*$，以及因果 wrench 历史

$$
\mathcal F_t=\{(s,\mathbf w(s))\mid t-T_F\leq s\leq t\},
$$

其中，$T_F$ 是历史窗口长度，$\mathbf w(s)=[\mathbf f(s);\boldsymbol\tau(s)]\in\mathbb R^6$ 是作用于末端的外部 wrench。位姿动作和 wrench 测量均使用对应的机器人基坐标系。力条件 teacher $T_\theta$ 将 $(V_t,L,S_t,\mathcal F_t)$ 映射为相对于 $S_t$ 的 $H$ 步动作块。下文的位姿动作与残差使用其归一化 pose-action 坐标，位姿差按动作通道标准差进行尺度归一化。

本文的目标是为力响应修正构造显式监督，并将其蒸馏为可以独立于生成式 teacher 计算的残差策略。令 $\mathcal P(\cdot)$ 提取动作中的位姿分量。我们采用参考状态一致的双残差分解，将命令位姿分解为一个 reference 与两个加性修正：

$$
\mathcal P(A^{\mathrm{cmd}})=\mathcal P(A^{\mathrm{ref}})
+\Delta\hat A^{\mathrm{force}}
+\Delta\hat A^{\mathrm{stale}},
$$

力条件项建模 teacher 的力条件偏差，陈旧性项补偿缓存 reference 所对应的较早机器人状态。两项修正都只作用于位姿；夹爪指令沿用 reference。

其中，位姿分量 $\mathcal P(A)=[p;r]$ 包含三维平移 $p$ 和连续六维旋转表示 $r$。平移直接相加；旋转修正在 teacher 的六维动作坐标中相加，再通过 Gram--Schmidt 正交化映射为合法旋转矩阵。

### 3.2 方法概述

训练分为图 2 所示的三个阶段。首先训练时序力条件 VLA teacher。随后冻结 teacher，并学习提供参考动作的力输入缺失模式。最后冻结两种模式，在缓存上下文下构造力修正和陈旧性修正目标，并训练残差策略。

### 3.3 力条件教师与力输入缺失参考

#### 时序力条件教师

教师基于 ForceVLA，将其瞬时力嵌入替换为因果时序编码：

$$
z_t^F=E_T^F(\mathcal F_t).
$$

教师在 $\mathcal D$ 上训练并预测

$$
A_{T,t,1:H}^{\mathrm{cond}}=T_\theta(V_t,L,S_t,z_t^F;\epsilon).
$$

其中 $\epsilon$ 为初始流噪声。训练完成后冻结 $\theta$。

#### 力输入缺失参考模式

我们严格区分“有意不提供 wrench 历史”和“测得的 wrench 恰好为零”。学习式力输入缺失模式用于前一种情况，由 missing-force token $z_{\varnothing F}$ 和 rank-$\rho$ 适配器 $\phi=\{z_{\varnothing F},W_{\mathrm{in}},W_{\mathrm{out}}\}$ 构成。适配器仅修改动作专家的位姿通道：

$$
v^p\leftarrow v^p+W_{\mathrm{out}}\sigma(W_{\mathrm{in}}h),
$$

其中，$h$ 是 action expert 的隐藏状态，$v^p$ 是其位姿通道的 flow 输出，$\sigma$ 是逐点非线性函数。该 adapter 只在力输入缺失模式下启用。保持 $\theta$ 固定，在 teacher 原有的 flow-matching 目标下优化 $\phi$，由此定义 teacher 的力输入缺失运行点。

所得参考预测为

$$
A_{T,t,1:H}^{\mathrm{miss}}
=T_{\theta,\phi}^{\mathrm{miss}}(V_t,L,S_t,z_{\varnothing F};\epsilon).
$$

### 3.4 参考状态一致的双残差蒸馏

#### 修正目标

对于 schedule replay 样本 $(t,k)$，令 $E_k$ 表示 reference 查询 $k$ 从 $(V_k,L,S_k)$ 发起时生成的视觉—语言前缀，$\epsilon_k$ 表示该查询的 flow noise。查询发布带时间戳的力输入缺失动作块 $A_k^{\mathrm{ref}}(\cdot)$，其中 $A_k^{\mathrm{ref}}=A_{T,k}^{\mathrm{miss}}$。为使监督信息的时间来源与部署时 residual 通路一致，target generation 复用 $E_k$，同时将机器人状态和力历史更新为时刻 $t$ 的取值：

$$
A_{T,t|k,1:K}^{\mathrm{cond}}
=T_\theta(E_k,S_t,z_t^F;\epsilon_k)_{1:K},
$$

$$
A_{T,t|k,1:K}^{\mathrm{miss}}
=T_{\theta,\phi}^{\mathrm{miss}}
(E_k,S_t,z_{\varnothing F};\epsilon_k)_{1:K}.
$$

这里，$T(E_k,\cdot)$ 表示复用缓存的视觉—语言前缀，不再编码当前图像。reference 查询、力条件查询和力输入缺失查询共享 $E_k$ 与 $\epsilon_k$，因此目标不依赖 student 不可见的当前图像，也不引入独立 flow noise。对于 $j=1,\ldots,K\leq H$，由 teacher 定义的力条件偏差目标为

$$
\Delta A_{T,t|k,j}^{\mathrm{force}}
=\mathcal P\!\left(A_{T,t|k,j}^{\mathrm{cond}}\right)
-\mathcal P\!\left(A_{T,t|k,j}^{\mathrm{miss}}\right).
$$

该目标用于监督力响应残差 head。

residual 查询预测在 $t_j=t+(j-1)/f_A$ 执行的 $K$ 个动作样本，其中 $f_A$ 是动作块内部的动作采样频率。令 $\widetilde S^p$ 表示归一化后的九维位姿坐标，$\sigma_S^p$ 和 $\sigma_A^p$ 表示训练集上相应通道的标准差。缓存状态差按下式转换到归一化 action 坐标：

$$
\Gamma(S_t,S_k)
=\frac{\sigma_S^p+\varepsilon}{\sigma_A^p+\varepsilon}
\odot(\widetilde S_t^p-\widetilde S_k^p),
\qquad \varepsilon=10^{-6}.
$$

陈旧性目标为

$$
\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}
=\mathcal P\!\left(A_{T,t|k,j}^{\mathrm{miss}}\right)
-\mathcal P\!\left(A_k^{\mathrm{ref}}(t_j)\right)
+\Gamma(S_t,S_k).
$$

令 $\mathcal R(S,a)$ 表示将以状态 $S$ 为基准的相对动作 $a$ 转换为绝对机器人命令。两项目标在每个预测步上对齐两个坐标基准：

$$
\begin{aligned}
&\mathcal R\!\left(S_k,\mathcal P(A_k^{\mathrm{ref}}(t_j))
+\Delta A_{T,t|k,j}^{\mathrm{force}}
+\Delta A_{T,k\rightarrow t,j}^{\mathrm{stale}}\right)\\
&\qquad\approx\mathcal R\!\left(S_t,
\mathcal P(A_{T,t|k,j}^{\mathrm{cond}})\right).
\end{aligned}
$$

#### 残差策略

每个 reference query 缓存由视觉—语言前缀 $E_k$ 及其 mask $m_k$ 池化得到的 $M$ 个 intent token，即 $Z_k^{\mathrm{intent}}=\operatorname{Pool}_M(E_k,m_k)$。$A_k^{\mathrm{ref}}$、$Z_k^{\mathrm{intent}}$ 和 $S_k$ 共同构成带时间戳的 reference update；最新发布的 update 缓存给快速路径使用。快速 residual 假设该任务层视觉上下文在一个 reference interval 内局部有效。对于时刻 $t$ 发起的 residual 查询，通过时间戳插值得到 reference sample $A_k^{\mathrm{ref}}(t)$。因果编码器将 $\mathcal F_t$ 映射为 $u_t^F$；已知重基准项 $\Gamma(S_t,S_k)$ 在策略外计算，不属于 student 的条件输入。缓存年龄和 reference 插值位置表示为

$$
\xi_{t,k}=\left[\min\!\left(\frac{t-t_k}{c},1\right);\alpha_{t,k}\right].
$$

残差策略采用有序条件序列

$$
\mathcal C_t=[P_Z(Z_k^{\mathrm{intent}});u_t^F;P_S(S_t);
P_A(A_k^{\mathrm{ref}}(t));P_\Gamma(\Gamma(S_t,S_k));P_\xi(\xi_{t,k})].
$$

其中，$P_Z,P_A,P_S,P_\Gamma$ 和 $P_\xi$ 将各自输入映射到共享 token 维度。令 $\mathcal C_t^{\mathrm{maskF}}=\operatorname{Mask}_F(\mathcal C_t)$ 表示保持序列结构不变、但屏蔽力 token 后的条件序列。

共享注意力模块 $\Phi$ 和学习式查询 $q_{\mathrm{res}}$ 预测两个长度为 $K$ 的修正块：

$$
\Delta\hat A^{\mathrm{force}}_{t,1:K}
=W_{\mathrm{force}}\Phi([\mathcal C_t;q_{\mathrm{res}}])[q_{\mathrm{res}}],
$$

$$
\Delta\hat A^{\mathrm{stale}}_{t,1:K}
=W_{\mathrm{stale}}
\Phi([\mathcal C_t^{\mathrm{maskF}};q_{\mathrm{res}}])[q_{\mathrm{res}}].
$$

该 mask 避免陈旧性输出直接依赖实测力。在缓存视觉上下文局部有效的前提下，staleness head 针对机器人状态与 reference 时间不匹配进行修正。两次计算共享 $\Phi$，并使用独立的输出投影。

#### 陈旧性感知时序重放

在离线残差策略训练中，我们通过采样 query 间隔和 reference-query latency 重放异步执行。训练时刻沿用数据中记录的 state--action 时间戳，而 $\mathcal F_t$ 保留因果窗口内的全部 wrench 样本。令 $t_i$ 和 $\bar t_i$ 分别表示查询 $i$ 的发起和发布时刻，则当前有效的 reference index 为

$$
k(t)=\arg\max_{i:\bar t_i\leq t}t_i.
$$

所选 reference update 提供缓存 intent、当前 reference sample 和时序特征；其带时间戳的动作块用于构造逐步陈旧性目标。重新采样调度使 reference age、插值相位和 reference-query latency 发生变化。

采用归一化时序权重 $w_j\propto\beta^{j-1}$（$0<\beta\leq1$）和归一化坐标均方损失 $\ell_{\mathrm{pose}}$，蒸馏目标为

$$
\mathcal L_{\mathrm{distill}}=\sum_{j=1}^{K}w_j\left[
\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{force}}_{t,j},\Delta A^{\mathrm{force}}_{T,t|k,j})
+\lambda_{\mathrm{stale}}\ell_{\mathrm{pose}}(\Delta\hat A^{\mathrm{stale}}_{t,j},\Delta A^{\mathrm{stale}}_{T,k\rightarrow t,j})
\right].
$$

$\lambda_{\mathrm{stale}}$ 用于平衡力条件监督与陈旧性监督。

### 3.5 独立残差执行

Reference worker 与 residual worker 相互独立运行。reference 查询完成后，worker 发布一个带时间戳的 reference update，其中包含力输入缺失动作块、缓存 intent 和查询状态。每个 residual 查询读取当前缓存的 reference 以及当前状态和因果 wrench 历史。其带时间戳的 $K$ 步修正块始终与预测时使用的 reference update 绑定，因此新发布的 reference 只供随后发起的 residual 查询使用。

执行时跳过已经过期的修正步骤，其余步骤不经插值而依次执行。reference 位姿通过时间戳插值得到。力条件修正与陈旧性修正分别限幅，在归一化 delta-action 坐标中完成组合，再根据 reference 查询状态转换为绝对机器人指令。

## 4. 实验

### 4.1 实验设置

#### 硬件、任务与数据采集

实验包括两个平台上的 5 项单臂任务和 4 项双臂任务（见任务表）。单臂平台采用 7 自由度 Franka Emika Panda，配备腕部和外部 Intel RealSense D405 相机。示范通过 3Dconnexion SpaceMouse 遥操作采集。双臂实验采用 X Square Robot 平台，每条机械臂配备一个腕部相机，并设置一个共享外部相机。示范通过 Leader--Follower 遥操作采集。与 ForceVLA [1] 一致，两个平台均使用机器人自身提供的 wrench 估计，不额外安装腕部力/力矩传感器。

每项任务均采集 200 条示范。多视角图像、机器人状态和命令动作以 30 Hz 记录，带时间戳的 wrench 估计以 100 Hz 记录。

| 平台 | 任务 | 交互形式 |
|---|---|---|
| 单臂 | 翻物体（Object Flipping） | 借助表面接触完成重定向 |
| 单臂 | 插 USB（USB Insertion） | 精密插入 |
| 单臂 | 开柜门（Cabinet Opening） | 摩擦主导的关节约束运动 |
| 单臂 | 按按钮（Button Pressing） | 离散接触驱动 |
| 单臂 | 擦白板（Whiteboard Wiping） | 持续表面接触 |
| 双臂 | 拔插头（Plug Removal） | 双臂协调的受约束拔出 |
| 双臂 | 插插头（Plug Insertion） | 双臂协调对准与插入 |
| 双臂 | 拉抽屉（Drawer Opening） | 双臂协调的摩擦主导运动 |
| 双臂 | 擦白板（Whiteboard Wiping） | 固定白板并执行擦除 |

#### 基线与评估协议

本文选择互补的基线进行比较。$\pi_{0.5}$ 代表不显式使用力反馈的通用 VLA；ForceVLA 同时也是本文 teacher 的基础，构成直接的力感知完整动作基线；TA-VLA 代表另一种 torque-aware VLA 设计；ImplicitRDP 则提供 VLA 系列之外的强端到端 slow--fast 视觉—力对照。Temporal Teacher 直接 rollout 第一阶段 teacher，用于隔离残差蒸馏与时序力条件本身的收益。快速完整动作生成与 reference-anchored residual prediction 之间的受控比较，由消融实验中架构匹配的 Fast full-action distillation 提供。每种方法在每项任务上进行 20 次评估。指标包括成功率、峰值接触力和完成时间。单臂峰值力按全部试验中的 $\max_t\|\mathbf f(t)\|_2$ 计算，双臂峰值力按 $\max_{t,a\in\{L,R\}}\|\mathbf f_a(t)\|_2$ 计算。完成时间仅基于成功试验统计。

#### 实现细节

两个平台均通过带比例—微分反馈的笛卡尔阻抗控制器执行策略指令。出于安全考虑，两个平台的笛卡尔指令接口均限制在最高 100 Hz。时序力条件 teacher 使用 8 张 NVIDIA A100 GPU 进行全参数微调。真实机器人部署使用一台配备 2 张 24 GB NVIDIA RTX 4090 GPU 的工作站。

### 4.2 主要结果

#### 定性结果

残差在持续接触开始时增大，在力瞬态附近达到峰值，并在释放阶段减小，展示了其接触响应式局部行为（见图 4）。

**图 4：代表性按按钮 rollout。** 4 个交互阶段的外部相机与腕部相机观测，与机器人估计的末端力分量和平移残差幅值在时间上对齐；竖线标出所选图像帧。

#### 任务性能

ForceDelta-VLA 的平均成功率为 82.2%，比最强基线 ImplicitRDP（72.8%）高 9.4 个百分点，比 Temporal Teacher（68.9%）高 13.3 个百分点；其余无显式力输入或生成完整动作的基线均低于 60%（见表 II）。USB 插入（80% 对 55%）、插插头（70% 对 50%）和拔插头（80% 对 65%）贡献了相对 ImplicitRDP 跨任务总计 85 个百分点优势中的 60 个百分点，即 70.6%。ForceDelta-VLA 与 ImplicitRDP 在翻物体（90%）、按按钮（85%）和单臂擦白板（90%）上持平。

基线递进关系区分了时序力建模与完整 residual framework。Temporal Teacher 以时序力历史替换 ForceVLA 的瞬时力编码，平均成功率从 54.4% 提升至 68.9%；ForceDelta-VLA 达到 82.2%。ForceDelta-VLA 在两个平台上也取得最低的成功试验平均完成时间：单臂/双臂分别为 23.9/25.8 s，Temporal Teacher 为 27.8/29.2 s，ImplicitRDP 为 25.8/27.9 s。相对 Temporal Teacher，完成时间分别降低 14.0%/11.6%；相对 ImplicitRDP，分别降低 7.4%/7.5%。因此，成功率和接触力收益没有伴随成功试验平均完成时间增加。后文的运行时与消融结果进一步分析 reference-anchored correction 的贡献。

| 方法 | 翻物体 | 插 USB | 开柜门 | 按按钮 | 单臂擦白板 | 拔插头 | 插插头 | 拉抽屉 | 双臂擦白板 | 平均值 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| $\pi_{0.5}$ | 55 | 35 | 50 | 45 | 70 | 40 | 30 | 50 | 50 | 47.2 |
| ForceVLA | 65 | 40 | 60 | 50 | 70 | 50 | 35 | 60 | 60 | 54.4 |
| TA-VLA | 65 | 35 | 60 | 60 | 75 | 55 | 40 | 60 | 65 | 57.2 |
| ImplicitRDP | **90** | 55 | 70 | **85** | **90** | 65 | 50 | 75 | 75 | 72.8 |
| Temporal Teacher | 75 | 55 | 70 | 80 | 85 | 65 | 50 | 70 | 70 | 68.9 |
| **ForceDelta-VLA (Ours)** | **90** | **80** | **80** | **85** | **90** | **80** | **70** | **80** | **85** | **82.2** |

#### 接触质量

ForceDelta-VLA 在两个平台聚合结果上均取得所有方法中最低的峰值接触力（见表 III）。相较 Temporal Teacher，单臂任务的峰值力从 15.1 N 降至 11.8 N，双臂任务从 14.7 N 降至 12.5 N，分别降低 21.9% 和 15.0%；相较 ImplicitRDP，降幅分别为 15.7% 和 18.8%。这种改善在两个平台上方向一致，并伴随更高的任务成功率，与“降低接触严重程度而不采用更保守执行或牺牲完成率”的解释一致。

**表 III：峰值力与完成时间。** 峰值力使用全部试验，完成时间使用成功试验；数值为均值 $\pm$ 标准差。

| 方法 | 单臂峰值力 ↓ | 双臂峰值力 ↓ | 单臂完成时间 ↓ | 双臂完成时间 ↓ |
|---|---:|---:|---:|---:|
| $\pi_{0.5}$ | 18.9 $\pm$ 8.3 | 20.4 $\pm$ 9.1 | 30.4 $\pm$ 11.2 | 33.1 $\pm$ 12.8 |
| ForceVLA | 16.0 $\pm$ 4.7 | 16.8 $\pm$ 6.2 | 29.1 $\pm$ 6.5 | 31.7 $\pm$ 9.4 |
| TA-VLA | 16.5 $\pm$ 6.4 | 15.9 $\pm$ 4.8 | 28.2 $\pm$ 9.7 | 30.0 $\pm$ 7.1 |
| ImplicitRDP | 14.0 $\pm$ 3.5 | 15.4 $\pm$ 5.3 | 25.8 $\pm$ 5.9 | 27.9 $\pm$ 8.4 |
| Temporal Teacher | 15.1 $\pm$ 5.8 | 14.7 $\pm$ 4.3 | 27.8 $\pm$ 8.8 | 29.2 $\pm$ 6.9 |
| **ForceDelta-VLA (Ours)** | $\mathbf{11.8}\pm 3.4$ | $\mathbf{12.5}\pm 2.9$ | $\mathbf{23.9}\pm 5.4$ | $\mathbf{25.8}\pm 7.6$ |

#### 失败分析

#### 定性失败模式

对 ForceDelta-VLA 失败轨迹的检查显示三种反复出现的局限。第一，错误 reference 可能使轨迹超出有界 residual 的修正范围。第二，reference age 超过训练调度的覆盖范围时，缓存的局部规划可能与持续变化的机器人—环境状态不一致。第三，卡滞或错误侧接触可能需要回退、重新接近或策略切换等非局部恢复，而这些行为无法由局部位姿修正表达。

### 4.3 独立残差执行

两个实验用于检验独立残差执行。Runtime profiling 量化 reference 与 residual 通路之间实际实现的频率分离；reference-delay 实验则检验当缓存 reference 逐渐陈旧时，这种频率分离能否改善接触行为。

#### 实际频率分离

表 IV 报告相同按按钮部署设置下各策略通路的独立 batch-one 模型前向延迟。100 Hz Cartesian target-update 上限对应名义 10 ms 单次更新计算预算。ForceDelta residual 的前向耗时为 2.43 ms，约占该预算四分之一；Temporal Teacher 和 ForceDelta reference 分别需要 176.4 ms 和 189.7 ms，不能在每次 target update 时重新运行。因此，生成式 reference 异步刷新并缓存复用，而 force-conditioned correction model 可以在名义单次更新推理预算内运行。

| 方法/路径 | 推理延迟 (ms) ↓ |
|---|---:|
| ImplicitRDP reactive | 12.86 $\pm$ 0.23 |
| Temporal Teacher | 176.4 $\pm$ 5.9 |
| ForceDelta reference | 189.7 $\pm$ 6.8 |
| ForceDelta residual | **2.43** $\pm$ 0.12 |

#### 延迟 reference 鲁棒性

我们在 USB 插入和插头插入任务上施加 0、100 和 200 ms 的额外 reference-publication delay；每个任务、方法和延迟条件测试 10 次。对两个任务聚合后，ForceDelta-VLA 在所有测试延迟下均保持比 Temporal Teacher 更高的成功率和更低的峰值力（见图 5）。从 0 ms 增加到 200 ms 时，ForceDelta-VLA 的成功率从 75% 降至 60%，Temporal Teacher 从 60% 降至 35%；二者的峰值接触力分别从 12.5 N 增至 16.2 N 和从 16.0 N 增至 25.6 N。这些结果表明，residual framework 相比直接执行 Temporal Teacher 对 reference availability delay 更鲁棒。

**图 5：两项插入任务对额外 reference-publication delay 的敏感性。** 左：成功率；右：峰值力。蓝色实线与圆形标记表示 ForceDelta-VLA，灰色虚线与方形标记表示 Temporal Teacher。

### 4.4 消融实验

我们将消融分为两组（见表 V）。**执行/组件消融**保持已训练模型不变：**Reference only** 不使用 learned corrections 或 state rebasing；**w/o force residual** 关闭 force output；**w/o learned staleness residual** 保留 $\Gamma(S_t,S_k)$，只关闭 learned staleness output；**Shuffled force history** 保留 force history 的经验边缘分布，但破坏其与当前观测和动作的对应关系；**Zeroed residual input** 将实测力历史替换为全零张量，同时保留 force branch。

**监督/分解消融**重新训练受影响的变体：**Demonstration-derived residual target** 将同一查询状态下的示范动作减去力输入缺失模式的 teacher 预测；**Zero-wrench teacher query** 在构造 reference 和监督目标时，以输入全零 wrench 历史的 teacher 查询替代学习式力输入缺失模式；**w/o schedule replay** 在 residual 训练中不采样异步查询周期和 reference-query latency schedule；**Single combined-residual head** 在保留 deterministic rebasing 的情况下，使用一个 head 预测两个 learned targets 之和；**Fast full-action distillation** 保持相同的小型 student 输入与更新调度，但直接蒸馏冻结 teacher 的力条件位姿动作。

**表 V：四项任务上的消融结果：单臂 USB 插入和擦白板，以及双臂插插头和打开抽屉。成功率和峰值力分别按两个平台报告。**

| 变体 | 单臂成功率（%）↑ | 双臂成功率（%）↑ | 单臂峰值力（N）↓ | 双臂峰值力（N）↓ |
|---|---:|---:|---:|---:|
| *执行/组件消融* |  |  |  |  |
| Reference only | 62.5 | 50.0 | 16.7 $\pm$ 6.9 | 18.2 $\pm$ 7.6 |
| w/o force residual | 70.0 | 57.5 | 15.4 $\pm$ 5.8 | 17.1 $\pm$ 6.3 |
| w/o learned staleness residual | 77.5 | 67.5 | 13.2 $\pm$ 4.1 | 14.5 $\pm$ 5.0 |
| Shuffled force history | 55.0 | 45.0 | 18.4 $\pm$ 8.1 | 20.1 $\pm$ 8.7 |
| Zeroed residual input | 65.0 | 55.0 | 15.9 $\pm$ 6.4 | 17.7 $\pm$ 7.0 |
| **ForceDelta-VLA (full)** | **85.0** | **75.0** | $\mathbf{11.7}\pm 3.1$ | $\mathbf{12.8}\pm 3.8$ |
| *监督/分解消融* |  |  |  |  |
| Demonstration-derived residual target | 72.5 | 57.5 | 14.6 $\pm$ 5.2 | 16.5 $\pm$ 6.1 |
| Zero-wrench teacher query | 65.0 | 55.0 | 16.1 $\pm$ 6.7 | 17.3 $\pm$ 6.5 |
| w/o schedule replay | 77.5 | 62.5 | 13.8 $\pm$ 4.7 | 15.6 $\pm$ 5.8 |
| Single combined-residual head | 75.0 | 65.0 | 13.5 $\pm$ 4.3 | 14.9 $\pm$ 4.9 |
| Fast full-action distillation | 67.5 | 52.5 | 15.2 $\pm$ 5.6 | 17.9 $\pm$ 7.4 |
| **ForceDelta-VLA (full)** | **85.0** | **75.0** | $\mathbf{11.7}\pm 3.1$ | $\mathbf{12.8}\pm 3.8$ |

在力输入诊断中，打乱力历史导致最大的退化：单臂/双臂任务组的成功率从 85.0%/75.0% 降至 55.0%/45.0%，峰值力从 11.7/12.8 N 升至 18.4/20.1 N。将力历史置零后，成功率降至 65.0%/55.0%，峰值力升至 15.9/17.7 N。Shuffled input 保留力历史的边缘幅值分布，但破坏其与当前交互状态的配对；该退化说明 residual policy 使用了正确配对的力输入。

单一组合 head 的成功率为 75.0%/65.0%、峰值力为 13.5/14.9 N，完整模型则为 85.0%/75.0% 和 11.7/12.8 N。移除 schedule replay 后峰值力升至 13.8/15.6 N。保留 $\Gamma$、仅关闭 learned staleness residual 时，成功率为 77.5%/67.5%、峰值力为 13.2/14.5 N；关闭 force residual 时则为 70.0%/57.5% 和 15.4/17.1 N。因此，learned staleness residual 的影响小于 force residual。分离的 force-conditioned 与 force-masked predictions 优于对两项目标之和进行预测的单一 predictor。完整动作蒸馏的成功率为 67.5%/52.5%、峰值力为 15.2/17.9 N，支持 reference-anchored residual prediction 相比紧凑完整动作生成的优势。示范导出目标和 zero-wrench teacher query 同样使两项指标退化。总体而言，最清楚的效果来自正确配对的力输入和 reference-anchored residual prediction。

### 4.5 未见物理条件下的泛化

我们评估学习得到的局部修正能否在示范数据未覆盖的物理条件下保持有效。USB 插入将初始插入偏角范围由<span style="color:red">TBD（训练范围）</span>调整为<span style="color:red">TBD（未见测试范围）</span>；白板擦拭则将训练表面替换为<span style="color:red">TBD（未见表面或摩擦条件）</span>。在相同的初始状态分布、控制器设置和安全限幅下，比较 Temporal Teacher 与 ForceDelta-VLA，每种方法和条件测试<span style="color:red">TBD</span>次。成功率衡量任务是否完成，峰值接触力衡量接触动力学发生变化后的交互质量。

**表 VI：在示范数据未覆盖的物理条件下进行泛化评估。所有 TBD 项将在固定 held-out 评估协议完成后替换。**

| 任务 | 方法 | 成功率（%）↑ | 峰值力（N）↓ |
|---|---|---:|---:|
| USB 插入 | Temporal Teacher | <span style="color:red">TBD</span> | <span style="color:red">TBD</span> |
|  | ForceDelta-VLA | <span style="color:red">TBD</span> | <span style="color:red">TBD</span> |
| 白板擦拭 | Temporal Teacher | <span style="color:red">TBD</span> | <span style="color:red">TBD</span> |
|  | ForceDelta-VLA | <span style="color:red">TBD</span> | <span style="color:red">TBD</span> |

<span style="color:red">TBD：报告绝对性能、相对分布内条件的性能下降，以及 residual pathway 在各项物理偏移下是否仍能维持更低的峰值力。</span>

## 5. 结论与未来工作

本文提出 ForceDelta-VLA：从缓存上下文下的 teacher 查询构造力修正与陈旧性修正，并使其独立于生成式 reference 执行。在九项真实机器人任务上，ForceDelta-VLA 达到 82.2% 的平均成功率；相较 ImplicitRDP，单臂和双臂平台的峰值接触力分别降低约 16% 和 19%，并在两个平台上取得最低的成功试验平均完成时间。轻量 residual 符合平台的名义 target-update 计算预算，生成式 reference 则异步刷新。力输入诊断显示策略对正确配对的 wrench history 敏感，dual-head 和 schedule replay 消融支持该分解。

该方法的能力受缓存 reference 的局部性和覆盖范围限制：位姿 residual 无法替代错误的任务级规划，也无法执行回退、重新接近和策略切换等非局部恢复。快速通路还假设缓存视觉上下文在一个 reference interval 内保持有效，因此对未反映在机器人状态或 wrench 中的场景变化响应有限。最后，机器人估计的 wrench 限制了系统对细微接触事件的敏感性。未来工作将结合置信度触发的 reference refresh、轻量级当前视觉更新、更丰富的接触感知，以及利用纠正性 rollout 训练的恢复策略。
