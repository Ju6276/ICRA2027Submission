# ForceDelta-VLA teaser visual draft v1

Generated using the built-in image_gen tool. Preview only; main.tex is unchanged.

## Initial prompt

Use case: scientific-educational / infographic-diagram.
Create ONE polished graphical abstract / teaser image for a robotics research paper named ForceDelta-VLA. This is a fresh design preview, NOT a TikZ-looking flowchart. Wide landscape about 3:1 aspect ratio, high resolution ideally 3072 x 1024. White background, restrained navy text, blue reference paths, orange learned corrections, muted purple offline supervision. Modern editorial robotics illustration, crisp professional typography, restrained isometric robot rendering, spacious composition, readable at full paper width. NOT a marketing poster: no hype, rankings, fabricated plots, metrics, medals, neon, or decorative slogans. Illustrations represent a conceptual method, not evidence photographs.

Composition:
Small title "ForceDelta-VLA" upper left, subtitle "Distilled force corrections for contact-rich manipulation".
Left roughly 22%: modest comparison panel labeled "Full-action prediction". A compact elegant abstract network glyph receives "Vision · Language · State · Force", emits one small blue/orange integrated sequence glyph labeled "Full action chunk". Avoid derogatory red warnings or crosses. This is secondary to the proposed method.
Right roughly 75%: a visually engaging but scientifically precise proposed method, arranged as a shallow upper offline strip and a larger lower deployment area. At the far right of the lower area show an attractive editorial 3D illustration of a white 7-DoF single robot arm with black joints and parallel gripper guiding a USB connector toward a port on a fixture. Blue dashed local reference trajectory and orange short local correction near contact, restrained contact cue. Keep geometric objects recognizable; gripper actually grasps connector. No humans.

Upper strip heading "Force-residual supervision". Show a compact frozen teacher icon with two paired prediction mini-panels, labels exactly "Force-conditioned" and "Missing-force", with explicit subtraction order force-conditioned MINUS missing-force. They share one small annotation "Shared context and noise". Their difference produces a small orange label "Force residual target". A purple DASHED arrow labeled "Distill" leads from this target to the residual policy in the lower area; no solid deployment data arrow for this supervision. This strip illustrates the force supervision component, not the full three-stage training.

Lower area heading "Asynchronous execution". Show a slim blue element labeled "Cached reference", sourced from a small label "Missing-force VLA". The cached reference must have TWO outgoing paths: one blue arrow into "Residual policy" as conditioning, and an important explicit blue BYPASS arrow directly into a separate circular PLUS composition node at the right. Lower left also include two clear inputs "Wrench history" and "Current state", each with its own arrow into "Residual policy". The residual policy can look like a layered compact network tile, NOT a large generic rectangular box. Its orange outgoing arrow labeled "Pose corrections" enters the same PLUS node; these corrections represent learned force and staleness residuals. A separate small gray input "State rebasing" also enters that PLUS node. The plus node combines exactly cached reference, pose corrections, and state rebasing, and outputs a single arrow labeled "Pose command" toward the robot illustration. Place a small sober label "100 Hz command interface" adjacent to this final output only. Do NOT label the residual inference frequency as 100 Hz. The residual policy MUST NOT directly output a complete action without the plus composition and reference bypass.

All visible copy should be limited to the quoted labels above. Use clean, legible, professionally typeset English with correct spelling, clear hierarchy and short text. Nodes, arrows, and robot must not overlap. The state input should be visually distinct from measured wrench. The blue reference bypass must be obvious, with minimal crossings. No dense equations, no legend paragraphs, no axes, no figure numbers, no caption text. The overall feel is a high-quality ICRA/RA-L paper teaser with a tangible manipulation scene and clear conceptual contribution, not a slide full of boxes.

## Targeted correction prompt

Edit this ForceDelta-VLA graphical abstract with exactly one scientific wiring correction. Preserve all layout, text, robot illustration, white background, colors, reference bypass and other arrows unchanged.
The purple dashed arrow labelled "Distill" currently ends at the circular PLUS command composition node. This is WRONG: the offline Force residual target supervises the orange Residual policy neural network, NOT the plus node.
Remove that entire existing purple dashed path and its "Distill" label. Draw a new short, carefully routed purple DASHED supervision arrow from the orange "Force residual target" element in the upper panel down to the TOP of the orange "Residual policy" network tile in the lower panel. Put the "Distill" label next to this new dashed arrow. Its arrowhead must touch the orange policy tile top, nowhere near the circular plus node. Route the arrow through available whitespace with minimal crossings and keep the "Residual policy" text readable. The plus node must now have only three input arrows: blue cached reference, orange Pose corrections, and gray State rebasing. Do not remove or change any of those three inputs. No other changes.

