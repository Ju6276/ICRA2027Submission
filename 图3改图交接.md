# 交接:图 3(异步部署图)精简改写

本文件用于把工作从 Linux 工作站转到 Mac。在 Mac 上 `git pull` 后即可继续。
目标文件:`figures/fig03_async_deployment.tex`(TikZ 源码,需重新编译生成 PDF)。

---

## 一、待做的任务

### 1. 必改:术语撞车(唯一会导致误读的实质问题)

源码约第 50 行:

```latex
\node[note,anchor=west] at (7.62,1.55)
  {expired prefix skipped\\for execution};
```

**问题**:`prefix` 在正文里是专有含义,指视觉语言前缀 $E_k$(见 `03_methods.tex`:"let $E_k$ be the visual--language prefix";"reuse of the cached visual--language prefix")。而这句话想说的是**修正块里已过期的时间步**,与 $E_k$ 无关。更糟的是这句注释画在 "CACHED REF. + CONTEXT" 泳道上,即 $Z_k^{\mathrm{intent}}$ 所在位置,读者会理解成"缓存上下文被跳过了"——这是错的。

**改法**:改为 `expired correction steps skipped`,与 `03_methods.tex` 的 "Expired correction steps are skipped" 用词一致。

### 2. 必改:记号只在补充材料里定义

源码约第 55 行:

```latex
\node[note,anchor=west,text=cFast] at (1.90,0.82)
  {Fast observation $\mathcal F_{t_q^F},S_{t_q^F}$};
```

**问题**:$t_q^F$ 在正文中一次都没出现,只在 `supplementary_material.tex` 第 82 行定义。正文用 $t$ 表示残差查询时刻。图是正文的图,读者不应为看懂图去翻补充材料。

**改法**:改为 $\mathcal F_t, S_t$。

### 3. 建议改:缓存切换时刻可能被读错

发布边界虚线在 `x=4.45`,快刻度在 `2.15, 2.85, 3.55, 4.20, 4.80, 5.50, ...`。

按 `03_methods.tex`:"a newly published reference is used only by subsequent residual queries",**`4.80` 这一格就应已在读 packet $k$**。但 `read packet $k$` 箭头指向 `5.65`(即 `5.50` 那格),中间隔了一格,读者容易理解成 `4.80` 仍在用旧包,与正文矛盾。

**改法**:箭头改指 `4.80`,或在 `4.80` 上加标记。

### 4. 建议改:同一事件两个标签

`x=4.35` 的注释 `available $\bar t_k$` 与 `x=4.45` 虚线的 `publish packet $k$` 是同一时刻的两个名字(正文定义 $\bar t_i$ 即 publication time)。合并为一个。

---

## 二、精简方案(可选,但收益最大)

### 诊断

当前图**同时是架构图和时序图**:三条泳道对应三个组件(架构图思路),横轴又是时间且有离散刻度(时序图思路)。两套视觉语法叠加,元素数量翻倍。

源码统计:**约 32 个图元,其中 10 个是文字注释**。参照 RTC(NeurIPS 2025)Figure 3 —— 该方向最标准的异步时序图 —— 只用一条时间轴、一排离散动作格,信息全部靠**格子填充色**编码(冻结区 / 指数衰减区 / 全新生成区),**只有三个区域标签**。

差别不在信息量,在于信息编码在**几何和颜色**里还是编码在**句子**里。

另一类参照(DuoCore-FS、Libra-VLA、FiS-VLA 等快慢系统论文)**根本不画时间轴**:两个块 + 中间一个 buffer,频率直接写成块上标签("1–3 Hz"、"25–30 Hz")。

**结论:画时序图这个选择是对的**,比只标频率的做法更契合本文贡献(陈旧性、包绑定、过期步骤都需要时间轴才说得清)。问题只是用了两倍的墨。

### 三招减法

**(a) 把"读哪个包"编码进快刻度的填充色,删掉整条缓存泳道。**

当前用「2 个缓存框 + 3 条箭头 + 2 条 read packet 注释」表达"发布前读旧包、发布后读新包"。改为发布线左侧 $\Delta$ 格填灰(`cOld`)、右侧填绿(`cCache`),一眼可见,无需箭头与注释。

删除:1 个泳道标签、2 个缓存框、3 条箭头、2 条注释 = **8 个元素**,并腾出一整行纵向空间。
包内容 $A_k^{\mathrm{ref}}, Z_k^{\mathrm{intent}}, S_k$ 移入 query 框或 caption。

**(b) 底部长注释搬进 caption。** 源码约第 76–78 行那条两行注释,以及 `each correction chunk remains bound to the packet it reads`,本质都是 caption 句子,不该长在图里。

**(c) 合并重复标注**(见上文第 4 条)。

减完约 **12 个图元**,与 RTC 同量级:2 条泳道、1 条发布线、一排双色刻度、1 个跨度括号、3 个短标签。

### 比例尺(可不改)

图上参考周期约 3.4 单位、快刻度间隔约 0.68,比例 **5:1**;实际参考约 5 Hz、快路径 100 Hz,即 **20:1**(按推理时间算 78:1)。示意图压缩比例是普遍做法,不算错,但这张图的唯一目的就是展示频率分离,画成 5:1 把卖点缩小了四倍。版面允许的话把快刻度加密一倍。

---

## 三、图 3 逻辑本身没有问题

三条泳道(参考查询 / 缓存包 / 快指令)、发布边界、修正块绑定到所读的包、query $k{+}1$ 挂起期间残差继续运行 —— 均与 `03_methods.tex` 的 "Independent Residual Execution" 一节一一对应,无矛盾。上述条目中只有第 1 条会导致误读。

---

## 四、本轮已完成的正文改动(供上下文,无需重做)

基线 `fdaba2e` 之后的提交:

- `6295139` — 式 (10) 删掉代码中不存在的 $P_\Gamma$ 条件 token 并说明 $\Gamma$ 只进入监督目标;§III-A 补双臂维度与共享 bound;式 (11) 补恒等式说明;成功率段删除无数据支撑的机制主张;消融段修正两处方向错误的推论。
- `f9f0ecc` — "ordered conditioning sequence" → "conditioning set",补无位置编码说明。
- `d4b7f24` — 删去过度解释的半句。
- `daf3984` — Runtime 表由 worker packet rate 改为与 100 Hz 控制周期对比(消除了"基线开销为负"与"138.6 Hz 超过指令率"两个可被算术证伪的点)。
- `a453c5b` — 他人提交,采纳了 Task performance 段与 §IV-C 端点值的改写。

---

## 五、仍未处理的事项

- **§IV-C 是否加入 RTC 对照。** RTC(Black et al., NeurIPS 2025)是 flow-based chunk policy 推理延迟的标准方法,而 §IV-C 是全文唯一直接验证"异步设计带来延迟鲁棒性"的实验。该实验仅 2 任务 × 3 延迟,补一条 `Temporal Teacher + RTC` 曲线成本极低。注:RTC 未在本项目的 openpi fork 中实现。
- **图 4a 的 0 ms 点与 Table II 不一致。** 图中 Temporal Teacher 在 0 ms 为 60%,而 Table II 中该方法在 USB insertion / plug insertion 上为 55% / 50%(均值 52.5%)。本方法一侧则完全吻合(80% / 70%,均值 75%)。合理解释是两批独立试验(10 次/任务 vs 20 次/任务),建议在 §IV-C 加半句说明,否则读者会问"哪个是对的"。
- **Fast full-action distillation 是否提升到主表。** 它是与 fast full-action 生成路线的唯一受控对照,现仅在消融中出现且只覆盖 4 个任务。
- **补充材料第 38 行**仍写着 "the realized publication rate is measured directly",与正文新口径(不再报发布率)不一致,待同步。
