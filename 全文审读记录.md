# ForceDelta-VLA 全文审读记录

本轮以英文正文为主，核对摘要、引言、相关工作、方法、实验、结论、图示和中文稿。以下区分已修正的文字/一致性问题与需要真实实现或日志才能确认的问题。没有重新核验实验原始数据，也没有把内部补充材料的占位内容视为真实配置。

## 已完成

- 删除 Shuffled force history 的定义、表格行、结果分析和结论；同步中文稿和待办记录。保留其余消融和全部剩余实验数值。
- 保留 FAVLA-style causal TCN 的方法引用及 Temporal Teacher 对照，不将借用编码器作为本文创新。
- 摘要将“无需奖励、无需在线纠正、无需额外示范”的连续否定简化为“仅使用原始离线示范数据集”。保留 missing-force/zero-wrench 的必要语义区分。
- 将“注意力模块 Φ 置换不变”改为“条件 token 连同类型嵌入一起置换时，固定 residual-query 输出不变”。注意力模块整体的置换等变性与固定 query 输出的不变性不能混用。
- 将“100 Hz 对应 10 ms 模型推理预算”改为“100 Hz 更新间隔为 10 ms，2.43 ms 前向耗时低于此间隔”。不从 isolated forward latency 推导真实更新率。
- 相对 Temporal Teacher 的成功率提升按表中未舍入成功次数计算为 (148−127)/180×100=11.666… 个百分点，统一为 11.7。其余主表均值与所列任务结果相符。
- 消融分析突出教师差分监督、全动作蒸馏、双 head 和 schedule replay 的具体对照；去掉重复的贡献辩护式说明。
- 泛化结果用具体成功率和峰值力代替“competitive”“degrades substantially”等主观评价，保留原有红色待确认标记。
- 单条定性轨迹的描述限定到该 rollout；将恢复失败的绝对断言改为“所需运动可能超出有界修正范围”。
- 中文稿同步英文的解析 Γ：从 learned staleness target 和网络输入中移除，放入最终组合，并与 learned staleness 一起限幅；同步两阶段 intent pooling（M=16，Q=2）、固定 schedule replay、成功试验统计范围和当前泛化表。修复旧图号（定性图 5、延迟图 6）。
- 修复平台图的失效引用：源码原引用不存在的 setup_franka.png/setup_XR.png，改为目录中已有 frankasetup.pdf/xrrobotsetup.pdf。未修改图片内容。

## 尚需按实现/日志确认

1. **双臂平台图与名称。** 正文写 X Square Robot，现有 xrrobotsetup.pdf 的机械臂上可见 YASKAWA 标识。品牌标识本身不能确定整个平台名称；需要确认该图是否就是实际评估平台。此次仅修复路径，没有更改平台身份声明。
2. **教师与学生的 force encoder 关系。** 方法中分别用 z_t^F 与 u_t^F 表示两条路径的力表征，但图 2 写 u_t^F=E_T^F(F_t)，与教师使用同一编码器符号。需要说明是仅架构相同、复制初始化，还是共享/冻结同一组参数；不能仅凭“FAVLA-style”确定。
3. **残差目标的学生信息。** 目标依赖未来 reference samples 及共享 flow noise，正文列出的学生输入只有当前 reference sample 与压缩 intent。需要说明 flow noise 是否固定，以及未来 reference 是否确实不作为输入。这是待澄清的可学习性条件，不是已证实的实现错误。
4. **异步执行与消融协议。** 正文仍缺 H/K/T_F、各查询和有效更新频率、schedule 的具体分布/固定参数、clip 数值，以及 reference 用尽后的处理。Demonstration-derived target 替换哪个监督、w/o schedule replay 使用什么同步或固定延迟设置，也需要明确。保留现有实验，不用猜测值填充。
5. **missing-force 对照的解释边界。** missing-force query 使用独立训练的 adapter，zero-wrench teacher-query 对照同时改变 reference 与 target。因此它评价整个 missing-force 方案，不单独识别 token 的贡献；正文继续使用 teacher-defined residual，不解释成力的因果效应。
6. **泛化结果状态。** 三种白板—板擦配置的数字仍为红色。此次仅整理表述和同步翻译，不能由文字修改认定实验已最终确认。新物体组合同时改变视觉、几何与材料，现有协议不能单独识别摩擦因素。
7. **结果与实现版本。** 原有待办记录仍包含“解析 rebasing 改动后需同步实现、重新训练并核对实验”的未完成项；无法从论文文件判断该记录是否过期。需要按 checkpoint、配置和最终日志确认，不能仅靠公式一致就认定表格已验证新实现。
8. **内部补充材料尚未同步。** supplementary_material.tex 中仍有旧的 Γ 组合、16 个 intent token、per-arm bounds、全部试验 peak force 与 macro-average 等记录，与当前正文不同。它们不能直接作为最终补充材料提交，也不应当用于推断真实配置。本轮未将内部暂定内容覆盖到英文正文。

## 验证

- 主文 latexmk 编译成功，生成 9 页 PDF（含参考文献和一页任务附录）。
- 未发现未定义引用、重复 label 或 overfull box；仍有 caption 包兼容性/锚点及少量 underfull box 提示。
- 已查看平台图及包含消融、泛化表的渲染页；PDF 不再包含 Shuffled force history。
- 页数仅为当前构建结果，本轮未核验投稿年份的具体格式政策。

## ImplicitRDP 统计纠正

作者随后提供修正的主实验汇总数值：九项成功率为 50/25/45/35/65/55/30/55/45%，均值 45.0%；单臂/双臂峰值力为 15.2±5.1 / 14.8±4.9 N，完成时间为 27.1±7.2 / 27.4±7.8 s。已更新正文和中文稿，重新计算相对成功率差（37.2 个百分点）及峰值力降幅（22.4%/15.5%）。摘要与结论随后按作者要求改为主要对比 ForceVLA，Temporal Teacher 保留为受控对照。原始日志未在工作区提供；独立泛化和运行时数据未随主实验数值替换。
