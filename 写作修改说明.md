> 当前术语更新：state rebasing 已统一改为 reference-state alignment（参考状态对齐），包括正文、补充材料与图 1/2。Γ 的定义与训练／执行用法不变。完整中英文对照见 [术语修改对照表](术语修改对照表.md)；以下旧记录中的名称仅代表历史版本。

> 行文结构调整：明确已有参考动作与两个当前教师预测的区别；先解释双修正动机并给出延迟目标，再定义 Γ 的归一化计算；压缩摘要、引言及相关工作中重复的方法细节。保留两项方法贡献和一项性能提升；公式内容与引用集合不变，中文对应段落已同步。

> 英文用语审读：通读摘要、引言、相关工作、方法、实验、结论和补充材料，简化 operating point、exercise off-grid interpolation、nonlocal recovery 等抽象表达，展开过密的名词短语。保留三条技术贡献及 force-agnostic、state rebasing 等必要术语；公式和结果表保持不变。图 1 图注明确约 6 Hz 修饰参考动作更新。正文和补充材料均编译通过。

> 作者最新澄清：100 Hz 仅指机器人控制指令发送频率，由控制循环限频；不能据此认定修正网络每 10 ms 启动一次。此前关于“快速修正 100 Hz”及图 3 的 10 ms 推理调度是假设，现已撤销。图 1 的 100 Hz 标注移到命令输出端，图 3 改为单栏三行时序示意图，分别表示参考生成、修正推理和机器人指令；颜色表示参考配对关系，突出跨越参考完成时刻的修正仍保留旧参考。仅控制指令行标注 100 Hz，不标修正查询频率，时间轴不按实际比例绘制。前向耗时 2.43 ms 与命令发送频率分别报告。此澄清优先于以下旧记录。

> 图 3 清晰度更新：移除未解释的 q 编号，改用 Update 1–3；聚焦一次参考动作交接，以相对发布时间的毫秒轴显示 10 ms 修正周期与示意的 2.4 ms 计算时间，明确非实测轨迹。区分可用参考与最终输出使用的参考，并用圆圈/方块标出输入读取和计算完成。共享参考在查询启动时绑定，保持现有执行定义。

> 频率与写作更新：按作者本轮明确提供的信息，图 1 标注 ForceVLA 约 6 Hz、慢参考通路约 6 Hz、快速修正 100 Hz；这些运行频率来自作者确认，不是由前向耗时倒数推算。同步实现细节。图 3 改为非等比例的事件顺序示意，移除 K=5 及执行步骤编号，突出查询启动时绑定参考动作与完成后替换修正；K=5 配置仍保留在方法/实现说明中。Task success 改为 Quantitative results。结论收紧为方法、结果、边界和后续方向，未加入评价性形容词。

> 图 1/2 适配完成：图 1 区分力目标和延迟目标，并突出参考动作更新之间的响应；图 2 补齐三项预测、Γ 与双目标的依赖连接。去除图内旧 Offline/Online 和 base mode 表述，保留共享模块两次计算、延迟力输入遮罩和三项动作组合。英文图注及中文翻译已同步；首页右侧两个 rollout 占位仍保留。

> 摘要更新：以作者提供的较早摘要为底稿，恢复“动作耦合—分解标签缺失—教师差分蒸馏”的叙事。统一 reference action、force correction、delay correction；保留共享缓存上下文和异步调度重放，删除重复的 offline/online 及数据来源限定。方法定义为 correction-distillation framework，轻量策略明确为其组成部分。英文摘要和中文翻译已同步；保留成功率和成功试验峰值力结果。

> 排版更新：Fig. 3 由双栏 figure* 改为单栏 figure，按栏宽重新调整标签、字号与时间轴布局，允许就近放置。原双栏页顶图与章节浮动屏障共同造成的近空白页已消除；本次修改前 PDF 为 11 页，修改后为 10 页（含参考文献与任务附录）。方法文字与公式保持不变，编译无 overfull box 或未定义引用；已目视检查方法末尾与实验开头的衔接。

> 贡献表述更新：按作者意见恢复三项技术贡献——force-agnostic correction distillation、state-consistent dual-correction decomposition、independently executable correction pathway。删除贡献列表中的 Real-robot evaluation；真机结果保留在引言结果段和实验章节。本文贡献应突出方法定义与机制，不能在改善可读性时改写成泛化的工作分类。此决定优先于下文较早的“监督、执行、评估”组织记录。

> 最新表述更新：将图 2 和正文的公共模块统一为 Shared attention module，明确两次前向共享参数、分别使用完整条件与屏蔽力输入的条件。摘要和引言分别说明离线力修正监督、延迟补偿目标及独立在线更新。同步中文稿，修正图 2 力监督箭头起点和预测步数 K 的标注；数学定义、实验数值及首页待补图片位置不变。

> 后续术语更新：动作通路输出统一称 **reference action（参考动作）**，表示慢通路此前预测、供快通路修正的动作；公式上标由 `base` 同步为 `ref`。保留 **force correction + delay correction**。缓存只作为执行机制解释；Γ 仍包含在 delay target 中，部署不另加。正文、中英文补充材料及三张方法图已同步。这一更新优先于下文较早的术语记录。

# ForceDelta-VLA 写作修改说明（2026-09-08）

## 修改范围

按作者要求改善叙事、可读性和章节结构，不增加实验。核心方法定义以修改前英文正文为依据，未推定实现或实验日志已经重新核验。

## 主要改动

1. **摘要与引言**：以“从离线示范构造可独立执行的修正监督”为主线；按接触任务需求、监督困难、教师差分方法、已有证据展开。贡献改为离线监督、独立执行与真机评估三层。保留 ForceVLA 主要比较，前移 Temporal Teacher 受控对照。
2. **方法**：改为 Overview and Notation → Teacher and Force-Agnostic Base Policy → Paired Correction Targets → Correction Policy and Training → Independent Correction Execution。公式前解释缓存基础动作、刷新无力输入预测、力条件预测三者关系。池化、时间特征和条件 token 等细节移至补充材料。
3. **术语**：统一基础动作、力修正和延迟修正；明确延迟目标已包含 Γ，部署不重复添加解析项。修正始终是教师定义的预测差异。相关工作中旧的独立 deterministic rebasing 表述已修正。
4. **实验**：主结果先定量后定性；消融按监督来源、预测形式和异步适配组织，同时保留是否重训的协议。未见物体泛化使用与实验操作一致的标题。失败模式集中到 Limitations。
5. **暂定内容**：正文移除 Analytic state rebasing 和 Current base-action sample only 两行暂定估计及其结果性描述；补充材料移除尚无结果的修正边界敏感性表。这些删除不构成新增实验要求。
6. **图文**：重绘图 1 左侧为核心思路图，详细网络留在图 2，异步时序留在图 3。按作者安排保留首页右侧两个 rollout 位置；后续在 figures/draw_fig01_method_overview.py 的 COMPARISON_IMAGES 中填入图片路径再生成。图注已删除制作备注。
7. **同步**：重写中文主稿及补充材料翻译，统一公式、术语、统计范围与图表编号。两份内部审读/待办记录增加当前状态，并将旧内容标为历史记录。

## 验证

- 主稿 latexmk 编译通过：main.pdf 共 10 页，包含参考文献及一页任务附录；本轮未调整投稿页数政策。
- 补充材料 latexmk 编译通过：supplementary_material.pdf 共 3 页。
- 逐行比对确认，保留的所有含数值实验表格行与修改前一致。
- 力目标、延迟目标、Γ、命令等价关系、动作组合、条件集合、双输出及损失共九组公式块与修改前一致。
- 正文交叉引用全部可解析，无重复 label、未定义引用或 overfull box。
- 已查看完整 PDF 页面缩略图及首页、方法页细图；图 1 的 rollout 空位是作者明确保留的后续素材位置。
- 仍有原有 caption 包兼容性及 hypcap 提示，未阻止编译。

## 仍保留的作者事项

- 首页右侧实际 rollout 图像由作者后续补入。
- 补充材料中原有红色训练配置、成功阈值和少量协议项仍待按最终记录核对；本轮没有用推测值将其改为已确认信息。
