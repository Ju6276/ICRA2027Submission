# ForceDelta-VLA 补充材料中文翻译

> 对应当前英文补充材料。红色文字仍为内部待核对配置或协议，不代表本轮已核验；本轮未新增实验。

## 1. 训练与架构细节

<span style="color:red">教师采用 AdamW 训练 50,000 步，峰值学习率为 $2\times10^{-5}$。它使用 100 Hz wrench 流中最近 100 ms 的历史，预测 $H=50$ 个动作；flow-matching 推理使用 $N_{\mathrm{flow}}=10$ 步。</span>

冻结教师后，<span style="color:red">使用 $10^{-4}$ 学习率训练 rank-32 无力输入 adapter 10,000 步。视觉—语言前缀池化到 $M=16$ 个位置区间，再投影为 $Q=2$ 个任务上下文 token。修正策略包含因果力编码器和一个处理条件 token 的注意力模块。层归一化在注意力计算之前执行，力修正与延迟修正使用分别初始化为零的独立输出投影。预测长度为 $K=5$，使用 $3\times10^{-4}$ 学习率在采样异步调度下训练 30,000 步。延迟前向计算在注意力有效性掩码中屏蔽力 token 位置。</span>

### 1.1 动作表示与上下文编码

位姿动作 $\mathcal P(A)=[p;r]$ 包含三维平移 $p$ 与六维连续旋转表示 $r$。位姿动作和 wrench 使用对应机器人基坐标系；差值按训练集动作标准差缩放。旋转修正在教师六维动作坐标中相加，经过 Gram–Schmidt 正交化得到合法旋转。双臂输入输出拼接每臂量，逐分量构造修正，并对每臂独立限幅。

视觉—语言前缀 $E_k$ 不包含状态、力或动作 token。$m_k$ 选择可用图像视角及非 padding 语言 token。带掩码的均值池化将填充后的前缀分成 $M=16$ 个连续、近似等宽的位置区间：

$$
(\bar E_k,\bar m_k)=\operatorname{Pool}_M(E_k,m_k),\qquad
Z_k^{\mathrm{ctx}}=P_{\mathrm{ctx}}(\bar E_k,\bar m_k).
$$

学习投影器通过 $Q=2$ 个查询聚合有效池化 token。各条件的投影将任务上下文、力历史、状态、参考动作片段和时序特征映射到共享 token 维度。各条件使用学习式类型嵌入，条件 token 集合不使用位置编码。条件 token 及其类型嵌入一起置换时，固定修正查询的输出保持不变。力与延迟的两次前向计算共享注意力模块参数并使用独立输出投影；力计算使用全部条件，延迟计算屏蔽力 token。

缓存年龄和插值相位表示为

$$
\xi_{t,k}=\left[\min\left(\frac{t-t_k}{c},1\right);\alpha_{t,k}\right],\qquad c=0.6\text{ s},
$$

$\alpha_{t,k}$ 为 $t$ 在相邻参考动作样本之间的位置。

动作表示及旋转组合见主文 III-A。令 $\widetilde S^p$ 为机器人状态转换到连续六维旋转表示后的归一化位姿坐标，每臂九维；$\sigma_S^p$ 与 $\sigma_A^p$ 为相应训练集状态和动作标准差：

参考状态对齐将当前预测表示为相对于参考动作查询状态 $S_k$ 的动作。

$$
\Gamma(S_t,S_k)=\frac{\sigma_S^p+\varepsilon}{\sigma_A^p+\varepsilon}
\odot(\widetilde S_t^p-\widetilde S_k^p),\qquad\varepsilon=10^{-6}.
$$

这一经过尺度转换的逐分量差值包含在学习式延迟修正目标中，将当前无力输入预测与参考动作置于同一归一化差值动作坐标系。组合动作先反归一化，再相对于参考动作查询状态 $S_k$ 应用，最后通过 Gram–Schmidt 投影得到合法旋转。

修正策略观察与输出时间戳对齐的完整 $K$ 步参考动作片段。该片段展平并投影为一个带类型的参考动作 token，使预测可使用对应未来参考动作路点，且不增加注意力序列长度。

部署前，两项修正在逐分量上独立限幅。实现支持非负九维边界 $b_F,b_S\in\mathbb R_{\geq0}^9$，或广播到位姿坐标的标量；双臂分别限幅。<span style="color:red">最终机器人配置仍需填写实际使用的 $b_F$ 和 $b_S$。</span>

训练样本锚定于 30 Hz 状态/动作时间戳，保留 $\mathcal F_t$ 内全部 100 Hz wrench 样本。独立采样查询周期 $T_i\sim\mathcal U(1/15,1/5)$ s，查询延迟 $L_i\sim\mathcal U(0.05,0.30)$ s。先在记录格点贪心选择查询行，再加入时间戳抖动 $J_i\sim\mathcal U(-0.01,0.01)$ s，以覆盖非格点插值。部署修正查询可更频繁启动；实际修正更新率应直接测量，不能由块内采样频率推算。

### 1.2 无力输入模式优化

学习 token 和专用 adapter 仅用于无力输入查询。冻结教师参数 $\theta$，按原 flow-matching 目标优化参考动作参数 $\phi$：

$$
\mathcal L_{\mathrm{ref}}=\mathbb E_{\mathcal D,\eta,\epsilon}
\left[\ell_{\mathrm{FM}}(T_{\theta,\phi}^{\mathrm{ref}},A_{t:t+H-1}^*;\eta,\epsilon)\right].
$$

$\eta$ 是 flow time，$\epsilon$ 是初始 flow noise。同一修正目标中的力条件和无力输入预测共享 $\epsilon$。

### 1.3 修正损失加权

$K$ 步修正块的归一化时间权重为

$$w_j=\frac{\beta^{j-1}}{\sum_{m=1}^K\beta^{m-1}},\qquad0<\beta\leq1.$$

$\beta<1$ 更重视近期修正；$\beta=1$ 各步等权。$\sum_jw_j=1$ 保持随 $K$ 变化的损失尺度稳定。损失是归一化位姿动作坐标中的均方误差。

## 2. 参考动作与修正的异步执行

### 2.1 参考动作更新

参考动作查询 $k$ 在状态 $S_k$ 下于 $t_k$ 启动，并在结果于 $\bar t_k$ 可用时发布固定的参考动作更新 $(A_{T,k,1:H}^{\mathrm{ref}},\bar E_k,\bar m_k,S_k,t_k,\bar t_k)$。各 参考动作样本的预定执行时刻为

$$
t_{k,n}^{\mathrm{ref}}=t_k+(n-1)/f_A,
\qquad n=1,\ldots,H.
$$

在时刻 $t$，当前有效参考动作是已经完成且未被更新查询取代的最新查询：

$$
k^*(t)=\arg\max_{k:\,\bar t_k\leq t}t_k.
$$

因此，较旧查询即使更晚完成，也不能替换更新的当前 参考动作更新。预定执行时刻早于 $\bar t_{k^*}$ 的样本不再直接执行，但缓存动作块本身不会被截断。对于 $t\geq\bar t_{k^*}$，参考动作位姿 $A_{k^*}^{\mathrm{ref}}(t)$ 通过原始带时间戳动作块进行插值得到。

### 2.2 修正更新

修正 查询 $q$ 在 $t_q^F$ 启动，读取 $k_q=k^*(t_q^F)$ 以及 $(\mathcal F_{t_q^F},S_{t_q^F})$，并在 $\bar t_q^F$ 完成时输出力条件和延迟修正块。各修正样本的时间戳为

$$
t^F_{q,j}=t_q^F+(j-1)/f_A,
\qquad j=1,\ldots,K.
$$

其中，$f_A$ 定义 correction chunk 内修正样本之间的时间间隔，修正 查询启动时刻 $t_q^F$ 则独立调度。第 $j$ 个修正步骤在 $[t^F_{q,j},t^F_{q,j}+1/f_A)$ 内保持不变，不对修正样本进行插值。控制时刻 $t$ 选择启动时间最新、结果已经完成且仍然有效的 修正 查询，并确定其当前有效步骤：

$$
\begin{aligned}
q^*(t)&=\arg\max_{q:\,\bar t_q^F\leq t<t_q^F+K/f_A}t_q^F,\\
j(t;q^*)&=1+\left\lfloor(t-t_{q^*}^F)f_A\right\rfloor.
\end{aligned}
$$

因此，推理期间已经过期的前缀被跳过，其余有效步骤依次执行，直至更新的 correction chunk 完成。令 $j^*=j(t;q^*)$。在当前所选区间内，主文式 (1) 使用所选 修正 查询的启动时刻与当前有效步骤计算：

$$
\begin{aligned}
\mathcal P(A^{\mathrm{cmd}}(t))
\equiv{}&\mathcal P(A^{\mathrm{cmd}}_{t_{q^*}^F,j^*})\\
={}&\mathcal P\!\left(A_{k_{q^*}}^{\mathrm{ref}}
(t^F_{q^*,j^*})\right)
+\Delta\hat A^{\mathrm{force}}_{q^*,j^*}
+\Delta\hat A^{\mathrm{delay}}_{q^*,j^*}.
\end{aligned}
$$

两项修正分别逐分量限幅，并在归一化 delta-action 坐标中组合。延迟修正的训练目标已包含 $\Gamma$，部署不另加该项。组合后的指令仍以 $S_{k_{q^*}}$ 为基准，再通过主文定义的映射 $\mathcal R$ 转换为绝对机器人指令。在 $t_q^F+K/f_A$ 之后才完成的 correction chunk 将被拒绝；若不再存在有效 correction chunk，执行器调用平台的安全保持或中止策略。<span style="color:red">最终实现记录需要补充 gripper 的采样方式，以及超过最后一个有效 参考动作 时间戳后的处理方式。</span>

## 3. 补充评估协议

### 3.1 任务成功标准

以下红色阈值为待核对协议：

| 平台 | 任务 | 成功标准 |
|---|---|---|
| 单臂 | 翻物体 | <span style="color:red">物体旋转至少 150° 并保持稳定</span> |
| 单臂 | 插 USB | <span style="color:red">连接器距最终插入深度不超过 2 mm</span> |
| 单臂 | 开柜门 | <span style="color:red">柜门打开超过 60°</span> |
| 单臂 | 按按钮 | <span style="color:red">按钮被完全触发</span> |
| 单臂 | 擦白板 | <span style="color:red">至少清除 90% 的标记区域</span> |
| 双臂 | 拔插头 | <span style="color:red">插头完全分离且未掉落任一部件</span> |
| 双臂 | 插插头 | <span style="color:red">一臂稳定插座时，插头被完全插入</span> |
| 双臂 | 拉抽屉 | <span style="color:red">抽屉拉出至少 15 cm</span> |
| 双臂 | 擦白板 | <span style="color:red">左臂固定白板、右臂执行擦除，并至少清除 90% 的标记区域</span> |

### 3.2 指标聚合

与主文一致，峰值力和完成时间报告各平台内跨任务汇集的成功试验均值 ± 标准差。因此，每任务对汇集指标的贡献取决于该方法在该任务上的成功次数。主实验成功率为九任务平均，每方法每任务 20 次试验。未见物体评估包含三项任务，每任务 10 次、每方法共 30 次；成功率为成功总次数除以 30。<span style="color:red">单试验双臂力聚合的精确定义，以及完成时间的起止边界，仍需与最终评估代码核对。</span>

### 3.3 逐任务试验次数

<span style="color:red">最终实验记录将列出每方法每任务的成功试验数。每个计数以 20 次评估为分母，汇总后对应主文成功率表。</span>
