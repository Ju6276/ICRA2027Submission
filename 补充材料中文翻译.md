# ForceDelta-VLA 补充材料中文翻译

## 1. 训练与架构细节

<span style="color:red">Teacher 使用 AdamW 训练 50,000 步，峰值学习率为 $2\times10^{-5}$。它根据 100 Hz wrench 流中最近 100 ms 的历史，以 30 Hz 预测长度为 $H=50$ 的动作块；flow-matching 推理使用 $N_{\mathrm{flow}}=10$ 步。</span>

冻结 teacher 后，<span style="color:red">rank-32 力空值模式 adapter 以 $10^{-4}$ 的学习率训练 10,000 步。视觉—语言前缀被池化为 $M=16$ 个 intent token。残差策略由因果力编码器和一个 pre-norm attention block 构成，并使用独立的零初始化 force 与 staleness 输出投影。该策略预测 $K=5$ 个修正，并在重放异步调度下以 $3\times10^{-4}$ 的学习率训练 30,000 步。最终实现记录还需明确 intent-token pooling 的具体规则，以及 staleness head 屏蔽 force token 时所使用的 embedding。</span>

动作表示和旋转残差组合方式已在主文第三章 A 节定义。部署时，力条件修正与陈旧性修正在归一化位姿—动作空间中分别限幅，再与参考动作组合。<span style="color:red">令 $\mu_A$ 和 $D_A=\operatorname{diag}(\sigma_A)$ 表示训练集动作统计量。绝对动作坐标采用 $\mathcal N(a)=D_A^{-1}(a-\mu_A)$，动作差值与重基准项采用仅缩放映射 $\mathcal N_\Delta(\delta a)=D_A^{-1}\delta a$。绝对指令映射 $\mathcal R$ 在相对于 reference 查询状态应用组合动作前先进行反归一化。最终实现记录必须核对这些约定，并给出两个输出 head 的修正范围。</span>

<span style="color:red">残差策略训练样本锚定在 30 Hz 的状态/动作时间戳上，同时保留 $\mathcal F_t$ 内所有 100 Hz wrench 样本。部署时 residual packet 可以更频繁地发起查询；实际发布率直接测量，而不是由 packet 内动作采样频率推算。</span>

### 1.1 力空值模式优化

学习式力空值 token 与力空值专用 adapter 仅在力空值查询中启用。保持力条件 teacher 参数 $\theta$ 冻结，只使用 teacher 原有的 flow-matching 目标优化力空值模式参数 $\phi$：

$$
\mathcal L_{\mathrm{null}}
=\mathbb E_{\mathcal D,\eta,\epsilon}
\left[\ell_{\mathrm{FM}}\!\left(
T_{\theta,\phi}^{\mathrm{null}},
A_{t:t+H-1}^{*};\eta,\epsilon\right)\right].
$$

其中，$\eta$ 表示 flow time，$\epsilon$ 表示初始 flow noise。用于构造同一残差目标的完整力条件预测和力空值预测共享相同的 $\epsilon$。

### 1.2 残差损失的时序权重

对于长度为 $K$ 的残差动作块，归一化时序权重为

$$
w_j=\frac{\beta^{j-1}}{\sum_{m=1}^{K}\beta^{m-1}},
\qquad 0<\beta\leq1.
$$

当 $\beta<1$ 时，损失更重视近期修正；当 $\beta=1$ 时，各预测步权重相同。由于 $\sum_jw_j=1$，改变 $K$ 时损失尺度基本保持稳定。损失是在归一化位姿—动作坐标中计算的均方误差。

## 2. 异步数据包执行

### 2.1 Reference 数据包

Reference 查询 $k$ 在状态 $S_k$ 下于 $t_k$ 发起，并在结果于 $\bar t_k$ 可用时发布不可变数据包 $(A_{T,k,1:H}^{\mathrm{null}},Z_k^{\mathrm{intent}},S_k,t_k,\bar t_k)$。各 reference 样本的预定执行时刻为

$$
t_{k,n}^{\mathrm{ref}}=t_k+(n-1)/f_A,
\qquad n=1,\ldots,H.
$$

在时刻 $t$，当前有效 reference 是已经完成且未被更新查询取代的最新查询：

$$
k^*(t)=\arg\max_{k:\,\bar t_k\leq t}t_k.
$$

因此，较旧查询即使更晚完成，也不能替换更新的当前数据包。预定执行时刻早于 $\bar t_{k^*}$ 的样本不再直接执行，但缓存动作块本身不会被截断。对于 $t\geq\bar t_{k^*}$，reference 位姿 $A_{k^*}^{\mathrm{ref}}(t)$ 通过原始带时间戳动作块进行插值得到。

### 2.2 Residual 数据包

Residual 推理数据包 $q$ 在 $t_q^F$ 发起，读取 $k_q=k^*(t_q^F)$ 以及 $(\mathcal F_{t_q^F},S_{t_q^F})$，并在 $\bar t_q^F$ 完成时输出力条件和陈旧性修正块。各修正样本的时间戳为

$$
t^F_{q,j}=t_q^F+(j-1)/f_A,
\qquad j=1,\ldots,K.
$$

其中，$f_A$ 定义数据包内动作样本之间的时间间隔，residual 查询时刻 $t_q^F$ 则独立调度。第 $j$ 个修正步骤在 $[t^F_{q,j},t^F_{q,j}+1/f_A)$ 内保持不变，不对修正样本进行插值。控制时刻 $t$ 选择发起时间最新、已经完成且仍然有效的数据包，并确定其当前有效步骤：

$$
\begin{aligned}
q^*(t)&=\arg\max_{q:\,\bar t_q^F\leq t<t_q^F+K/f_A}t_q^F,\\
j(t;q^*)&=1+\left\lfloor(t-t_{q^*}^F)f_A\right\rfloor.
\end{aligned}
$$

因此，推理期间已经过期的前缀被跳过，其余有效步骤依次执行，直至更新的数据包完成。令 $j^*=j(t;q^*)$。在当前所选区间内，主文式 (1) 使用 residual 数据包的查询时刻与当前有效步骤计算：

$$
\begin{aligned}
\mathcal P(A^{\mathrm{cmd}}(t))
\equiv{}&\mathcal P(A^{\mathrm{cmd}}_{t_{q^*}^F,j^*})\\
={}&\mathcal P\!\left(A_{k_{q^*}}^{\mathrm{ref}}
(t^F_{q^*,j^*})\right)
+\Delta\hat A^{\mathrm{force}}_{q^*,j^*}
+\Delta\hat A^{\mathrm{stale}}_{q^*,j^*}.
\end{aligned}
$$

两项修正分别限幅，并在归一化 delta-action 坐标中组合。组合后的指令仍以 $S_{k_{q^*}}$ 为基准，再通过主文定义的映射 $\mathcal R$ 转换为绝对机器人指令。在 $t_q^F+K/f_A$ 之后才完成的数据包将被拒绝；若不再存在有效 residual 数据包，执行器调用平台的安全保持或中止策略。<span style="color:red">最终实现记录需要补充 gripper 的采样方式，以及超过最后一个有效 reference 时间戳后的处理方式。</span>

## 3. 补充评估协议

### 3.1 任务成功标准

各任务的具体成功标准汇总于下表。

| 平台 | 任务 | 成功标准 |
|---|---|---|
| 单臂 | 翻物体 | <span style="color:red">物体旋转至少 150° 并保持稳定</span> |
| 单臂 | 插 USB | <span style="color:red">连接器距最终插入深度不超过 2 mm</span> |
| 单臂 | 开柜门 | <span style="color:red">柜门打开超过 60°</span> |
| 单臂 | 按按钮 | <span style="color:red">按钮被完全触发</span> |
| 单臂 | 擦白板 | <span style="color:red">至少清除 90% 的标记区域</span> |
| 双臂 | 拔插头 | <span style="color:red">插头完全分离且未掉落任一部件</span> |
| 双臂 | 插插头 | <span style="color:red">一臂稳定插座时，插头被完全插入</span> |
| 双臂 | 拉抽屉 | <span style="color:red">抽屉拉出至少 15 cm</span> |
| 双臂 | 擦白板 | <span style="color:red">左臂固定白板、右臂执行擦除，并至少清除 90% 的标记区域</span> |

### 3.2 指标聚合

<span style="color:red">峰值力在全部试验上计算。对于双臂试验，峰值力取两个机械臂在全部时刻上的力范数最大值。完成时间仅在成功试验上统计，从第一条策略指令开始，到满足任务成功标准为止。平台层面结果先在每项任务内聚合试验，再分别对五项单臂任务和四项双臂任务作宏平均。这些约定必须与最终评估代码核对。</span>

### 3.3 按 Seed 报告任务结果

<span style="color:red">最终补充材料将报告每个训练 seed、每种方法和每项任务的成功试验数。每个计数均以 10 次试验为分母，并汇总为主文成功率表中的结果。</span>

### 3.4 Residual Bound 敏感性

在 USB 插入和打开抽屉任务上，将部署修正范围设为默认值的 $0.5\times$、$1\times$ 和 $2\times$。力条件与陈旧性输出 head 分别限幅；双臂任务中每条机械臂独立限幅，结果汇总于下表。

| Bound | USB 成功率 (%) | USB 峰值力 (N) | 开抽屉成功率 (%) | 开抽屉峰值力 (N) |
|---|---:|---:|---:|---:|
| $0.5\times$ | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| $1\times$ | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
| $2\times$ | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> | <span style="color:red">X</span> |
