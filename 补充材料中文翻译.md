# ForceDelta-VLA 补充材料中文翻译

## 实现细节

时序力条件 teacher 在八张 NVIDIA A100 GPU 上进行全参数微调。所有真实机器人推理和 runtime 测量均使用一张 48 GB NVIDIA RTX 4090D GPU。<span style="color:red">Teacher 使用 AdamW 训练 50,000 步，峰值学习率为 $2\times10^{-5}$。它根据 100 Hz wrench 流中最近 100 ms 的历史，以 30 Hz 预测长度为 $H=50$ 的动作块；flow-matching 推理使用 $N_{\mathrm{flow}}=10$ 步。</span>

冻结 teacher 后，<span style="color:red">rank-32 空力模式 adapter 以 $10^{-4}$ 的学习率训练 10,000 步。视觉—语言前缀被池化为 $M=16$ 个 intent token。残差策略由因果力编码器和一个 pre-norm attention block 构成，并使用独立的 force 与 staleness 输出头。该策略预测 $K=5$ 个修正，并在重放异步调度下以 $3\times10^{-4}$ 的学习率训练 30,000 步。</span>

动作表示和旋转残差组合方式已在主文第三章 A 节定义。部署时，力条件修正与陈旧性修正在归一化位姿—动作空间中分别限幅，再与参考动作组合。
