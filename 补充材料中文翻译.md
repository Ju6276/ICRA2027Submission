# ForceDelta-VLA 补充材料中文翻译

## 1. 训练与架构细节

时序力条件 teacher 在八张 NVIDIA A100 GPU 上进行全参数微调。所有真实机器人推理和 runtime 测量均使用一张 48 GB NVIDIA RTX 4090D GPU。<span style="color:red">Teacher 使用 AdamW 训练 50,000 步，峰值学习率为 $2\times10^{-5}$。它根据 100 Hz wrench 流中最近 100 ms 的历史，以 30 Hz 预测长度为 $H=50$ 的动作块；flow-matching 推理使用 $N_{\mathrm{flow}}=10$ 步。</span>

冻结 teacher 后，<span style="color:red">rank-32 空力模式 adapter 以 $10^{-4}$ 的学习率训练 10,000 步。视觉—语言前缀被池化为 $M=16$ 个 intent token。残差策略由因果力编码器和一个 pre-norm attention block 构成，并使用独立的 force 与 staleness 输出头。该策略预测 $K=5$ 个修正，并在重放异步调度下以 $3\times10^{-4}$ 的学习率训练 30,000 步。最终实现记录还需明确 intent-token pooling 的具体规则，以及 staleness head 屏蔽 force token 时所使用的 embedding。</span>

动作表示和旋转残差组合方式已在主文第三章 A 节定义。部署时，力条件修正与陈旧性修正在归一化位姿—动作空间中分别限幅，再与参考动作组合。<span style="color:red">最终实现记录需要补充归一化统计量和两个输出 head 各自的修正范围。</span>

### 1.1 空力模式优化

学习式空力 token 与空力专用 adapter 仅在空力查询中启用。保持力条件 teacher 参数 $\theta$ 冻结，只使用 teacher 原有的 flow-matching 目标优化空力模式参数 $\phi$：

$$
\mathcal L_{\mathrm{null}}
=\mathbb E_{\mathcal D,\eta,\epsilon}
\left[\ell_{\mathrm{FM}}\!\left(
T_{\theta,\phi}^{\mathrm{null}},
A_{t:t+H-1}^{*};\eta,\epsilon\right)\right].
$$

其中，$\eta$ 表示 flow time，$\epsilon$ 表示初始 flow noise。用于构造同一残差目标的完整力条件预测和空力预测共享相同的 $\epsilon$。

### 1.2 残差损失的时序权重

对于长度为 $K$ 的残差动作块，归一化时序权重为

$$
w_j=\frac{\beta^{j-1}}{\sum_{m=1}^{K}\beta^{m-1}},
\qquad 0<\beta\leq1.
$$

当 $\beta<1$ 时，损失更重视近期修正；当 $\beta=1$ 时，各预测步权重相同。由于 $\sum_jw_j=1$，改变 $K$ 时损失尺度基本保持稳定。损失是在归一化位姿—动作坐标中计算的均方误差。

## 2. 异步数据包执行

### 2.1 Reference 数据包

Reference 查询 $k$ 在状态 $S_k$ 下于 $t_k$ 发起，并在推理完成时刻 $\bar t_k$ 发布不可变数据包 $(A_{T,k,1:H}^{\mathrm{null}},Z_k^{\mathrm{intent}},S_k,t_k,\bar t_k)$。各 reference 样本的预定执行时刻为

$$
t_{k,n}^{\mathrm{ref}}=t_k+(n-1)/f_A,
\qquad n=1,\ldots,H.
$$

预定执行时刻早于 $\bar t_k$ 的样本不再直接执行，但缓存动作块本身不会被截断。对于 $t\geq\bar t_k$，reference 位姿 $A_k^{\mathrm{ref}}(t)$ 通过原始带时间戳动作块进行插值得到。

### 2.2 Residual 数据包

Residual 推理数据包 $q$ 在 $t_q^F$ 发起，读取最新 reference 数据包 $k_q$ 以及 $(\mathcal F_{t_q^F},S_{t_q^F})$，并在 $\bar t_q^F$ 完成时输出力条件和陈旧性修正块。各修正样本的时间戳为

$$
t^F_{q,j}=t_q^F+(j-1)/f_A,
\qquad j=1,\ldots,K.
$$

第 $j$ 个修正步骤在 $[t^F_{q,j},t^F_{q,j}+1/f_A)$ 内保持不变，不对修正样本进行插值。控制时刻 $t$ 选择发起时间最新、已经完成且仍然有效的数据包，并确定其当前有效步骤：

$$
\begin{aligned}
q^*(t)&=\arg\max_{q:\,\bar t_q^F\leq t<t_q^F+K/f_A}t_q^F,\\
j(t;q^*)&=1+\left\lfloor(t-t_{q^*}^F)f_A\right\rfloor.
\end{aligned}
$$

因此，推理期间已经过期的前缀被跳过，其余有效步骤依次执行，直至更新的数据包完成。在当前所选区间内，主文式 (1) 中的运行时变量为

$$
\begin{aligned}
k&=k_{q^*},\\
A_k^{\mathrm{ref}}(t)
&\equiv A_{k_{q^*}}^{\mathrm{ref}}
(t^F_{q^*,j(t;q^*)}),\\
\Delta\hat A^{(\cdot)}(t)
&=\Delta\hat A^{(\cdot)}_{q^*,j(t;q^*)}.
\end{aligned}
$$

两项修正分别限幅，并在归一化 delta-action 坐标中组合。组合后的指令仍以 $S_{k_{q^*}}$ 为基准，再通过主文定义的映射 $\mathcal R$ 转换为绝对机器人指令。在 $t_q^F+K/f_A$ 之后才完成的数据包将被拒绝；若不再存在有效 residual 数据包，执行器调用平台的安全保持或中止策略。<span style="color:red">最终实现记录需要补充 gripper 的采样方式，以及超过最后一个有效 reference 时间戳后的处理方式。</span>
