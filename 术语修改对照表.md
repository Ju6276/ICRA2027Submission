# 术语修改对照表

以下为当前论文使用的英文名称及中文对应；旧表达仅用于追踪修改。

| 此前表达 | 当前英文 | 中文对应 |
|---|---|---|
| base action / cached base action | reference action | 参考动作 |
| force residual / force-responsive residual | force correction | 力修正 |
| staleness residual / staleness correction | delay correction | 延迟修正 |
| shared set-attention backbone | shared attention module | 共享注意力模块 |
| residual policy | correction policy | 修正策略 |
| residual-distillation framework | correction-distillation framework | 修正蒸馏框架 |
| state rebasing | reference-state alignment | 参考状态对齐 |
| offline teacher | frozen teacher（描述参数固定状态时） | 冻结教师 |
| state-consistent dual-correction decomposition 与 independently executable correction pathway（两项贡献） | state-consistent dual-correction policy（合并后的第二项贡献） | 状态一致的双修正策略 |

以下专业术语继续保留：

| 英文 | 中文对应 | 含义 |
|---|---|---|
| force-conditioned prediction | 力条件预测 | 使用力历史的预测 |
| force-agnostic prediction | 无力输入预测 | 使用学习得到的 token 和 adapter，不等于输入零力 |
| force-agnostic correction distillation | 无力输入修正蒸馏 | 第一项方法贡献：通过匹配教师预测构造力修正目标 |
| asynchronous schedule replay | 异步调度重放 | 用采样得到的查询时间和延迟构造训练条件 |

参考状态对齐将当前预测表示为相对于已有参考动作生成时状态的动作。Γ 已包含在延迟监督目标中，部署时不重复添加；将组合动作转换为绝对命令是后续的独立步骤。

当前贡献结构为两项方法贡献与一项性能提升；合并贡献名称不改变方法定义。
